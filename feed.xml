<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://crystalhansen.ca/feed.xml" rel="self" type="application/atom+xml"/><link href="https://crystalhansen.ca/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-17T20:10:33+00:00</updated><id>https://crystalhansen.ca/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Running Maven Builds: From Testing to EC2 Deployment</title><link href="https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2/" rel="alternate" type="text/html" title="Running Maven Builds: From Testing to EC2 Deployment"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2/"><![CDATA[<p>Whether you’re shipping a microservice or a full-stack Java application, Maven is the backbone of most JVM build pipelines. This guide walks through the full lifecycle — running tests, packaging your app, and deploying it to an AWS EC2 instance — with practical commands you can drop straight into your workflow.</p> <hr/> <h2 id="prerequisites">Prerequisites</h2> <p>Before you start, make sure you have:</p> <ul> <li><strong>Java 11+</strong> installed (<code class="language-plaintext highlighter-rouge">java -version</code>)</li> <li><strong>Maven 3.6+</strong> installed (<code class="language-plaintext highlighter-rouge">mvn -version</code>)</li> <li>An AWS EC2 instance running (Amazon Linux 2 or Ubuntu)</li> <li>SSH access to the instance (your <code class="language-plaintext highlighter-rouge">.pem</code> key file)</li> <li>Java installed on the EC2 instance</li> </ul> <hr/> <h2 id="part-1-running-tests-with-maven">Part 1: Running Tests with Maven</h2> <p>Maven’s test lifecycle is straightforward. The key phases you’ll use are <code class="language-plaintext highlighter-rouge">test</code>, <code class="language-plaintext highlighter-rouge">verify</code>, and <code class="language-plaintext highlighter-rouge">surefire:test</code>.</p> <h3 id="run-all-unit-tests">Run all unit tests</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span>
</code></pre></div></div> <p>This compiles your source code and test code, then runs all unit tests found under <code class="language-plaintext highlighter-rouge">src/test/java</code>. Test reports land in <code class="language-plaintext highlighter-rouge">target/surefire-reports/</code>.</p> <h3 id="run-tests-and-continue-on-failure">Run tests and continue on failure</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dmaven</span>.test.failure.ignore<span class="o">=</span><span class="nb">true</span>
</code></pre></div></div> <p>Useful in CI when you want to collect all test failures rather than stopping at the first one.</p> <h3 id="run-a-single-test-class">Run a single test class</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dtest</span><span class="o">=</span>UserServiceTest
</code></pre></div></div> <p>Or target a specific method:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dtest</span><span class="o">=</span>UserServiceTest#shouldReturnUserById
</code></pre></div></div> <h3 id="run-integration-tests">Run integration tests</h3> <p>Integration tests typically live in the <code class="language-plaintext highlighter-rouge">verify</code> phase, often powered by the Maven Failsafe plugin:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn verify
</code></pre></div></div> <p>This runs both unit tests (<code class="language-plaintext highlighter-rouge">*Test.java</code>) and integration tests (<code class="language-plaintext highlighter-rouge">*IT.java</code>) in sequence.</p> <h3 id="skip-tests-entirely">Skip tests entirely</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn package <span class="nt">-DskipTests</span>
</code></pre></div></div> <blockquote> <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">-DskipTests</code> skips test <em>execution</em> but still compiles test classes. Use <code class="language-plaintext highlighter-rouge">-Dmaven.test.skip=true</code> to skip compilation too.</p> </blockquote> <hr/> <h2 id="part-2-building-the-artifact">Part 2: Building the Artifact</h2> <p>Once tests pass, package your application into a deployable artifact — typically a <code class="language-plaintext highlighter-rouge">.jar</code> or <code class="language-plaintext highlighter-rouge">.war</code>.</p> <h3 id="build-a-jar">Build a JAR</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">clean</code> wipes the <code class="language-plaintext highlighter-rouge">target/</code> directory first. <code class="language-plaintext highlighter-rouge">package</code> compiles, tests, and packages. Your artifact ends up at:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>target/your-app-1.0.0.jar
</code></pre></div></div> <h3 id="build-a-fatuber-jar-spring-boot">Build a fat/uber JAR (Spring Boot)</h3> <p>If you’re using Spring Boot or the Maven Shade plugin, <code class="language-plaintext highlighter-rouge">mvn package</code> produces a self-contained executable JAR with all dependencies bundled:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package <span class="nt">-DskipTests</span>
</code></pre></div></div> <p>Run it directly:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java <span class="nt">-jar</span> target/your-app-1.0.0.jar
</code></pre></div></div> <h3 id="build-for-a-specific-maven-profile">Build for a specific Maven profile</h3> <p>Maven profiles let you swap configs — for example, <code class="language-plaintext highlighter-rouge">dev</code> vs <code class="language-plaintext highlighter-rouge">prod</code> database URLs:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package <span class="nt">-Pprod</span> <span class="nt">-DskipTests</span>
</code></pre></div></div> <hr/> <h2 id="part-3-deploying-to-ec2">Part 3: Deploying to EC2</h2> <p>With your artifact built, the next step is getting it onto the EC2 instance. Here are the most practical deployment options.</p> <h3 id="option-a-scp--ssh-simple-and-direct">Option A: SCP + SSH (Simple and Direct)</h3> <p>The most straightforward approach for smaller teams or manual deployments.</p> <p><strong>Step 1 — Copy the JAR to EC2:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scp <span class="nt">-i</span> ~/.ssh/your-key.pem <span class="se">\</span>
  target/your-app-1.0.0.jar <span class="se">\</span>
  ec2-user@&lt;YOUR_EC2_IP&gt;:/home/ec2-user/app/
</code></pre></div></div> <blockquote> <p>On Ubuntu instances, replace <code class="language-plaintext highlighter-rouge">ec2-user</code> with <code class="language-plaintext highlighter-rouge">ubuntu</code>.</p> </blockquote> <p><strong>Step 2 — SSH into the instance:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-i</span> ~/.ssh/your-key.pem ec2-user@&lt;YOUR_EC2_IP&gt;
</code></pre></div></div> <p><strong>Step 3 — Run the application:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /home/ec2-user/app
<span class="nb">nohup </span>java <span class="nt">-jar</span> your-app-1.0.0.jar <span class="o">&gt;</span> app.log 2&gt;&amp;1 &amp;
</code></pre></div></div> <hr/> <h3 id="option-b-maven-wagon-plugin">Option B: Maven Wagon Plugin</h3> <p>Wire the SCP step directly into your Maven build. Add this to your <code class="language-plaintext highlighter-rouge">pom.xml</code>:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;build&gt;</span>
  <span class="nt">&lt;extensions&gt;</span>
    <span class="nt">&lt;extension&gt;</span>
      <span class="nt">&lt;groupId&gt;</span>org.apache.maven.wagon<span class="nt">&lt;/groupId&gt;</span>
      <span class="nt">&lt;artifactId&gt;</span>wagon-ssh<span class="nt">&lt;/artifactId&gt;</span>
      <span class="nt">&lt;version&gt;</span>3.5.3<span class="nt">&lt;/version&gt;</span>
    <span class="nt">&lt;/extension&gt;</span>
  <span class="nt">&lt;/extensions&gt;</span>

  <span class="nt">&lt;plugins&gt;</span>
    <span class="nt">&lt;plugin&gt;</span>
      <span class="nt">&lt;groupId&gt;</span>org.codehaus.mojo<span class="nt">&lt;/groupId&gt;</span>
      <span class="nt">&lt;artifactId&gt;</span>wagon-maven-plugin<span class="nt">&lt;/artifactId&gt;</span>
      <span class="nt">&lt;version&gt;</span>2.0.2<span class="nt">&lt;/version&gt;</span>
      <span class="nt">&lt;configuration&gt;</span>
        <span class="nt">&lt;serverId&gt;</span>ec2-server<span class="nt">&lt;/serverId&gt;</span>
        <span class="nt">&lt;url&gt;</span>scp://ec2-user@<span class="nt">&lt;YOUR_EC2_IP&gt;</span>/home/ec2-user/app<span class="nt">&lt;/url&gt;</span>
        <span class="nt">&lt;fromFile&gt;</span>target/your-app-1.0.0.jar<span class="nt">&lt;/fromFile&gt;</span>
        <span class="nt">&lt;toFile&gt;</span>your-app.jar<span class="nt">&lt;/toFile&gt;</span>
      <span class="nt">&lt;/configuration&gt;</span>
    <span class="nt">&lt;/plugin&gt;</span>
  <span class="nt">&lt;/plugins&gt;</span>
<span class="nt">&lt;/build&gt;</span>
</code></pre></div></div> <p>Add your EC2 SSH key to <code class="language-plaintext highlighter-rouge">~/.m2/settings.xml</code>:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;servers&gt;</span>
  <span class="nt">&lt;server&gt;</span>
    <span class="nt">&lt;id&gt;</span>ec2-server<span class="nt">&lt;/id&gt;</span>
    <span class="nt">&lt;privateKey&gt;</span>/path/to/your-key.pem<span class="nt">&lt;/privateKey&gt;</span>
  <span class="nt">&lt;/server&gt;</span>
<span class="nt">&lt;/servers&gt;</span>
</code></pre></div></div> <p>Then deploy with a single command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package wagon:upload-single <span class="nt">-DskipTests</span>
</code></pre></div></div> <hr/> <h3 id="option-c-github-actions-cicd-pipeline">Option C: GitHub Actions CI/CD Pipeline</h3> <p>For a fully automated pipeline, here’s a GitHub Actions workflow that tests, builds, and deploys on every push to <code class="language-plaintext highlighter-rouge">main</code>:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .github/workflows/deploy.yml</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">Build and Deploy to EC2</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build-and-deploy</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Java</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-java@v3</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">java-version</span><span class="pi">:</span> <span class="s1">'</span><span class="s">17'</span>
          <span class="na">distribution</span><span class="pi">:</span> <span class="s1">'</span><span class="s">temurin'</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run tests</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mvn test</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build artifact</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mvn clean package -DskipTests</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to EC2</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">EC2_HOST</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">EC2_USER</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">EC2_KEY</span><span class="pi">:</span> <span class="s">$</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">echo "$EC2_KEY" &gt; key.pem</span>
          <span class="s">chmod 600 key.pem</span>
          <span class="s">scp -i key.pem -o StrictHostKeyChecking=no \</span>
            <span class="s">target/*.jar $EC2_USER@$EC2_HOST:/home/$EC2_USER/app/app.jar</span>
          <span class="s">ssh -i key.pem -o StrictHostKeyChecking=no $EC2_USER@$EC2_HOST \</span>
            <span class="s">"pkill -f 'java -jar' || true &amp;&amp; \</span>
             <span class="s">nohup java -jar /home/$EC2_USER/app/app.jar &gt; /home/$EC2_USER/app/app.log 2&gt;&amp;1 &amp;"</span>
          <span class="s">rm key.pem</span>
</code></pre></div></div> <p>Store <code class="language-plaintext highlighter-rouge">EC2_HOST</code>, <code class="language-plaintext highlighter-rouge">EC2_USER</code>, and <code class="language-plaintext highlighter-rouge">EC2_SSH_KEY</code> as GitHub repository secrets.</p> <hr/> <h2 id="part-4-running-as-a-systemd-service-recommended-for-production">Part 4: Running as a systemd Service (Recommended for Production)</h2> <p>Instead of <code class="language-plaintext highlighter-rouge">nohup</code>, manage the process with systemd — it restarts automatically on crash and survives reboots.</p> <p><strong>Create a service file on EC2:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/systemd/system/myapp.service
</code></pre></div></div> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[Unit]</span>
<span class="py">Description</span><span class="p">=</span><span class="s">My Java Application</span>
<span class="py">After</span><span class="p">=</span><span class="s">network.target</span>

<span class="nn">[Service]</span>
<span class="py">User</span><span class="p">=</span><span class="s">ec2-user</span>
<span class="py">WorkingDirectory</span><span class="p">=</span><span class="s">/home/ec2-user/app</span>
<span class="py">ExecStart</span><span class="p">=</span><span class="s">/usr/bin/java -jar /home/ec2-user/app/app.jar</span>
<span class="py">SuccessExitStatus</span><span class="p">=</span><span class="s">143</span>
<span class="py">Restart</span><span class="p">=</span><span class="s">on-failure</span>
<span class="py">RestartSec</span><span class="p">=</span><span class="s">10</span>
<span class="py">StandardOutput</span><span class="p">=</span><span class="s">journal</span>
<span class="py">StandardError</span><span class="p">=</span><span class="s">journal</span>

<span class="nn">[Install]</span>
<span class="py">WantedBy</span><span class="p">=</span><span class="s">multi-user.target</span>
</code></pre></div></div> <p><strong>Enable and start the service:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl daemon-reload
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>myapp
<span class="nb">sudo </span>systemctl start myapp
</code></pre></div></div> <p><strong>Check status and tail logs:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status myapp
<span class="nb">sudo </span>journalctl <span class="nt">-u</span> myapp <span class="nt">-f</span>
</code></pre></div></div> <p>To deploy a new version, copy the new JAR and restart:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart myapp
</code></pre></div></div> <hr/> <h2 id="quick-reference">Quick Reference</h2> <table> <thead> <tr> <th style="text-align: left">Goal</th> <th style="text-align: left">Command</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Run all tests</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn test</code></td> </tr> <tr> <td style="text-align: left">Run integration tests</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn verify</code></td> </tr> <tr> <td style="text-align: left">Run a single test</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn test -Dtest=MyTest</code></td> </tr> <tr> <td style="text-align: left">Build (skip tests)</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn clean package -DskipTests</code></td> </tr> <tr> <td style="text-align: left">Build for prod profile</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn clean package -Pprod</code></td> </tr> <tr> <td style="text-align: left">Copy JAR to EC2</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">scp -i key.pem target/app.jar ec2-user@HOST:/path/</code></td> </tr> <tr> <td style="text-align: left">SSH to EC2</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">ssh -i key.pem ec2-user@HOST</code></td> </tr> <tr> <td style="text-align: left">Restart systemd service</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">sudo systemctl restart myapp</code></td> </tr> </tbody> </table> <hr/> <h2 id="wrapping-up">Wrapping Up</h2> <p>The Maven build lifecycle covers everything from compiling and testing to packaging, and with a few shell commands (or a CI pipeline), you can take that artifact all the way to a running EC2 instance. For production workloads, running your app as a systemd service is worth the five-minute setup — you get automatic restarts, centralized logging via <code class="language-plaintext highlighter-rouge">journalctl</code>, and clean start/stop semantics.</p> <p>From here, the natural next step is moving toward a more managed deployment target — ECS, Elastic Beanstalk, or a CodeDeploy-managed EC2 fleet — but this pipeline gets most teams very far, very fast.</p>]]></content><author><name></name></author><category term="devops"/><category term="maven"/><category term="java"/><category term="aws"/><category term="ec2"/><category term="devops"/><category term="ci-cd"/><summary type="html"><![CDATA[A practical guide to running mvn test, packaging your Java app, and deploying it to AWS EC2 — including CI/CD with GitHub Actions and running as a systemd service.]]></summary></entry><entry><title type="html">Layer 5 — Spring Security: Role-Based Authorization and CORS</title><link href="https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors/" rel="alternate" type="text/html" title="Layer 5 — Spring Security: Role-Based Authorization and CORS"/><published>2026-05-12T14:40:00+00:00</published><updated>2026-05-12T14:40:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>By the time a request reaches Spring Security’s authorization layer, it has already cleared four checkpoints:</p> <ol> <li><strong>nginx</strong> — TLS terminated, tenant host header preserved</li> <li><strong>TenantResolverFilter</strong> — tenant identity established</li> <li><strong>JwtAuthenticationFilter</strong> — user identity established (or not)</li> <li><strong>ApiKeyFilter</strong> — machine identity established (or not)</li> </ol> <p>Layer 5 answers the final question: <strong>is this authenticated principal allowed to do what they’re asking to do?</strong></p> <p>This is also where CORS is configured — determining which frontend origins are permitted to make cross-origin requests to the API.</p> <hr/> <h2 id="authorization-vs-authentication">Authorization vs Authentication</h2> <p>These terms are often conflated. In this filter chain they are strictly separated:</p> <ul> <li><strong>Authentication</strong> (Layers 3 and 4): <em>Who are you?</em> Verified by signature, token validity, key hash.</li> <li><strong>Authorization</strong> (Layer 5): <em>Are you allowed to do this?</em> Decided by roles, tenant context, and endpoint rules.</li> </ul> <p>A user can be perfectly authenticated but unauthorized — a valid <code class="language-plaintext highlighter-rouge">ROLE_USER</code> JWT that tries to reach an <code class="language-plaintext highlighter-rouge">ROLE_ADMIN</code> endpoint should get <code class="language-plaintext highlighter-rouge">403 Forbidden</code>, not <code class="language-plaintext highlighter-rouge">401 Unauthorized</code>. The distinction matters both for security semantics and client error handling.</p> <hr/> <h2 id="the-role-model">The Role Model</h2> <p>In a multi-tenant SaaS, roles need to carry tenant scope. A user who is an admin for tenant A must not be able to access tenant B’s admin endpoints — even with a valid token.</p> <p>We enforce this at two levels:</p> <p><strong>Level 1 — Token claims</strong> (Layer 3): The JWT’s <code class="language-plaintext highlighter-rouge">domain</code> claim is cross-checked against the resolved tenant context. A token for tenant A can’t be presented to tenant B’s API subdomain.</p> <p><strong>Level 2 — Spring Security authorization</strong> (Layer 5): <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> checks roles. Roles are loaded from the JWT claims for the current tenant, so the role context is already scoped to the right tenant by the time authorization runs.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Roles in use</span>
<span class="no">ROLE_USER</span>        <span class="c1">// standard authenticated user</span>
<span class="no">ROLE_ADMIN</span>       <span class="c1">// tenant administrator (full access to tenant data)</span>
<span class="no">ROLE_SUPERADMIN</span>  <span class="c1">// platform administrator (cross-tenant operations)</span>
<span class="no">ROLE_API_CLIENT</span>  <span class="c1">// machine-to-machine (API key authenticated)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ROLE_SUPERADMIN</code> is the only role that crosses tenant boundaries — and it’s never issued via the normal JWT flow. It’s only used for platform-level admin operations (provisioning, billing, monitoring).</p> <hr/> <h2 id="securityfilterchain-configuration">SecurityFilterChain Configuration</h2> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="nd">@EnableMethodSecurity</span>  <span class="c1">// enables @PreAuthorize</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SecurityConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">SecurityFilterChain</span> <span class="nf">filterChain</span><span class="o">(</span><span class="nc">HttpSecurity</span> <span class="n">http</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">http</span>
            <span class="c1">// Disable CSRF for stateless API — session cookie uses SameSite=Lax instead</span>
            <span class="o">.</span><span class="na">csrf</span><span class="o">(</span><span class="n">csrf</span> <span class="o">-&gt;</span> <span class="n">csrf</span><span class="o">.</span><span class="na">disable</span><span class="o">())</span>

            <span class="c1">// CORS configured separately</span>
            <span class="o">.</span><span class="na">cors</span><span class="o">(</span><span class="n">cors</span> <span class="o">-&gt;</span> <span class="n">cors</span><span class="o">.</span><span class="na">configurationSource</span><span class="o">(</span><span class="n">corsConfigurationSource</span><span class="o">()))</span>

            <span class="c1">// Session management — stateless for API, but allow session for browser</span>
            <span class="o">.</span><span class="na">sessionManagement</span><span class="o">(</span><span class="n">session</span> <span class="o">-&gt;</span> <span class="n">session</span>
                <span class="o">.</span><span class="na">sessionCreationPolicy</span><span class="o">(</span><span class="nc">SessionCreationPolicy</span><span class="o">.</span><span class="na">IF_REQUIRED</span><span class="o">))</span>

            <span class="c1">// Authorization rules</span>
            <span class="o">.</span><span class="na">authorizeHttpRequests</span><span class="o">(</span><span class="n">auth</span> <span class="o">-&gt;</span> <span class="n">auth</span>

                <span class="c1">// Public endpoints — no auth required</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">GET</span><span class="o">,</span> <span class="s">"/api/appoint/day/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">POST</span><span class="o">,</span> <span class="s">"/api/appoint/request"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">GET</span><span class="o">,</span> <span class="s">"/api/resources/public/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

                <span class="c1">// Auth endpoints — permit all (they ARE the auth flow)</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/auth/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

                <span class="c1">// Admin endpoints</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"ADMIN"</span><span class="o">)</span>

                <span class="c1">// Platform endpoints — superadmin only</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/platform/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"SUPERADMIN"</span><span class="o">)</span>

                <span class="c1">// API client endpoints</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/automation/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"API_CLIENT"</span><span class="o">)</span>

                <span class="c1">// Everything else requires authentication</span>
                <span class="o">.</span><span class="na">anyRequest</span><span class="o">().</span><span class="na">authenticated</span><span class="o">()</span>
            <span class="o">);</span>

        <span class="c1">// Register filters in order</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">tenantResolverFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">jwtAuthenticationFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">apiKeyFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>

        <span class="k">return</span> <span class="n">http</span><span class="o">.</span><span class="na">build</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="method-level-authorization-with-preauthorize">Method-Level Authorization with @PreAuthorize</h2> <p>URL-based authorization (<code class="language-plaintext highlighter-rouge">authorizeHttpRequests</code>) is coarse-grained — it matches URL patterns to roles. Method-level authorization is fine-grained — it checks conditions at the controller or service method.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">CrmController</span> <span class="o">{</span>

    <span class="c1">// Any authenticated user can list clients (for their tenant — data filtered by Hibernate)</span>
    <span class="nd">@GetMapping</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"isAuthenticated()"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ClientDto</span><span class="o">&gt;</span> <span class="nf">listClients</span><span class="o">()</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Only admins can update client records</span>
    <span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ClientDto</span> <span class="nf">updateClient</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="nd">@RequestBody</span> <span class="nc">ClientUpdateDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Only admins can toggle consent — this is a compliance-sensitive operation</span>
    <span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}/consent"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">updateConsent</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="nd">@RequestBody</span> <span class="nc">ConsentDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Admins can delete, but not API clients — even if they have ROLE_API_CLIENT</span>
    <span class="nd">@DeleteMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN') and !hasRole('API_CLIENT')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">deleteClient</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Why use both URL rules and <code class="language-plaintext highlighter-rouge">@PreAuthorize</code>?</strong> URL rules catch misconfigured requests early (before hitting the controller). <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> is the authoritative check at the business logic boundary. Defense in depth — if a URL rule is accidentally too permissive, the method annotation is a second gate.</p> <hr/> <h2 id="cors-configuration">CORS Configuration</h2> <p>CORS (Cross-Origin Resource Sharing) controls which browser origins can make requests to your API. It’s a browser security mechanism — it does not protect server-to-server calls, but it does protect your users’ sessions from being hijacked by malicious web pages.</p> <h3 id="the-wrong-way">The Wrong Way</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// NEVER do this in production</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOriginPatterns</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"*"</span><span class="o">));</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowCredentials</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>  <span class="c1">// This combination is especially dangerous</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">allowedOriginPatterns("*")</code> with <code class="language-plaintext highlighter-rouge">allowCredentials(true)</code> is invalid in modern CORS specs and may be rejected by the browser — but more importantly, it signals a fundamental misunderstanding of the threat model. Credentials (cookies, auth headers) should never be sent to any origin.</p> <h3 id="the-pattern-to-avoid--regex-that-can-be-tricked">The Pattern to Avoid — Regex That Can Be Tricked</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Looks safe, but can be tricked</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOriginPatterns</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"https://*.yourdomain.com"</span><span class="o">));</span>
</code></pre></div></div> <p>This pattern allows <code class="language-plaintext highlighter-rouge">https://evil-yourdomain.com</code> to pass the check if the regex is imprecise. Subdomain wildcards require careful implementation.</p> <h3 id="the-right-way--explicit-allowlist">The Right Way — Explicit Allowlist</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Bean</span>
<span class="kd">public</span> <span class="nc">CorsConfigurationSource</span> <span class="nf">corsConfigurationSource</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">CorsConfiguration</span> <span class="n">config</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CorsConfiguration</span><span class="o">();</span>

    <span class="c1">// Explicit, complete list of allowed origins</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedOrigins</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span>
        <span class="s">"https://www.tenant-a.example.com"</span><span class="o">,</span>
        <span class="s">"https://www.tenant-b.example.com"</span><span class="o">,</span>
        <span class="s">"https://admin.yoursaas.com"</span>
    <span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedMethods</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"GET"</span><span class="o">,</span> <span class="s">"POST"</span><span class="o">,</span> <span class="s">"PATCH"</span><span class="o">,</span> <span class="s">"DELETE"</span><span class="o">,</span> <span class="s">"OPTIONS"</span><span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedHeaders</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span>
        <span class="s">"Authorization"</span><span class="o">,</span>
        <span class="s">"Content-Type"</span><span class="o">,</span>
        <span class="s">"X-API-KEY"</span>
    <span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowCredentials</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>  <span class="c1">// Required for cookies</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setMaxAge</span><span class="o">(</span><span class="mi">3600L</span><span class="o">);</span>           <span class="c1">// Cache preflight for 1 hour</span>

    <span class="nc">UrlBasedCorsConfigurationSource</span> <span class="n">source</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UrlBasedCorsConfigurationSource</span><span class="o">();</span>
    <span class="n">source</span><span class="o">.</span><span class="na">registerCorsConfiguration</span><span class="o">(</span><span class="s">"/api/**"</span><span class="o">,</span> <span class="n">config</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">source</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>For a multi-tenant system with dynamic tenant onboarding</strong>, maintain the allowed origins list in the same place you store tenant domain configuration — loaded at startup, refreshable via a cache. When a new tenant is onboarded, their frontend domain is added to the CORS allowlist as part of the provisioning process.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Bean</span>
<span class="kd">public</span> <span class="nc">CorsConfigurationSource</span> <span class="nf">corsConfigurationSource</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// Load dynamically from tenant registry</span>
    <span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">allowedOrigins</span> <span class="o">=</span> <span class="n">tenantRegistry</span><span class="o">.</span><span class="na">getAllDomains</span><span class="o">()</span>
        <span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">domain</span> <span class="o">-&gt;</span> <span class="s">"https://www."</span> <span class="o">+</span> <span class="n">domain</span><span class="o">)</span>
        <span class="o">.</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toList</span><span class="o">());</span>

    <span class="c1">// Add platform admin origin</span>
    <span class="n">allowedOrigins</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"https://admin."</span> <span class="o">+</span> <span class="n">platformDomain</span><span class="o">);</span>

    <span class="nc">CorsConfiguration</span> <span class="n">config</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CorsConfiguration</span><span class="o">();</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedOrigins</span><span class="o">(</span><span class="n">allowedOrigins</span><span class="o">);</span>
    <span class="c1">// ... rest of config</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="common-authorization-mistakes">Common Authorization Mistakes</h2> <h3 id="1-authorizing-reads-but-not-writes">1. Authorizing Reads but Not Writes</h3> <p>A common pattern: GET endpoints are secured, but PATCH/DELETE endpoints on the same resource are missed.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Secure the write endpoints explicitly — don't assume URL rules catch everything</span>
<span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
<span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>  <span class="c1">// Don't forget this on write methods</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">update</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="o">...)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>
</code></pre></div></div> <h3 id="2-trusting-the-client-for-role-escalation">2. Trusting the Client for Role Escalation</h3> <p>If a client can pass their own role in a request body or query parameter, and the server uses it without re-validating from the token, that’s a privilege escalation vulnerability.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// WRONG — trusts client-provided role</span>
<span class="nd">@PostMapping</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">create</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">role</span> <span class="o">=</span> <span class="n">dto</span><span class="o">.</span><span class="na">getRole</span><span class="o">();</span>  <span class="c1">// attacker sets this to "ADMIN"</span>
    <span class="n">userService</span><span class="o">.</span><span class="na">createWithRole</span><span class="o">(</span><span class="n">role</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// CORRECT — derive role from authenticated principal</span>
<span class="nd">@PostMapping</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">create</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateDto</span> <span class="n">dto</span><span class="o">,</span>
                   <span class="nd">@AuthenticationPrincipal</span> <span class="nc">UserDetails</span> <span class="n">currentUser</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">role</span> <span class="o">=</span> <span class="n">determineAllowedRole</span><span class="o">(</span><span class="n">currentUser</span><span class="o">);</span>  <span class="c1">// derived from token, not request body</span>
    <span class="n">userService</span><span class="o">.</span><span class="na">createWithRole</span><span class="o">(</span><span class="n">role</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="3-missing-cors-preflight-handling">3. Missing CORS Preflight Handling</h3> <p>Browsers send an <code class="language-plaintext highlighter-rouge">OPTIONS</code> preflight request before state-changing cross-origin requests. If your security config blocks <code class="language-plaintext highlighter-rouge">OPTIONS</code> requests (requiring authentication for all methods), CORS will silently fail.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Ensure OPTIONS is permitted (Spring Security handles this automatically if CORS is configured,</span>
<span class="c1">// but verify it's not being blocked by an overly broad auth rule)</span>
<span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">OPTIONS</span><span class="o">,</span> <span class="s">"/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
</code></pre></div></div> <h3 id="4-using-secured-instead-of-preauthorize">4. Using <code class="language-plaintext highlighter-rouge">@Secured</code> Instead of <code class="language-plaintext highlighter-rouge">@PreAuthorize</code></h3> <p><code class="language-plaintext highlighter-rouge">@Secured</code> only supports role names. <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> supports SpEL expressions, which allows compound conditions (<code class="language-plaintext highlighter-rouge">hasRole('ADMIN') and !hasRole('API_CLIENT')</code>). Prefer <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> for flexibility.</p> <hr/> <h2 id="integration-tests">Integration Tests</h2> <p>The most important tests at this layer are integration tests that exercise the complete filter chain — not mocked units.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SpringBootTest</span>
<span class="nd">@AutoConfigureMockMvc</span>
<span class="kd">class</span> <span class="nc">SecurityConfigIT</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">publicEndpoint_noAuth_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/appoint/day/2026-05-12"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">protectedEndpoint_noAuth_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isUnauthorized</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">protectedEndpoint_userRole_adminEndpoint_returns403</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">userToken</span> <span class="o">=</span> <span class="n">generateToken</span><span class="o">(</span><span class="s">"ROLE_USER"</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/users"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">userToken</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isForbidden</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminEndpoint_adminRole_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">adminToken</span> <span class="o">=</span> <span class="n">generateToken</span><span class="o">(</span><span class="s">"ROLE_ADMIN"</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/users"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">adminToken</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">corsPreflightRequest_allowedOrigin_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">options</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Origin"</span><span class="o">,</span> <span class="s">"https://www.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Access-Control-Request-Method"</span><span class="o">,</span> <span class="s">"GET"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">())</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">header</span><span class="o">().</span><span class="na">exists</span><span class="o">(</span><span class="s">"Access-Control-Allow-Origin"</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">corsPreflightRequest_unknownOrigin_noCorsHeader</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">options</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Origin"</span><span class="o">,</span> <span class="s">"https://evil.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Access-Control-Request-Method"</span><span class="o">,</span> <span class="s">"GET"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">header</span><span class="o">().</span><span class="na">doesNotExist</span><span class="o">(</span><span class="s">"Access-Control-Allow-Origin"</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SpringBootTest</span>
<span class="nd">@AutoConfigureMockMvc</span>
<span class="kd">class</span> <span class="nc">AdminTokenIT</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminApiKey_canAccessAdminEndpoints</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/report/weekly"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">validAdminKey</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminApiKey_cannotAccessSuperadminEndpoints</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/platform/tenants"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">validAdminKey</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isForbidden</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Role Design
  [ ] Roles are documented with their permitted operations
  [ ] ROLE_SUPERADMIN is never issued via normal auth flow
  [ ] Role escalation via request body is not possible

URL-Based Authorization
  [ ] Public endpoints are explicitly listed (no implicit opens)
  [ ] /api/auth/** is allowed (not accidentally locked)
  [ ] OPTIONS method is not blocked (CORS preflight works)

Method-Level Authorization
  [ ] All POST/PATCH/DELETE endpoints have @PreAuthorize
  [ ] Read endpoints returning sensitive data have @PreAuthorize
  [ ] @PreAuthorize uses roles derived from token, not request body

CORS
  [ ] allowedOrigins is an explicit whitelist (no wildcard with credentials)
  [ ] Allowed origins list matches the tenant registry
  [ ] Unknown origin returns no Access-Control-Allow-Origin header
  [ ] CORS preflight (OPTIONS) returns correct headers for known origins

Tests
  [ ] Public endpoint, no auth → 200
  [ ] Protected endpoint, no auth → 401
  [ ] Protected endpoint, wrong role → 403
  [ ] Protected endpoint, correct role → 200
  [ ] CORS: known origin → Access-Control-Allow-Origin present
  [ ] CORS: unknown origin → Access-Control-Allow-Origin absent
  [ ] Admin API key → admin endpoints accessible
  [ ] Admin API key → superadmin endpoints blocked
</code></pre></div></div> <hr/> <h2 id="the-full-chain-in-perspective">The Full Chain in Perspective</h2> <p>This is the final layer. When all five layers are working correctly, a request has passed:</p> <table> <thead> <tr> <th>Layer</th> <th>Concern</th> <th>Failure Response</th> </tr> </thead> <tbody> <tr> <td>nginx</td> <td>TLS, rate limit, logging</td> <td>Connection refused / 429</td> </tr> <tr> <td>TenantResolverFilter</td> <td>Tenant identity</td> <td>400 Bad Request</td> </tr> <tr> <td>JwtAuthenticationFilter</td> <td>User identity</td> <td>401 Unauthorized</td> </tr> <tr> <td>ApiKeyFilter</td> <td>Machine identity</td> <td>401 Unauthorized</td> </tr> <tr> <td>Spring Security</td> <td>Authorization, CORS</td> <td>403 Forbidden / CORS error</td> </tr> </tbody> </table> <p>Each layer has a single job. Each layer has tests. Each layer fails explicitly rather than silently.</p> <p>The result is a security chain you can audit by reading down the filter order — and one where a failure at any layer is visible, testable, and fixable without touching the others.</p> <hr/> <p><em>This concludes the five-layer deep-dive series. Return to the overview: <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS: Layered Filters, JWT, and API Keys</a></em></p>]]></content><author><name></name></author><category term="architecture"/><category term="spring-security"/><category term="authorization"/><category term="cors"/><category term="roles"/><category term="java"/><category term="spring-boot"/><summary type="html"><![CDATA[A deep dive into Spring Security's authorization layer — covering @PreAuthorize, tenant-scoped roles, CORS configuration mistakes, and the integration tests that validate the full security chain.]]></summary></entry><entry><title type="html">Layer 4 — API Key Authentication: Securing Machine-to-Machine Requests</title><link href="https://crystalhansen.ca/blog/2026/layer4-api-key-authentication/" rel="alternate" type="text/html" title="Layer 4 — API Key Authentication: Securing Machine-to-Machine Requests"/><published>2026-05-12T14:30:00+00:00</published><updated>2026-05-12T14:30:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer4-api-key-authentication</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer4-api-key-authentication/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>JWTs work well for human users. A person logs in, gets a token, and uses it for the next hour. But scheduled jobs, internal automation scripts, and admin tooling don’t have a human at the keyboard to re-authenticate. They need a credential that works unattended.</p> <p>API keys solve this — but they introduce their own set of risks that need to be managed carefully.</p> <hr/> <h2 id="when-to-use-api-keys-vs-jwts">When to Use API Keys vs JWTs</h2> <table> <thead> <tr> <th>Scenario</th> <th>Auth Method</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>User logs in via browser</td> <td>JWT + session cookie</td> <td>Short-lived, tied to identity, revocable via session</td> </tr> <tr> <td>Mobile client makes API calls</td> <td>JWT</td> <td>Stateless, short-lived</td> </tr> <tr> <td>Scheduled job (nightly report)</td> <td>API key</td> <td>Unattended, no human to re-auth</td> </tr> <tr> <td>Internal admin tooling</td> <td>API key</td> <td>Trusted internal caller, long-lived credential</td> </tr> <tr> <td>External webhook receiver</td> <td>API key + signature</td> <td>Inbound, no session possible</td> </tr> <tr> <td>Service-to-service (ML scorer)</td> <td>API key or mTLS</td> <td>Machine identity, no user context</td> </tr> </tbody> </table> <p>The key distinction is <strong>attended vs. unattended</strong>. If there’s a human who can re-authenticate, use JWTs. If the caller is a machine running on a schedule or reacting to events, use an API key.</p> <hr/> <h2 id="the-filter">The Filter</h2> <p>The API key filter sits after the JWT filter in the chain. By the time a request reaches this filter, one of two things is true:</p> <ol> <li>The JWT filter already authenticated the request (SecurityContext is populated)</li> <li>No valid JWT was found (SecurityContext is empty)</li> </ol> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@Order</span><span class="o">(</span><span class="mi">3</span><span class="o">)</span>  <span class="c1">// after TenantResolver (1) and JwtFilter (2)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ApiKeyFilter</span> <span class="kd">extends</span> <span class="nc">OncePerRequestFilter</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ApiKeyService</span> <span class="n">apiKeyService</span><span class="o">;</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doFilterInternal</span><span class="o">(</span><span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">,</span>
                                    <span class="nc">HttpServletResponse</span> <span class="n">response</span><span class="o">,</span>
                                    <span class="nc">FilterChain</span> <span class="n">chain</span><span class="o">)</span>
            <span class="kd">throws</span> <span class="nc">ServletException</span><span class="o">,</span> <span class="nc">IOException</span> <span class="o">{</span>

        <span class="c1">// CRITICAL: If a valid session/JWT already authenticated this request, skip.</span>
        <span class="c1">// This is the session passthrough fix — see below.</span>
        <span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">();</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">existingAuth</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">existingAuth</span><span class="o">.</span><span class="na">isAuthenticated</span><span class="o">())</span> <span class="o">{</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="nc">String</span> <span class="n">apiKey</span> <span class="o">=</span> <span class="n">request</span><span class="o">.</span><span class="na">getHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">);</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">apiKey</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="n">apiKey</span><span class="o">.</span><span class="na">isBlank</span><span class="o">())</span> <span class="o">{</span>
            <span class="c1">// No API key provided — pass through. Spring Security decides if auth is required.</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="c1">// Validate the key</span>
        <span class="nc">ApiKeyPrincipal</span> <span class="n">principal</span> <span class="o">=</span> <span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">apiKey</span><span class="o">,</span> <span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">());</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">principal</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">,</span> <span class="s">"Invalid API key"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="c1">// Set authentication in SecurityContext</span>
        <span class="nc">ApiKeyAuthenticationToken</span> <span class="n">auth</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ApiKeyAuthenticationToken</span><span class="o">(</span>
            <span class="n">principal</span><span class="o">,</span>
            <span class="n">principal</span><span class="o">.</span><span class="na">getAuthorities</span><span class="o">()</span>
        <span class="o">);</span>
        <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">setAuthentication</span><span class="o">(</span><span class="n">auth</span><span class="o">);</span>

        <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="the-session-passthrough-bug">The Session Passthrough Bug</h2> <p>This filter has a subtle interaction with the JWT/session filter that produced a real production bug worth documenting.</p> <p><strong>The scenario:</strong></p> <ol> <li>A browser user is logged in with a valid session cookie</li> <li>The user’s request reaches the API key filter</li> <li>The request happens to include an <code class="language-plaintext highlighter-rouge">X-API-KEY</code> header (e.g., from a browser extension, a misconfigured fetch call, or a developer testing tool)</li> <li>That API key is invalid</li> <li>The filter returns <code class="language-plaintext highlighter-rouge">401</code> — even though the user has a perfectly valid session</li> </ol> <p>The user sees a confusing <code class="language-plaintext highlighter-rouge">401 Unauthorized</code> with no apparent reason.</p> <p><strong>The fix:</strong> Check whether the <code class="language-plaintext highlighter-rouge">SecurityContext</code> already contains a valid authentication before evaluating the API key header. If auth already exists, the filter is a no-op. The session is authoritative; the API key header is irrelevant.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This check must be FIRST in the filter, before reading X-API-KEY</span>
<span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">();</span>
<span class="k">if</span> <span class="o">(</span><span class="n">existingAuth</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">existingAuth</span><span class="o">.</span><span class="na">isAuthenticated</span><span class="o">())</span> <span class="o">{</span>
    <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="k">return</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p>This is a good example of why filter ordering and precedence must be explicitly tested, not assumed.</p> <hr/> <h2 id="storing-api-keys-securely">Storing API Keys Securely</h2> <p>An API key is a secret credential. Storing it in plaintext in the database means a database breach exposes every key immediately.</p> <p><strong>The pattern:</strong> store a hash of the key, validate by hashing the incoming value and comparing.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// On key creation — return the plaintext key to the caller ONCE</span>
<span class="kd">public</span> <span class="nc">ApiKeyCreationResult</span> <span class="nf">createKey</span><span class="o">(</span><span class="nc">String</span> <span class="n">tenantDomain</span><span class="o">,</span> <span class="nc">String</span> <span class="n">label</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">rawKey</span> <span class="o">=</span> <span class="n">generateSecureKey</span><span class="o">();</span>       <span class="c1">// 32 bytes of SecureRandom, base64url-encoded</span>
    <span class="nc">String</span> <span class="n">hashedKey</span> <span class="o">=</span> <span class="n">hash</span><span class="o">(</span><span class="n">rawKey</span><span class="o">);</span>           <span class="c1">// SHA-256 or bcrypt</span>

    <span class="nc">ApiKey</span> <span class="n">entity</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ApiKey</span><span class="o">();</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setDomain</span><span class="o">(</span><span class="n">tenantDomain</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setLabel</span><span class="o">(</span><span class="n">label</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setKeyHash</span><span class="o">(</span><span class="n">hashedKey</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setCreatedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">());</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setLastUsedAt</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>
    <span class="n">repository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">entity</span><span class="o">);</span>

    <span class="c1">// Return plaintext key to caller — this is the ONLY time it's available</span>
    <span class="k">return</span> <span class="k">new</span> <span class="nf">ApiKeyCreationResult</span><span class="o">(</span><span class="n">entity</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span> <span class="n">rawKey</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// On validation — hash the incoming key and compare</span>
<span class="kd">public</span> <span class="nc">ApiKeyPrincipal</span> <span class="nf">validate</span><span class="o">(</span><span class="nc">String</span> <span class="n">rawKey</span><span class="o">,</span> <span class="nc">String</span> <span class="n">tenantDomain</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">hash</span> <span class="o">=</span> <span class="n">hash</span><span class="o">(</span><span class="n">rawKey</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">repository</span><span class="o">.</span><span class="na">findByKeyHashAndDomain</span><span class="o">(</span><span class="n">hash</span><span class="o">,</span> <span class="n">tenantDomain</span><span class="o">)</span>
                     <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">key</span> <span class="o">-&gt;</span> <span class="o">{</span>
                         <span class="n">key</span><span class="o">.</span><span class="na">setLastUsedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">());</span>  <span class="c1">// audit trail</span>
                         <span class="n">repository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
                         <span class="k">return</span> <span class="k">new</span> <span class="nf">ApiKeyPrincipal</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
                     <span class="o">})</span>
                     <span class="o">.</span><span class="na">orElse</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Hashing algorithm choice:</strong></p> <ul> <li><strong>SHA-256</strong>: Fast, appropriate for API keys since the keys are long (256 bits of entropy) — brute force is infeasible regardless of hash speed.</li> <li><strong>bcrypt</strong>: Better for passwords (short, user-chosen secrets). Adds latency for a high-volume API key endpoint.</li> </ul> <p>For API keys with high entropy, SHA-256 is standard and widely used (GitHub, Stripe, and others follow this pattern).</p> <p><strong>Key format:</strong> Prefix your keys with a recognizable pattern so they’re easy to identify if accidentally leaked in logs, screenshots, or error messages:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myapp_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
^           ^
prefix      32 random bytes (base64url)
</code></pre></div></div> <p>GitHub, Stripe, and Anthropic all use this pattern. Tools like <code class="language-plaintext highlighter-rouge">git secrets</code> and secret scanning in GitHub can be configured to catch these prefixes before they’re committed.</p> <hr/> <h2 id="key-scoping-and-tenant-isolation">Key Scoping and Tenant Isolation</h2> <p>API keys must be tenant-scoped. A key for tenant A must not work against tenant B’s API subdomain.</p> <p>The <code class="language-plaintext highlighter-rouge">validate()</code> method above passes <code class="language-plaintext highlighter-rouge">tenantDomain</code> from <code class="language-plaintext highlighter-rouge">TenantContext</code> and matches it against the key’s stored domain. A valid key for the wrong tenant returns <code class="language-plaintext highlighter-rouge">null</code> and the filter returns <code class="language-plaintext highlighter-rouge">401</code>.</p> <p>This means:</p> <ul> <li>Keys can’t be shared across tenants even intentionally</li> <li>A compromised key for one tenant doesn’t affect others</li> <li>The tenant domain is part of the key’s identity, visible in audit logs</li> </ul> <hr/> <h2 id="rotation-and-revocation">Rotation and Revocation</h2> <p>Unlike JWTs, API keys don’t expire automatically. This is their advantage (no re-auth) and their liability (a leaked key stays valid until explicitly revoked).</p> <p><strong>Rotation procedure:</strong></p> <ol> <li>Create a new key (system returns plaintext once)</li> <li>Update the caller (scheduled job, script) with the new key</li> <li>Verify the new key is working</li> <li>Delete the old key</li> </ol> <p>This is a zero-downtime rotation if done carefully. The two keys coexist briefly during the switchover.</p> <p><strong>Audit trail:</strong> The <code class="language-plaintext highlighter-rouge">lastUsedAt</code> timestamp on each key makes it possible to:</p> <ul> <li>Identify keys that haven’t been used in 90+ days (candidates for removal)</li> <li>Detect unexpected usage patterns (a key used at 3am that normally runs at midnight)</li> <li>Confirm a rotated key is no longer being used before deletion</li> </ul> <hr/> <h2 id="endpoint-allowlist">Endpoint Allowlist</h2> <p>Not every endpoint should accept API key authentication. Admin endpoints for bulk operations, data exports, or configuration changes should require explicit allowlisting rather than being implicitly available to any valid key.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SecurityConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">SecurityFilterChain</span> <span class="nf">filterChain</span><span class="o">(</span><span class="nc">HttpSecurity</span> <span class="n">http</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">http</span><span class="o">.</span><span class="na">authorizeHttpRequests</span><span class="o">(</span><span class="n">auth</span> <span class="o">-&gt;</span> <span class="n">auth</span>
            <span class="c1">// Public endpoints — no auth required</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/appoint/day/**"</span><span class="o">,</span> <span class="s">"/api/appoint/request"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

            <span class="c1">// API-key-accessible admin endpoints — explicitly listed</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/report/weekly"</span><span class="o">).</span><span class="na">hasAuthority</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/import/csv"</span><span class="o">).</span><span class="na">hasAuthority</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)</span>

            <span class="c1">// Everything else requires JWT-based auth</span>
            <span class="o">.</span><span class="na">anyRequest</span><span class="o">().</span><span class="na">hasAnyRole</span><span class="o">(</span><span class="s">"USER"</span><span class="o">,</span> <span class="s">"ADMIN"</span><span class="o">)</span>
        <span class="o">);</span>
        <span class="k">return</span> <span class="n">http</span><span class="o">.</span><span class="na">build</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>This makes the attack surface explicit: you can read the security config and know exactly which endpoints an API key can reach.</p> <hr/> <h2 id="testing">Testing</h2> <p>The test matrix for the API key filter must cover all the interaction cases, especially the session passthrough scenarios:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">ApiKeyFilterTest</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">validApiKey_authenticatesRequest</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">key</span> <span class="o">=</span> <span class="s">"myapp_test_validkeyvalue"</span><span class="o">;</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">))</span>
            <span class="o">.</span><span class="na">thenReturn</span><span class="o">(</span><span class="k">new</span> <span class="nc">ApiKeyPrincipal</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">,</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)));</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">key</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()).</span><span class="na">isNotNull</span><span class="o">();</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isNotEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">invalidApiKey_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">any</span><span class="o">())).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_badkey"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">noApiKey_passesThroughToNextFilter</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// No X-API-KEY header — chain should continue</span>
        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">verify</span><span class="o">(</span><span class="n">chain</span><span class="o">).</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()).</span><span class="na">isNull</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="c1">// THE CRITICAL SESSION PASSTHROUGH TEST</span>
    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">existingValidSession_withInvalidApiKey_doesNotReturn401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// Simulate a valid session already in the SecurityContext (set by JWT filter)</span>
        <span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UsernamePasswordAuthenticationToken</span><span class="o">(</span>
            <span class="s">"alice"</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="nc">AuthorityUtils</span><span class="o">.</span><span class="na">createAuthorityList</span><span class="o">(</span><span class="s">"ROLE_USER"</span><span class="o">)</span>
        <span class="o">);</span>
        <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">setAuthentication</span><span class="o">(</span><span class="n">existingAuth</span><span class="o">);</span>

        <span class="c1">// Add an invalid API key header (simulates browser extension / bad client config)</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_invalidkey"</span><span class="o">);</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">any</span><span class="o">())).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="c1">// MUST pass through — the existing session should NOT be invalidated</span>
        <span class="n">verify</span><span class="o">(</span><span class="n">chain</span><span class="o">).</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isNotEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">validApiKey_wrongTenant_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// Key is valid for tenant-a, but request is for tenant-b</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">eq</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">))).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_tenantakey"</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Key Storage
  [ ] API keys are stored as hashes (SHA-256 or bcrypt), never plaintext
  [ ] Plaintext key is returned exactly once at creation, not stored
  [ ] Keys have a recognizable prefix for secret scanning

Key Design
  [ ] Keys are &gt;= 256 bits of entropy (32 bytes from SecureRandom)
  [ ] Keys are tenant-scoped — cross-tenant use is rejected
  [ ] lastUsedAt is updated on every successful validation (audit trail)

Filter Logic
  [ ] Existing valid session → filter is a no-op (session passthrough)
  [ ] No X-API-KEY header → passes through (endpoint decides)
  [ ] Invalid key → 401 (not 403, not 500)
  [ ] Valid key, wrong tenant → 401

Endpoint Control
  [ ] API-key-accessible endpoints are explicitly listed, not open-ended
  [ ] ROLE_API_CLIENT is distinct from ROLE_USER and ROLE_ADMIN
  [ ] Sensitive endpoints (bulk delete, export) require additional scope or are excluded

Lifecycle
  [ ] Rotation procedure is documented and tested
  [ ] Keys unused for 90+ days are flagged for review
  [ ] Revocation removes the key immediately (no TTL/cache delay)

Tests
  [ ] Valid key → authenticated
  [ ] Invalid key → 401
  [ ] No key → passes through
  [ ] Valid session + invalid key → passes through (not 401)
  [ ] Valid key + wrong tenant → 401
</code></pre></div></div> <hr/> <p><em>Next in the series: <a href="/blog/2026/layer5-spring-security-roles-cors">Layer 5 — Spring Security: Role-Based Authorization and CORS</a></em></p> <hr/> <blockquote> <p><strong>A note on publishing this post:</strong> The first push of this article was rejected by GitHub’s secret scanner because the example key format (<code class="language-plaintext highlighter-rouge">sk_live_...</code>) matched Stripe’s live API key regex exactly. A blog post about API key security, blocked for containing what looked like a real API key. The examples were updated to use a clearly app-namespaced prefix (<code class="language-plaintext highlighter-rouge">myapp_live_...</code>) instead. This is the secret scanning feature working exactly as intended — and a good reminder that realistic-looking placeholder values in documentation are a real source of accidental secret exposure.</p> </blockquote>]]></content><author><name></name></author><category term="architecture"/><category term="api-key"/><category term="authentication"/><category term="spring-boot"/><category term="security"/><category term="automation"/><category term="java"/><summary type="html"><![CDATA[A deep dive into API key authentication in a Spring Boot multi-tenant API — covering when to use keys vs JWTs, hashing at rest, the session passthrough bug, and a complete test matrix.]]></summary></entry><entry><title type="html">Layer 2 — Tenant Resolution: How a Single API Instance Serves Multiple Customers Safely</title><link href="https://crystalhansen.ca/blog/2026/layer2-tenant-resolution/" rel="alternate" type="text/html" title="Layer 2 — Tenant Resolution: How a Single API Instance Serves Multiple Customers Safely"/><published>2026-05-12T14:10:00+00:00</published><updated>2026-05-12T14:10:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer2-tenant-resolution</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer2-tenant-resolution/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>Tenant isolation is the security concern most specific to multi-tenant systems — and the one most likely to be quietly broken by a well-intentioned feature change. Unlike authentication failures (which produce visible errors), tenant isolation failures produce invisible ones: a query silently returns another tenant’s data without raising an exception.</p> <p>This post covers how we implement and audit tenant resolution in a Spring Boot application serving multiple customers from a single instance.</p> <hr/> <h2 id="the-core-problem">The Core Problem</h2> <p>When a single database and a single API process serve multiple tenants, every database query must be scoped to the correct tenant. There are two ways to achieve this:</p> <ol> <li><strong>Separate databases per tenant</strong> — clean isolation, expensive and operationally complex</li> <li><strong>Shared database with a tenant discriminator column</strong> — efficient, manageable, but requires discipline</li> </ol> <p>We use option 2: a <code class="language-plaintext highlighter-rouge">domain</code> column on every tenant-scoped table, with automatic query filtering applied at the ORM layer.</p> <p>The risk is clear: if the filter is missing on one entity, or if tenant context is set incorrectly, a query returns rows from the wrong tenant. The application never throws an exception — it just silently serves the wrong data.</p> <hr/> <h2 id="how-tenant-resolution-works">How Tenant Resolution Works</h2> <h3 id="step-1--read-the-host-header-nginx--filter">Step 1 — Read the Host Header (nginx → Filter)</h3> <p>nginx preserves the original <code class="language-plaintext highlighter-rouge">Host</code> header when proxying to the backend (see <a href="/blog/2026/layer1-nginx-ssl-termination">Layer 1</a>). A servlet filter reads it on every request:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@Order</span><span class="o">(</span><span class="mi">1</span><span class="o">)</span>  <span class="c1">// runs before JWT filter</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">TenantResolverFilter</span> <span class="kd">extends</span> <span class="nc">OncePerRequestFilter</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doFilterInternal</span><span class="o">(</span><span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">,</span>
                                    <span class="nc">HttpServletResponse</span> <span class="n">response</span><span class="o">,</span>
                                    <span class="nc">FilterChain</span> <span class="n">chain</span><span class="o">)</span>
            <span class="kd">throws</span> <span class="nc">ServletException</span><span class="o">,</span> <span class="nc">IOException</span> <span class="o">{</span>

        <span class="nc">String</span> <span class="n">host</span> <span class="o">=</span> <span class="n">request</span><span class="o">.</span><span class="na">getHeader</span><span class="o">(</span><span class="s">"Host"</span><span class="o">);</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">host</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="n">host</span><span class="o">.</span><span class="na">isBlank</span><span class="o">())</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_BAD_REQUEST</span><span class="o">,</span> <span class="s">"Missing Host header"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="c1">// Strip port if present: api.tenant.com:8080 → api.tenant.com</span>
        <span class="nc">String</span> <span class="n">domain</span> <span class="o">=</span> <span class="n">host</span><span class="o">.</span><span class="na">contains</span><span class="o">(</span><span class="s">":"</span><span class="o">)</span> <span class="o">?</span> <span class="n">host</span><span class="o">.</span><span class="na">split</span><span class="o">(</span><span class="s">":"</span><span class="o">)[</span><span class="mi">0</span><span class="o">]</span> <span class="o">:</span> <span class="n">host</span><span class="o">;</span>

        <span class="c1">// Resolve to the frontend domain if this is an API subdomain</span>
        <span class="c1">// api.tenant.com → tenant.com</span>
        <span class="nc">String</span> <span class="n">tenantDomain</span> <span class="o">=</span> <span class="n">resolveTenantDomain</span><span class="o">(</span><span class="n">domain</span><span class="o">);</span>

        <span class="k">if</span> <span class="o">(!</span><span class="n">tenantRegistry</span><span class="o">.</span><span class="na">isKnownTenant</span><span class="o">(</span><span class="n">tenantDomain</span><span class="o">))</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_BAD_REQUEST</span><span class="o">,</span> <span class="s">"Unknown tenant"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="n">tenantDomain</span><span class="o">);</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
            <span class="nc">TenantContext</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>  <span class="c1">// CRITICAL — see below</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="step-2--thread-local-context">Step 2 — Thread-Local Context</h3> <p><code class="language-plaintext highlighter-rouge">TenantContext</code> holds the current tenant for the duration of the request:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">TenantContext</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ThreadLocal</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="no">CURRENT_TENANT</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ThreadLocal</span><span class="o">&lt;&gt;();</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">setCurrentTenant</span><span class="o">(</span><span class="nc">String</span> <span class="n">domain</span><span class="o">)</span> <span class="o">{</span>
        <span class="no">CURRENT_TENANT</span><span class="o">.</span><span class="na">set</span><span class="o">(</span><span class="n">domain</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="nc">String</span> <span class="nf">getCurrentTenant</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="no">CURRENT_TENANT</span><span class="o">.</span><span class="na">get</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">clear</span><span class="o">()</span> <span class="o">{</span>
        <span class="no">CURRENT_TENANT</span><span class="o">.</span><span class="na">remove</span><span class="o">();</span>  <span class="c1">// remove(), not set(null)</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Why <code class="language-plaintext highlighter-rouge">remove()</code> and not <code class="language-plaintext highlighter-rouge">set(null)</code>?</strong> Thread pools reuse threads. If a thread processes request A (tenant X) and then request B (tenant Y), the ThreadLocal value from request A persists into request B unless explicitly cleared. <code class="language-plaintext highlighter-rouge">remove()</code> removes the entry from the ThreadLocal map entirely. <code class="language-plaintext highlighter-rouge">set(null)</code> leaves the entry in the map — which can cause subtle issues if code checks <code class="language-plaintext highlighter-rouge">!= null</code> rather than calling <code class="language-plaintext highlighter-rouge">getCurrentTenant()</code>.</p> <p>The <code class="language-plaintext highlighter-rouge">finally</code> block in the filter ensures <code class="language-plaintext highlighter-rouge">clear()</code> is called even if the request throws an exception.</p> <h3 id="step-3--hibernate-filter">Step 3 — Hibernate Filter</h3> <p>The resolved domain is applied as an automatic query filter via Hibernate’s <code class="language-plaintext highlighter-rouge">@Filter</code> mechanism:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// On the base entity or each tenant-scoped entity</span>
<span class="nd">@Entity</span>
<span class="nd">@FilterDef</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"tenantFilter"</span><span class="o">,</span>
           <span class="n">parameters</span> <span class="o">=</span> <span class="nd">@ParamDef</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"tenantDomain"</span><span class="o">,</span> <span class="n">type</span> <span class="o">=</span> <span class="nc">String</span><span class="o">.</span><span class="na">class</span><span class="o">))</span>
<span class="nd">@Filter</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"tenantFilter"</span><span class="o">,</span> <span class="n">condition</span> <span class="o">=</span> <span class="s">"domain = :tenantDomain"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">Appointment</span> <span class="o">{</span>
    <span class="nd">@Column</span><span class="o">(</span><span class="n">name</span> <span class="o">=</span> <span class="s">"domain"</span><span class="o">,</span> <span class="n">nullable</span> <span class="o">=</span> <span class="kc">false</span><span class="o">)</span>
    <span class="kd">private</span> <span class="nc">String</span> <span class="n">domain</span><span class="o">;</span>
    <span class="c1">// ... other fields</span>
<span class="o">}</span>
</code></pre></div></div> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// In a Spring interceptor or repository base class — enable the filter per session</span>
<span class="nd">@Autowired</span>
<span class="kd">private</span> <span class="nc">EntityManager</span> <span class="n">entityManager</span><span class="o">;</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">enableTenantFilter</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">Session</span> <span class="n">session</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">unwrap</span><span class="o">(</span><span class="nc">Session</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
    <span class="n">session</span><span class="o">.</span><span class="na">enableFilter</span><span class="o">(</span><span class="s">"tenantFilter"</span><span class="o">)</span>
           <span class="o">.</span><span class="na">setParameter</span><span class="o">(</span><span class="s">"tenantDomain"</span><span class="o">,</span> <span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">());</span>
<span class="o">}</span>
</code></pre></div></div> <p>The result: every <code class="language-plaintext highlighter-rouge">SELECT</code> that Hibernate generates for <code class="language-plaintext highlighter-rouge">@Filter</code>-annotated entities automatically appends <code class="language-plaintext highlighter-rouge">WHERE domain = 'tenant.example.com'</code>. The application code never needs to remember to add a <code class="language-plaintext highlighter-rouge">.where(domain = ?)</code> clause — it’s structural.</p> <hr/> <h2 id="the-failure-modes">The Failure Modes</h2> <h3 id="failure-mode-1--missing-filter-on-a-new-entity">Failure Mode 1 — Missing Filter on a New Entity</h3> <p>Every time a new entity is added to the codebase, the <code class="language-plaintext highlighter-rouge">@Filter</code> and <code class="language-plaintext highlighter-rouge">domain</code> column must be added deliberately. It’s easy to forget, especially on entities that feel “global” (e.g., configuration tables that seem cross-tenant but aren’t).</p> <p><strong>Detection:</strong> A test that queries the entity from one tenant context and asserts that records from another tenant are not returned:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Test</span>
<span class="kt">void</span> <span class="nf">domainFilter_preventsDataLeakAcrossTenants</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// Create a record for tenant A</span>
    <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-a.example.com"</span><span class="o">);</span>
    <span class="n">enableTenantFilter</span><span class="o">();</span>
    <span class="n">repository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="k">new</span> <span class="nc">SomeEntity</span><span class="o">(</span><span class="s">"tenant-a.example.com"</span><span class="o">,</span> <span class="s">"value-A"</span><span class="o">));</span>

    <span class="c1">// Query as tenant B — should return nothing</span>
    <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">);</span>
    <span class="n">enableTenantFilter</span><span class="o">();</span>
    <span class="nc">List</span><span class="o">&lt;</span><span class="nc">SomeEntity</span><span class="o">&gt;</span> <span class="n">results</span> <span class="o">=</span> <span class="n">repository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>

    <span class="n">assertThat</span><span class="o">(</span><span class="n">results</span><span class="o">).</span><span class="na">isEmpty</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="failure-mode-2--context-not-cleared-between-requests">Failure Mode 2 — Context Not Cleared Between Requests</h3> <p>If the <code class="language-plaintext highlighter-rouge">TenantContext.clear()</code> is not in a <code class="language-plaintext highlighter-rouge">finally</code> block, an exception during request processing leaves stale context on the thread. The next request processed by that thread inherits the wrong tenant.</p> <p><strong>Symptom:</strong> Intermittent wrong-tenant data, only under error conditions, very hard to reproduce.</p> <p><strong>Detection:</strong> A test that simulates an exception mid-request and then makes a new request on the same thread, asserting that the context is clean.</p> <h3 id="failure-mode-3--missing-host-header-returns-a-default-tenant">Failure Mode 3 — Missing Host Header Returns a Default Tenant</h3> <p>An early version of this filter had a fallback: if <code class="language-plaintext highlighter-rouge">Host</code> was missing, it defaulted to the primary tenant domain. This meant a malformed request (or a request from a misconfigured client) would silently get data from the default tenant.</p> <p>The correct behavior is to return <code class="language-plaintext highlighter-rouge">400 Bad Request</code> and log the missing header. No fallback, no default.</p> <h3 id="failure-mode-4--api-subdomain-leaks-into-tenant-domain">Failure Mode 4 — API Subdomain Leaks Into Tenant Domain</h3> <p>If the filter resolves <code class="language-plaintext highlighter-rouge">api.tenant.com</code> but the database records have <code class="language-plaintext highlighter-rouge">domain = 'www.tenant.com'</code>, the filter returns zero rows for every query — which looks like empty data rather than a bug.</p> <p>Normalize domain resolution in <code class="language-plaintext highlighter-rouge">resolveTenantDomain()</code> to always map to the canonical frontend domain, and assert in a test that various input formats (with/without <code class="language-plaintext highlighter-rouge">api.</code>, with/without port) all resolve to the same canonical value.</p> <hr/> <h2 id="testing-tenant-isolation">Testing Tenant Isolation</h2> <h3 id="unit-tests">Unit Tests</h3> <p>Test the filter in isolation with a <code class="language-plaintext highlighter-rouge">MockHttpServletRequest</code>:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Test</span>
<span class="kt">void</span> <span class="nf">resolvesCorrectDomain_fromHostHeader</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
    <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
    <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">);</span>

    <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

    <span class="n">assertThat</span><span class="o">(</span><span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">);</span>
<span class="o">}</span>

<span class="nd">@Test</span>
<span class="kt">void</span> <span class="nf">returns400_whenHostHeaderMissing</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
    <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
    <span class="c1">// No Host header</span>

    <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

    <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">400</span><span class="o">);</span>
    <span class="n">assertThat</span><span class="o">(</span><span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">()).</span><span class="na">isNull</span><span class="o">();</span> <span class="c1">// not set</span>
<span class="o">}</span>

<span class="nd">@Test</span>
<span class="kt">void</span> <span class="nf">clearsTenantContext_afterRequestCompletes</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
    <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
    <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">);</span>

    <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

    <span class="c1">// Context should be cleared after filter completes</span>
    <span class="n">assertThat</span><span class="o">(</span><span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">()).</span><span class="na">isNull</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="integration-test--domain-isolation-at-the-repository-level">Integration Test — Domain Isolation at the Repository Level</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SpringBootTest</span>
<span class="nd">@Transactional</span>
<span class="kd">class</span> <span class="nc">UserRepositoryDomainIsolationTest</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">userRepository_doesNotReturnDataAcrossTenants</span><span class="o">()</span> <span class="o">{</span>
        <span class="c1">// Seed data for two tenants</span>
        <span class="n">seedUser</span><span class="o">(</span><span class="s">"alice@tenant-a.example.com"</span><span class="o">,</span> <span class="s">"tenant-a.example.com"</span><span class="o">);</span>
        <span class="n">seedUser</span><span class="o">(</span><span class="s">"bob@tenant-b.example.com"</span><span class="o">,</span> <span class="s">"tenant-b.example.com"</span><span class="o">);</span>

        <span class="c1">// Query as tenant A</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-a.example.com"</span><span class="o">);</span>
        <span class="n">enableTenantFilter</span><span class="o">();</span>
        <span class="nc">List</span><span class="o">&lt;</span><span class="nc">User</span><span class="o">&gt;</span> <span class="n">tenantAUsers</span> <span class="o">=</span> <span class="n">userRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">tenantAUsers</span><span class="o">).</span><span class="na">hasSize</span><span class="o">(</span><span class="mi">1</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">tenantAUsers</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="mi">0</span><span class="o">).</span><span class="na">getEmail</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="s">"alice@tenant-a.example.com"</span><span class="o">);</span>
        <span class="c1">// Bob's record must not appear</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">tenantAUsers</span><span class="o">).</span><span class="na">noneMatch</span><span class="o">(</span><span class="n">u</span> <span class="o">-&gt;</span> <span class="n">u</span><span class="o">.</span><span class="na">getEmail</span><span class="o">().</span><span class="na">equals</span><span class="o">(</span><span class="s">"bob@tenant-b.example.com"</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>This test is the most important test in the security suite. It should run on every pull request.</p> <hr/> <h2 id="the-tenant-registry">The Tenant Registry</h2> <p>Knowing <em>which</em> domains are valid tenants is itself a security concern. Without a registry check, an attacker could craft a request with <code class="language-plaintext highlighter-rouge">Host: attacker.com</code> and potentially create a new tenant context in your system.</p> <p>The registry should be populated at startup from a trusted source (database, config file, environment variable) and cached. Invalid domains are rejected at the filter layer before any further processing.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">TenantRegistry</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Set</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">knownDomains</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">TenantRegistry</span><span class="o">(</span><span class="nc">TenantRepository</span> <span class="n">repo</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// Load at startup, cache in memory</span>
        <span class="k">this</span><span class="o">.</span><span class="na">knownDomains</span> <span class="o">=</span> <span class="n">repo</span><span class="o">.</span><span class="na">findAllDomains</span><span class="o">()</span>
                                <span class="o">.</span><span class="na">stream</span><span class="o">()</span>
                                <span class="o">.</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toUnmodifiableSet</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="kd">public</span> <span class="kt">boolean</span> <span class="nf">isKnownTenant</span><span class="o">(</span><span class="nc">String</span> <span class="n">domain</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">knownDomains</span><span class="o">.</span><span class="na">contains</span><span class="o">(</span><span class="n">domain</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>For a system where tenants are provisioned dynamically, the registry needs to be refreshable without a restart — typically via a cache with a short TTL or an event-driven invalidation mechanism.</p> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Tenant Resolution
  [ ] Host header is required — no fallback to a default tenant
  [ ] Unknown Host headers return 400, not 200 with empty data
  [ ] Domain is normalized (strip api. prefix, strip port)
  [ ] TenantRegistry validates Host against known tenants at startup

Thread-Local Context
  [ ] TenantContext.clear() is always in a finally block
  [ ] Uses ThreadLocal.remove(), not set(null)
  [ ] No code path sets TenantContext without a corresponding clear

Hibernate Filter
  [ ] @Filter annotation present on every tenant-scoped entity
  [ ] Filter is enabled per-session before any repository calls
  [ ] New entities added to a checklist review: "does this need tenant scoping?"

Tests
  [ ] TenantResolverFilterTest covers: valid host, missing host, unknown host
  [ ] TenantContextTest covers: set, get, clear, clear-on-exception
  [ ] UserRepositoryDomainIsolationTest (or equivalent) runs on every PR
  [ ] Integration test seeds two tenants, queries as one, asserts the other is invisible
</code></pre></div></div> <hr/> <h2 id="what-comes-next">What Comes Next</h2> <p>Once the tenant is resolved and set in context, the request knows <em>which</em> customer it’s serving. The next question is <em>who</em> the user is — which is where JWT validation takes over.</p> <hr/> <p><em>Next in the series: <a href="/blog/2026/layer3-jwt-authentication">Layer 3 — JWT Authentication: Stateless Identity with Short-Lived Tokens</a></em></p>]]></content><author><name></name></author><category term="architecture"/><category term="multi-tenant"/><category term="spring-boot"/><category term="hibernate"/><category term="security"/><category term="saas"/><category term="data-isolation"/><summary type="html"><![CDATA[A deep dive into multi-tenant data isolation using Host-header-based tenant resolution, thread-local context, and Hibernate filters — including the failure modes that cause data leakage.]]></summary></entry><entry><title type="html">Layer 1 — nginx as Your Security Perimeter: SSL Termination and Access Logging</title><link href="https://crystalhansen.ca/blog/2026/layer1-nginx-ssl-termination/" rel="alternate" type="text/html" title="Layer 1 — nginx as Your Security Perimeter: SSL Termination and Access Logging"/><published>2026-05-12T14:00:00+00:00</published><updated>2026-05-12T14:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer1-nginx-ssl-termination</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer1-nginx-ssl-termination/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>nginx sits at the edge. Before a request reaches your application, framework, or business logic, it passes through nginx — which means nginx is your first and best opportunity to reject bad traffic cheaply.</p> <p>This post covers what nginx is actually doing in a production multi-tenant setup, why each configuration decision matters, and how to audit that it’s doing its job.</p> <hr/> <h2 id="what-nginx-is-responsible-for">What nginx Is Responsible For</h2> <p>In this stack, nginx has four jobs:</p> <ol> <li><strong>TLS termination</strong> — decrypt HTTPS, forward plain HTTP internally</li> <li><strong>Tenant routing</strong> — forward requests with a <code class="language-plaintext highlighter-rouge">Host</code> header the app can trust</li> <li><strong>Access logging</strong> — record every request at the perimeter</li> <li><strong>A first line of rate limiting</strong> — before requests hit application threads</li> </ol> <p>It is <em>not</em> responsible for authentication, authorization, or business logic. The moment you start adding auth logic to nginx config, you’re fighting the tool.</p> <hr/> <h2 id="tls-configuration">TLS Configuration</h2> <h3 id="why-terminate-at-nginx">Why terminate at nginx?</h3> <p>Your application doesn’t need to know about TLS. By terminating at nginx:</p> <ul> <li>Certificate management is centralized in one place</li> <li>The app connects to nginx over plain HTTP on a local port (never exposed externally)</li> <li>You can upgrade TLS configuration without touching application code</li> </ul> <h3 id="lets-encrypt--certbot">Let’s Encrypt + certbot</h3> <p>Let’s Encrypt issues free 90-day certificates. The renewal lifecycle is fully automatable:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Initial issuance</span>
certbot <span class="nt">--nginx</span> <span class="nt">-d</span> api.yourdomain.com

<span class="c"># Auto-renewal (runs twice daily via cron or systemd timer)</span>
certbot renew <span class="nt">--quiet</span>

<span class="c"># Dry run — always test this in CI</span>
certbot renew <span class="nt">--dry-run</span>
</code></pre></div></div> <p><strong>The production risk:</strong> certbot renewal fails silently more often than people expect — usually due to firewall rules blocking port 80 (which the HTTP-01 challenge needs) or DNS propagation delays after infrastructure changes. Set a monitoring alert that fires when the certificate is fewer than 14 days from expiry.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check expiry from the command line</span>
<span class="nb">echo</span> | openssl s_client <span class="nt">-connect</span> api.yourdomain.com:443 2&gt;/dev/null <span class="se">\</span>
  | openssl x509 <span class="nt">-noout</span> <span class="nt">-dates</span>
</code></pre></div></div> <h3 id="tls-protocol-and-cipher-configuration">TLS Protocol and Cipher Configuration</h3> <p>Modern nginx config should look roughly like this:</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">ssl_protocols</span> <span class="s">TLSv1.2</span> <span class="s">TLSv1.3</span><span class="p">;</span>
<span class="k">ssl_prefer_server_ciphers</span> <span class="no">on</span><span class="p">;</span>
<span class="k">ssl_ciphers</span> <span class="s">'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384'</span><span class="p">;</span>
<span class="k">ssl_session_cache</span> <span class="s">shared:SSL:10m</span><span class="p">;</span>
<span class="k">ssl_session_timeout</span> <span class="s">1d</span><span class="p">;</span>
<span class="k">ssl_stapling</span> <span class="no">on</span><span class="p">;</span>
<span class="k">ssl_stapling_verify</span> <span class="no">on</span><span class="p">;</span>
</code></pre></div></div> <p><strong>What each line does:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">ssl_protocols</code> — TLS 1.0 and 1.1 are deprecated (RFC 8996). Never enable them.</li> <li><code class="language-plaintext highlighter-rouge">ssl_prefer_server_ciphers on</code> — the server’s cipher preference wins, not the client’s. Prevents a client from negotiating down to a weaker cipher.</li> <li><code class="language-plaintext highlighter-rouge">ssl_ciphers</code> — ECDHE cipher suites provide forward secrecy: even if the private key is later compromised, past sessions cannot be decrypted.</li> <li><code class="language-plaintext highlighter-rouge">ssl_stapling</code> — OCSP stapling caches the certificate revocation status at nginx, preventing a round trip to Let’s Encrypt’s OCSP server on every handshake.</li> </ul> <p><strong>Audit with testssl.sh:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Run against your live endpoint</span>
docker run <span class="nt">--rm</span> drwetter/testssl.sh api.yourdomain.com

<span class="c"># What to look for in output:</span>
<span class="c"># ✓ TLS 1.3 offered</span>
<span class="c"># ✓ TLS 1.2 offered</span>
<span class="c"># ✗ TLS 1.1 not offered</span>
<span class="c"># ✗ TLS 1.0 not offered</span>
<span class="c"># ✓ Forward Secrecy offered</span>
<span class="c"># ✓ OCSP stapling</span>
<span class="c"># ✗ No BEAST, POODLE, CRIME, HEARTBLEED vulnerabilities</span>
</code></pre></div></div> <hr/> <h2 id="tenant-routing-via-the-host-header">Tenant Routing via the Host Header</h2> <p>In a multi-tenant setup where a single backend instance serves multiple domains, nginx is responsible for preserving the <code class="language-plaintext highlighter-rouge">Host</code> header so the application can resolve the tenant.</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># api.tenant-a.example.com</span>
<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">443</span> <span class="s">ssl</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">api.tenant-a.example.com</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://localhost:8080</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Host</span> <span class="nv">$host</span><span class="p">;</span>             <span class="c1"># preserve original Host</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Real-IP</span> <span class="nv">$remote_addr</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-For</span> <span class="nv">$proxy_add_x_forwarded_for</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-Proto</span> <span class="nv">$scheme</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="c1"># api.tenant-b.example.com — same backend, different Host</span>
<span class="k">server</span> <span class="p">{</span>
    <span class="kn">listen</span> <span class="mi">443</span> <span class="s">ssl</span><span class="p">;</span>
    <span class="kn">server_name</span> <span class="s">api.tenant-b.example.com</span><span class="p">;</span>

    <span class="kn">location</span> <span class="n">/</span> <span class="p">{</span>
        <span class="kn">proxy_pass</span> <span class="s">http://localhost:8080</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">Host</span> <span class="nv">$host</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Real-IP</span> <span class="nv">$remote_addr</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-For</span> <span class="nv">$proxy_add_x_forwarded_for</span><span class="p">;</span>
        <span class="kn">proxy_set_header</span> <span class="s">X-Forwarded-Proto</span> <span class="nv">$scheme</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Critical:</strong> <code class="language-plaintext highlighter-rouge">proxy_set_header Host $host</code> — without this, nginx sends its own <code class="language-plaintext highlighter-rouge">server_name</code> as the Host header, which might be a catch-all and break tenant resolution entirely. This is a configuration mistake that’s easy to miss and hard to debug.</p> <p><strong>X-Forwarded-For and real IPs:</strong> If your application logs IP addresses for audit purposes, it must read from <code class="language-plaintext highlighter-rouge">X-Forwarded-For</code>, not <code class="language-plaintext highlighter-rouge">RemoteAddr</code> (which will always be <code class="language-plaintext highlighter-rouge">127.0.0.1</code> when proxied). Configure your application to trust this header only from localhost.</p> <hr/> <h2 id="access-log-design">Access Log Design</h2> <p>The default nginx access log format loses information you’ll want during a security incident. Design your log format before you need it.</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">log_format</span> <span class="s">detailed</span> <span class="s">'</span><span class="nv">$remote_addr</span> <span class="s">-</span> <span class="nv">$remote_user</span> <span class="s">[</span><span class="nv">$time_local</span><span class="s">]</span> <span class="s">'</span>
                    <span class="s">'"</span><span class="nv">$request</span><span class="s">"</span> <span class="nv">$status</span> <span class="nv">$body_bytes_sent</span> <span class="s">'</span>
                    <span class="s">'"</span><span class="nv">$http_referer</span><span class="s">"</span> <span class="s">"</span><span class="nv">$http_user_agent</span><span class="s">"</span> <span class="s">'</span>
                    <span class="s">'host=</span><span class="nv">$host</span> <span class="s">'</span>
                    <span class="s">'rt=</span><span class="nv">$request_time</span> <span class="s">'</span>         <span class="c1"># total request time</span>
                    <span class="s">'uct=</span><span class="nv">$upstream_connect_time</span> <span class="s">'</span>
                    <span class="s">'urt=</span><span class="nv">$upstream_response_time</span> <span class="s">'</span>
                    <span class="s">'cs=</span><span class="nv">$upstream_cache_status</span><span class="s">'</span><span class="p">;</span>

<span class="k">access_log</span> <span class="n">/var/log/nginx/access.log</span> <span class="s">detailed</span><span class="p">;</span>
</code></pre></div></div> <p><strong>Why <code class="language-plaintext highlighter-rouge">$host</code> in the log?</strong> In a multi-tenant setup, the same nginx process handles multiple domains. Without <code class="language-plaintext highlighter-rouge">host=</code> in the log line, you can’t filter logs by tenant during an incident.</p> <p><strong>Why <code class="language-plaintext highlighter-rouge">$request_time</code> and <code class="language-plaintext highlighter-rouge">$upstream_response_time</code>?</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">$request_time</code> — wall clock from first byte received to last byte sent (includes network time)</li> <li><code class="language-plaintext highlighter-rouge">$upstream_response_time</code> — time the backend spent processing</li> </ul> <p>A spike in <code class="language-plaintext highlighter-rouge">$upstream_response_time</code> with normal <code class="language-plaintext highlighter-rouge">$request_time</code> points to a slow query. A spike in <code class="language-plaintext highlighter-rouge">$request_time</code> with a fast <code class="language-plaintext highlighter-rouge">$upstream_response_time</code> points to a slow network or large response body. These two numbers together tell you where a performance problem lives without touching the application.</p> <p><strong>Log rotation:</strong> nginx logs are append-only and grow without bound. Configure logrotate:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/var/log/nginx/*.log {
    daily
    rotate 14
    compress
    delaycompress
    missingok
    notifempty
    sharedscripts
    postrotate
        nginx -s reopen
    endscript
}
</code></pre></div></div> <hr/> <h2 id="rate-limiting-at-the-perimeter">Rate Limiting at the Perimeter</h2> <p>nginx can reject excessive requests before they consume application threads — the cheapest possible form of rate limiting.</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Define a rate limit zone in http block</span>
<span class="k">http</span> <span class="p">{</span>
    <span class="kn">limit_req_zone</span> <span class="nv">$binary_remote_addr</span> <span class="s">zone=api_limit:10m</span> <span class="s">rate=30r/m</span><span class="p">;</span>
    <span class="kn">limit_req_zone</span> <span class="nv">$binary_remote_addr</span> <span class="s">zone=auth_limit:10m</span> <span class="s">rate=5r/m</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">server</span> <span class="p">{</span>
    <span class="c1"># Tighter limit on auth endpoints</span>
    <span class="kn">location</span> <span class="n">/api/auth/</span> <span class="p">{</span>
        <span class="kn">limit_req</span> <span class="s">zone=auth_limit</span> <span class="s">burst=3</span> <span class="s">nodelay</span><span class="p">;</span>
        <span class="kn">limit_req_status</span> <span class="mi">429</span><span class="p">;</span>
        <span class="kn">proxy_pass</span> <span class="s">http://localhost:8080</span><span class="p">;</span>
    <span class="p">}</span>

    <span class="c1"># General API limit</span>
    <span class="kn">location</span> <span class="n">/api/</span> <span class="p">{</span>
        <span class="kn">limit_req</span> <span class="s">zone=api_limit</span> <span class="s">burst=10</span> <span class="s">nodelay</span><span class="p">;</span>
        <span class="kn">limit_req_status</span> <span class="mi">429</span><span class="p">;</span>
        <span class="kn">proxy_pass</span> <span class="s">http://localhost:8080</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">burst</code></strong> — allows short bursts above the rate limit before throttling. <code class="language-plaintext highlighter-rouge">burst=10</code> means up to 10 requests can queue before nginx starts returning 429.</p> <p><strong><code class="language-plaintext highlighter-rouge">nodelay</code></strong> — processes burst requests immediately rather than spacing them out. Without this, a burst of 10 requests takes 10x the rate interval to drain, which breaks legitimate clients.</p> <p><strong>Tuning auth endpoints separately</strong> matters. A brute-force attack on a login endpoint looks like a burst of POST requests from one IP. <code class="language-plaintext highlighter-rouge">5r/m</code> with <code class="language-plaintext highlighter-rouge">burst=3</code> means an attacker gets 8 attempts before being throttled — your application’s own lockout logic (e.g., after 5 failed attempts) should handle the rest.</p> <hr/> <h2 id="security-headers">Security Headers</h2> <p>Add these to every HTTPS response:</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">add_header</span> <span class="s">Strict-Transport-Security</span> <span class="s">"max-age=31536000</span><span class="p">;</span> <span class="k">includeSubDomains"</span> <span class="s">always</span><span class="p">;</span>
<span class="k">add_header</span> <span class="s">X-Content-Type-Options</span> <span class="s">"nosniff"</span> <span class="s">always</span><span class="p">;</span>
<span class="k">add_header</span> <span class="s">X-Frame-Options</span> <span class="s">"DENY"</span> <span class="s">always</span><span class="p">;</span>
<span class="k">add_header</span> <span class="s">Referrer-Policy</span> <span class="s">"strict-origin-when-cross-origin"</span> <span class="s">always</span><span class="p">;</span>
<span class="k">add_header</span> <span class="s">Permissions-Policy</span> <span class="s">"geolocation=(),</span> <span class="s">microphone=(),</span> <span class="s">camera=()"</span> <span class="s">always</span><span class="p">;</span>
</code></pre></div></div> <p><strong>HSTS (<code class="language-plaintext highlighter-rouge">Strict-Transport-Security</code>):</strong> Tells browsers never to connect over plain HTTP. <code class="language-plaintext highlighter-rouge">max-age=31536000</code> is one year. <code class="language-plaintext highlighter-rouge">includeSubDomains</code> ensures subdomains are also forced to HTTPS — be careful here if you have any HTTP-only subdomains.</p> <p><strong><code class="language-plaintext highlighter-rouge">X-Content-Type-Options: nosniff</code>:</strong> Prevents browsers from MIME-sniffing a response away from the declared <code class="language-plaintext highlighter-rouge">Content-Type</code>. Stops a class of attacks where malicious content is uploaded as an image and executed as JavaScript.</p> <p><strong><code class="language-plaintext highlighter-rouge">X-Frame-Options: DENY</code>:</strong> Prevents your app from being embedded in an iframe on another domain — closes clickjacking.</p> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SSL / TLS
  [ ] TLS 1.0 and 1.1 disabled
  [ ] TLS 1.2 and 1.3 enabled
  [ ] ECDHE cipher suites (forward secrecy)
  [ ] OCSP stapling enabled
  [ ] Certificate expiry monitored (alert &lt; 14 days)
  [ ] certbot renew --dry-run passes in CI

Proxy Headers
  [ ] proxy_set_header Host $host present in all tenant server blocks
  [ ] X-Real-IP and X-Forwarded-For set correctly
  [ ] Application configured to trust X-Forwarded-For only from localhost

Logging
  [ ] Custom log format includes $host, $request_time, $upstream_response_time
  [ ] logrotate configured, 14-day retention minimum
  [ ] Logs shipped to a centralized store (even S3) for incident forensics

Rate Limiting
  [ ] Auth endpoints have tighter limits than general API
  [ ] 429 response (not 503) for rate-limited requests
  [ ] Burst configured so legitimate clients aren't broken

Security Headers
  [ ] HSTS with includeSubDomains
  [ ] X-Content-Type-Options: nosniff
  [ ] X-Frame-Options: DENY

External Scan
  [ ] testssl.sh shows no critical findings
  [ ] Mozilla Observatory score A or above (https://observatory.mozilla.org)
</code></pre></div></div> <hr/> <h2 id="what-nginx-does-not-do">What nginx Does Not Do</h2> <p>To close: nginx is not the right place to handle application-level auth. If you find yourself writing Lua scripts in nginx to validate JWTs or check database state, that logic belongs in the application. nginx’s job is to be a fast, reliable proxy with good TLS — not a policy engine.</p> <p>The tenant resolution, JWT validation, and authorization logic in the layers that follow all happen inside the application, where you have proper testing infrastructure and language-native error handling.</p> <hr/> <p><em>Next in the series: <a href="/blog/2026/layer2-tenant-resolution">Layer 2 — Tenant Resolution: How a Single API Instance Serves Multiple Customers Safely</a></em></p>]]></content><author><name></name></author><category term="architecture"/><category term="nginx"/><category term="ssl"/><category term="tls"/><category term="security"/><category term="devops"/><category term="lets-encrypt"/><summary type="html"><![CDATA[A deep dive into using nginx as the outermost security layer in a multi-tenant SaaS — covering TLS configuration, Let's Encrypt automation, access log design, and rate limiting.]]></summary></entry><entry><title type="html">Setting Up Transactional &amp;amp; Marketing Email with AWS SES</title><link href="https://crystalhansen.ca/blog/2026/aws-ses-email-setup/" rel="alternate" type="text/html" title="Setting Up Transactional &amp;amp; Marketing Email with AWS SES"/><published>2026-05-12T13:00:00+00:00</published><updated>2026-05-12T13:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/aws-ses-email-setup</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/aws-ses-email-setup/"><![CDATA[<p>Whether you’re building a booking platform, SaaS product, or any web application that needs to send emails at scale, AWS Simple Email Service (SES) is one of the most cost-effective and reliable choices available. But getting it right — especially when email deliverability and marketing effectiveness are on the line — requires more than just plugging in SMTP credentials.</p> <p>This post is the <strong>first in a four-part series</strong>:</p> <blockquote> <p>📬 <strong>Series: Email Infrastructure with AWS SES</strong></p> <ol> <li><strong>You are here</strong> — Overview, SMTP setup, DNS, templates, and marketing</li> <li><a href="/blog/2026/ses-multi-tenant-email-service/">Multi-Tenant FROM Address Resolution &amp; Spring EmailService</a></li> <li><a href="/blog/2026/ses-bounce-complaint-handling/">SNS Bounce &amp; Complaint Webhook Handling</a></li> <li><a href="/blog/2026/ses-scheduled-nurture-sequences/">Scheduled Email Triggers &amp; Nurture Sequence Architecture</a></li> </ol> </blockquote> <hr/> <h2 id="why-aws-ses">Why AWS SES?</h2> <p>AWS SES gives you high-volume sending at a fraction of the cost of services like SendGrid or Mailgun, with deep integration into the AWS ecosystem. It supports both transactional email (booking confirmations, password resets, reports) and marketing campaigns through the same infrastructure.</p> <p>The tradeoff is that SES requires more setup upfront — particularly around DNS and identity verification — but done right, it delivers exceptional inbox placement rates.</p> <hr/> <h2 id="understanding-the-email-architecture">Understanding the Email Architecture</h2> <p>A well-designed email system separates concerns cleanly. At a high level, you need:</p> <ul> <li><strong>Triggers</strong> — the events in your application that initiate an email</li> <li><strong>An Email Service layer</strong> — a centralized service class that orchestrates sending</li> <li><strong>A Template Renderer</strong> — resolves the correct email content per tenant or context</li> <li><strong>SES as the delivery layer</strong> — the SMTP or API endpoint that sends the message</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[App Triggers] → [EmailService] → [TemplateRenderer] → [AWS SES] → [Client Inbox]
</code></pre></div></div> <p>This separation matters because it keeps your sending logic, template logic, and delivery mechanism independent. When you need to swap a template, update a subject line per tenant, or change your sending domain, none of those changes bleed into each other.</p> <h3 id="trigger-types-to-plan-for">Trigger Types to Plan For</h3> <p>Before writing a single line of code, map out your email triggers. They fall into two categories:</p> <p><strong>Event-driven triggers</strong> fire immediately in response to user action:</p> <ul> <li>User signup and email verification</li> <li>Appointment or booking confirmation</li> <li>Password reset request</li> <li>Payment receipt</li> </ul> <p><strong>Scheduled triggers</strong> run on a timer regardless of user action:</p> <ul> <li>Weekly or monthly summary reports</li> <li>No-show reminders</li> <li>Re-engagement nudges for inactive users</li> </ul> <blockquote> <p>For a deep dive into the scheduled trigger architecture and nurture sequence design, see <a href="/blog/2026/ses-scheduled-nurture-sequences/">Part 4: Scheduled Email Triggers &amp; Nurture Sequence Architecture</a>.</p> </blockquote> <hr/> <h2 id="smtp-configuration-the-basics">SMTP Configuration: The Basics</h2> <p>AWS SES supports both an HTTP API and SMTP. For most Java/Spring applications already using <code class="language-plaintext highlighter-rouge">JavaMailSender</code> or a similar abstraction, SMTP is the simpler integration path.</p> <p><strong>The correct SMTP settings for SES:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host:       email-smtp.us-east-1.amazonaws.com
Port:       587
Encryption: STARTTLS (not SSL)
</code></pre></div></div> <p>Use <strong>port 587 with STARTTLS</strong> — not 465. Port 465 is a legacy SSL port that AWS does support, but 587/STARTTLS is the modern standard and more broadly compatible across mail clients and firewalls.</p> <h3 id="a-critical-note-on-regions">A Critical Note on Regions</h3> <p>SES is not available in every AWS region. The primary supported regions are <code class="language-plaintext highlighter-rouge">us-east-1</code> (N. Virginia), <code class="language-plaintext highlighter-rouge">us-west-2</code> (Oregon), and <code class="language-plaintext highlighter-rouge">eu-west-1</code> (Ireland). If your EC2 is in a region like <code class="language-plaintext highlighter-rouge">us-east-2</code> (Ohio), <strong>you can still use SES</strong> — just point your SMTP host explicitly at a supported region’s endpoint. Your compute and your email service do not need to share a region.</p> <h3 id="smtp-credentials-are-not-iam-credentials">SMTP Credentials Are Not IAM Credentials</h3> <p>Your SES SMTP username and password are <strong>not</strong> your AWS Access Key ID and Secret. You generate them specifically inside the SES console:</p> <ol> <li>Navigate to <strong>SES → SMTP Settings → Create SMTP Credentials</strong></li> <li>AWS creates a dedicated IAM user with <code class="language-plaintext highlighter-rouge">AmazonSesSendingAccess</code></li> <li>Store the SMTP username and password securely in environment variables</li> </ol> <pre><code class="language-dotenv">EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
EMAIL_PORT=587
EMAIL_USERNAME=YOUR_SES_SMTP_USERNAME
EMAIL_PASSWORD=YOUR_SES_SMTP_PASSWORD
</code></pre> <hr/> <h2 id="dns-configuration-the-foundation-of-deliverability">DNS Configuration: The Foundation of Deliverability</h2> <p>This is where most developers underinvest, and it’s the single biggest factor in whether your emails land in the inbox or spam. Three DNS records matter: <strong>SPF</strong>, <strong>DKIM</strong>, and <strong>DMARC</strong>.</p> <h3 id="spf--sender-policy-framework">SPF — Sender Policy Framework</h3> <p>SPF tells receiving mail servers which IP addresses are authorized to send email on behalf of your domain. Without it, spam filters will treat your legitimate emails with suspicion.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Type:  TXT
Name:  @
Value: v=spf1 include:amazonses.com ~all
</code></pre></div></div> <p><strong>Common mistake:</strong> If you use multiple services (SES for transactional, Mailchimp for newsletters), you need to include all of them in one SPF record. SPF only allows one TXT record per domain — multiple records will break it.</p> <h3 id="dkim--domainkeys-identified-mail">DKIM — DomainKeys Identified Mail</h3> <p>DKIM adds a cryptographic signature to every email your server sends. For SES, AWS generates the key pair for you:</p> <ol> <li>Go to <strong>SES → Verified Identities → your domain</strong></li> <li>AWS provides three CNAME records to add to your DNS</li> <li>Once propagated, SES automatically signs every outgoing email</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Type:  CNAME
Name:  [selector1]._domainkey.yourdomain.com
Value: [selector1].dkim.amazonses.com
</code></pre></div></div> <p>Repeat for all three records AWS provides. <strong>DKIM is non-negotiable for marketing volume</strong> — without it, bulk sends are far more likely to be filtered.</p> <h3 id="dmarc--domain-based-message-authentication">DMARC — Domain-based Message Authentication</h3> <p>DMARC builds on SPF and DKIM, telling receiving servers what to do when a message fails authentication checks.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Type:  TXT
Name:  _dmarc.yourdomain.com
Value: v=DMARC1; p=none; rua=mailto:dmarc-reports@yourdomain.com
</code></pre></div></div> <p>Start with <code class="language-plaintext highlighter-rouge">p=none</code> (monitor only), then graduate to <code class="language-plaintext highlighter-rouge">p=quarantine</code> or <code class="language-plaintext highlighter-rouge">p=reject</code> once you’re confident in your sending patterns.</p> <h3 id="dns-propagation-checklist">DNS Propagation Checklist</h3> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SPF <code class="language-plaintext highlighter-rouge">TXT</code> record at domain root includes <code class="language-plaintext highlighter-rouge">amazonses.com</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>All three DKIM <code class="language-plaintext highlighter-rouge">CNAME</code> records added and verified in SES console</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>DMARC <code class="language-plaintext highlighter-rouge">TXT</code> record at <code class="language-plaintext highlighter-rouge">_dmarc.yourdomain.com</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Domain shows as <strong>Verified</strong> in SES Verified Identities</li> </ul> <p>Tools like <a href="https://mxtoolbox.com">MXToolbox</a> and <a href="https://www.mail-tester.com">mail-tester.com</a> let you validate all three without sending a real email.</p> <hr/> <h2 id="building-a-template-engine">Building a Template Engine</h2> <p>If your application supports multiple brands or tenants, you need a template resolution strategy that’s flexible and maintainable. A practical two-tier approach:</p> <ol> <li><strong>Database overrides</strong> — per-tenant subject lines and HTML bodies stored in a <code class="language-plaintext highlighter-rouge">email_template_settings</code> table</li> <li><strong>File-based fallbacks</strong> — well-designed default HTML templates on disk</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TemplateRenderer.resolve(tenantId, templateType)
  → Check tenant_email_template_settings table
  → If found: use subject + HTML from DB
  → If not: load from /email_templates/{templateType}.html
  → Replace [placeholder_vars] with runtime values
  → Return HTML string to EmailService
</code></pre></div></div> <blockquote> <p>For the full Spring <code class="language-plaintext highlighter-rouge">@Service</code> implementation including tenant FROM address resolution, see <a href="/blog/2026/ses-multi-tenant-email-service/">Part 2: Multi-Tenant FROM Address Resolution</a>.</p> </blockquote> <hr/> <h2 id="ses-as-a-marketing-nurture-engine">SES as a Marketing Nurture Engine</h2> <p>Where SES becomes genuinely powerful for growth is as the backbone of a <strong>marketing nurture engine</strong> — automated sequences that guide leads from first contact to conversion.</p> <p><strong>Key considerations for marketing volume:</strong></p> <ul> <li><strong>Sandbox mode:</strong> New SES accounts can only send to verified addresses. Request production access before any real marketing sends.</li> <li><strong>Bounce and complaint handling:</strong> You must suppress bounced addresses and honor unsubscribes — or AWS will throttle and eventually suspend your sending.</li> <li><strong>Dedicated IPs:</strong> For high-volume senders, dedicated IPs let you build and control your own sending reputation independently of other SES users.</li> <li><strong>Warmup strategy:</strong> Never blast your full list from a fresh domain. Start at 50–200 emails/day to your most engaged users and scale up over 3–4 weeks.</li> </ul> <blockquote> <p>For SNS bounce/complaint webhook setup and suppression list management, see <a href="/blog/2026/ses-bounce-complaint-handling/">Part 3: SNS Bounce &amp; Complaint Webhook Handling</a>.</p> </blockquote> <hr/> <h2 id="production-readiness-checklist">Production Readiness Checklist</h2> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SES account out of sandbox (production access approved)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SPF, DKIM, and DMARC DNS records verified</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SMTP credentials stored in environment variables, not in code</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SNS topic configured to receive SES bounce and complaint notifications</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Bounce and unsubscribe handling implemented</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Outbound port 587 open in EC2 security group egress rules</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>SMTP connectivity tested from inside the production container</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>At least one sending identity (domain or email) verified in SES console</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Email templates tested across Gmail, Outlook, and Apple Mail</li> </ul> <hr/> <h2 id="whats-next">What’s Next</h2> <p>This overview covers the foundations. The three follow-up posts go deep on the implementation details:</p> <ul> <li><a href="/blog/2026/ses-multi-tenant-email-service/">Part 2: Multi-Tenant FROM Address Resolution &amp; Spring EmailService</a> — how to resolve the correct sender address per tenant and wire up the Spring service layer</li> <li><a href="/blog/2026/ses-bounce-complaint-handling/">Part 3: SNS Bounce &amp; Complaint Webhook Handling</a> — protecting your sending reputation with automated suppression</li> <li><a href="/blog/2026/ses-scheduled-nurture-sequences/">Part 4: Scheduled Email Triggers &amp; Nurture Sequence Architecture</a> — building the scheduler and conditional logic that powers marketing nurture</li> </ul>]]></content><author><name>your_name</name></author><category term="engineering"/><category term="infrastructure"/><category term="aws"/><category term="ses"/><category term="email"/><category term="devops"/><category term="smtp"/><category term="spring-boot"/><summary type="html"><![CDATA[A developer's guide to configuring AWS SES for transactional email, DNS authentication, multi-tenant templates, and marketing nurture sequences.]]></summary></entry><entry><title type="html">Layer 3 — JWT Authentication: Stateless Identity with Short-Lived Tokens</title><link href="https://crystalhansen.ca/blog/2026/layer3-jwt-authentication/" rel="alternate" type="text/html" title="Layer 3 — JWT Authentication: Stateless Identity with Short-Lived Tokens"/><published>2026-05-12T00:00:00+00:00</published><updated>2026-05-12T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer3-jwt-authentication</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer3-jwt-authentication/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>By the time a request reaches the JWT filter, nginx has already terminated TLS (<a href="/blog/2026/layer1-nginx-ssl-termination">Layer 1</a>) and the tenant is resolved (<a href="/blog/2026/layer2-tenant-resolution">Layer 2</a>). The system knows <em>which</em> customer this request is for. The JWT filter answers the next question: <strong>who is the user, and are they who they say they are?</strong></p> <hr/> <h2 id="why-jwts-for-a-saas-api">Why JWTs for a SaaS API?</h2> <p>JWTs are a good fit for this architecture because:</p> <ul> <li><strong>Stateless</strong> — the server doesn’t need to query a session store for every request. The token carries its own claims.</li> <li><strong>Self-contained</strong> — roles, tenant domain, and user identity travel together in one verifiable payload.</li> <li><strong>Works across services</strong> — a token issued by the main API can be verified by other services (ML scorer, Lambda functions) without a shared session store.</li> </ul> <p>The tradeoff: a JWT cannot be revoked before expiry without additional infrastructure (a deny-list or short-lived tokens + rotation). We manage this with a 1-hour expiry and session cookies for browser clients.</p> <hr/> <h2 id="algorithm-choice">Algorithm Choice</h2> <p>JWTs support multiple signing algorithms. The choice matters significantly:</p> <table> <thead> <tr> <th>Algorithm</th> <th>Type</th> <th>Key Material</th> <th>Use Case</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">HS256</code></td> <td>Symmetric (HMAC)</td> <td>Single shared secret</td> <td>Internal API, same process issues and verifies</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">RS256</code></td> <td>Asymmetric (RSA)</td> <td>Private key signs, public key verifies</td> <td>External IdP, third-party verification</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">none</code></td> <td>None</td> <td>—</td> <td><strong>Never use. A well-known attack vector.</strong></td> </tr> </tbody> </table> <p>We use <strong>HS256</strong> for tokens issued by our own API and <strong>RS256</strong> for tokens issued by external identity providers (e.g., Google OAuth2). The algorithm must be explicitly whitelisted — never inferred from the token header.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// WRONG — trusts the algorithm declared in the token</span>
<span class="nc">Jwts</span><span class="o">.</span><span class="na">parser</span><span class="o">().</span><span class="na">parseClaimsJws</span><span class="o">(</span><span class="n">token</span><span class="o">);</span>

<span class="c1">// CORRECT — explicitly require HS256</span>
<span class="nc">Jwts</span><span class="o">.</span><span class="na">parserBuilder</span><span class="o">()</span>
    <span class="o">.</span><span class="na">setSigningKey</span><span class="o">(</span><span class="n">secretKey</span><span class="o">)</span>
    <span class="o">.</span><span class="na">requireAlgorithm</span><span class="o">(</span><span class="s">"HS256"</span><span class="o">)</span>
    <span class="o">.</span><span class="na">build</span><span class="o">()</span>
    <span class="o">.</span><span class="na">parseClaimsJws</span><span class="o">(</span><span class="n">token</span><span class="o">);</span>
</code></pre></div></div> <p><strong>The <code class="language-plaintext highlighter-rouge">alg: none</code> attack:</strong> An attacker can craft a JWT with <code class="language-plaintext highlighter-rouge">"alg": "none"</code> in the header and an empty signature. A parser that trusts the token’s declared algorithm will skip signature verification entirely and accept the forged token. Always specify the algorithm on the parser, never on the token.</p> <hr/> <h2 id="token-structure-and-claims">Token Structure and Claims</h2> <p>A JWT has three parts: header, payload (claims), and signature. The payload is what matters for application logic:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"sub"</span><span class="p">:</span><span class="w"> </span><span class="s2">"user-uuid-here"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"alice@example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"domain"</span><span class="p">:</span><span class="w"> </span><span class="s2">"tenant.example.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"roles"</span><span class="p">:</span><span class="w"> </span><span class="p">[</span><span class="s2">"ROLE_ADMIN"</span><span class="p">],</span><span class="w">
  </span><span class="nl">"iat"</span><span class="p">:</span><span class="w"> </span><span class="mi">1746000000</span><span class="p">,</span><span class="w">
  </span><span class="nl">"exp"</span><span class="p">:</span><span class="w"> </span><span class="mi">1746003600</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Claim design decisions:</strong></p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">sub</code></strong> — the stable user identifier (UUID, not email, so it survives email changes)</li> <li><strong><code class="language-plaintext highlighter-rouge">domain</code></strong> — the tenant domain, baked into the token. This is a second layer of tenant isolation: even if the JWT filter ran before the tenant filter, the token itself carries the tenant context. We cross-check this against <code class="language-plaintext highlighter-rouge">TenantContext</code> as a defense-in-depth measure.</li> <li><strong><code class="language-plaintext highlighter-rouge">roles</code></strong> — kept as a list so multi-role users are handled naturally</li> <li><strong><code class="language-plaintext highlighter-rouge">iat</code> / <code class="language-plaintext highlighter-rouge">exp</code></strong> — issued-at and expiry, both validated on parse. <code class="language-plaintext highlighter-rouge">exp - iat = 3600</code> (1 hour)</li> <li><strong>No sensitive data in claims</strong> — JWT payloads are base64-encoded, not encrypted. Anyone with the token can read the claims. Never put passwords, PII beyond an identifier, or internal system details in a JWT.</li> </ul> <hr/> <h2 id="the-filter-implementation">The Filter Implementation</h2> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">JwtAuthenticationFilter</span> <span class="kd">extends</span> <span class="nc">OncePerRequestFilter</span> <span class="o">{</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doFilterInternal</span><span class="o">(</span><span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">,</span>
                                    <span class="nc">HttpServletResponse</span> <span class="n">response</span><span class="o">,</span>
                                    <span class="nc">FilterChain</span> <span class="n">chain</span><span class="o">)</span>
            <span class="kd">throws</span> <span class="nc">ServletException</span><span class="o">,</span> <span class="nc">IOException</span> <span class="o">{</span>

        <span class="nc">String</span> <span class="n">authHeader</span> <span class="o">=</span> <span class="n">request</span><span class="o">.</span><span class="na">getHeader</span><span class="o">(</span><span class="nc">HttpHeaders</span><span class="o">.</span><span class="na">AUTHORIZATION</span><span class="o">);</span>

        <span class="c1">// No Bearer token — pass through, let Spring Security decide</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">authHeader</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="o">!</span><span class="n">authHeader</span><span class="o">.</span><span class="na">startsWith</span><span class="o">(</span><span class="s">"Bearer "</span><span class="o">))</span> <span class="o">{</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="nc">String</span> <span class="n">token</span> <span class="o">=</span> <span class="n">authHeader</span><span class="o">.</span><span class="na">substring</span><span class="o">(</span><span class="mi">7</span><span class="o">);</span>

        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">Claims</span> <span class="n">claims</span> <span class="o">=</span> <span class="n">jwtUtil</span><span class="o">.</span><span class="na">validateAndParseClaims</span><span class="o">(</span><span class="n">token</span><span class="o">);</span>

            <span class="c1">// Cross-check token domain with resolved tenant context</span>
            <span class="nc">String</span> <span class="n">tokenDomain</span> <span class="o">=</span> <span class="n">claims</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"domain"</span><span class="o">,</span> <span class="nc">String</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
            <span class="nc">String</span> <span class="n">tenantDomain</span> <span class="o">=</span> <span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">();</span>
            <span class="k">if</span> <span class="o">(!</span><span class="n">tokenDomain</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">tenantDomain</span><span class="o">))</span> <span class="o">{</span>
                <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">,</span> <span class="s">"Token domain mismatch"</span><span class="o">);</span>
                <span class="k">return</span><span class="o">;</span>
            <span class="o">}</span>

            <span class="c1">// Build Spring Security authentication from claims</span>
            <span class="nc">UsernamePasswordAuthenticationToken</span> <span class="n">auth</span> <span class="o">=</span>
                <span class="k">new</span> <span class="nf">UsernamePasswordAuthenticationToken</span><span class="o">(</span>
                    <span class="n">claims</span><span class="o">.</span><span class="na">getSubject</span><span class="o">(),</span>
                    <span class="kc">null</span><span class="o">,</span>
                    <span class="nc">AuthorityUtils</span><span class="o">.</span><span class="na">createAuthorityList</span><span class="o">(</span>
                        <span class="n">claims</span><span class="o">.</span><span class="na">get</span><span class="o">(</span><span class="s">"roles"</span><span class="o">,</span> <span class="nc">List</span><span class="o">.</span><span class="na">class</span><span class="o">).</span><span class="na">toArray</span><span class="o">(</span><span class="k">new</span> <span class="nc">String</span><span class="o">[</span><span class="mi">0</span><span class="o">])</span>
                    <span class="o">)</span>
                <span class="o">);</span>

            <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">setAuthentication</span><span class="o">(</span><span class="n">auth</span><span class="o">);</span>

        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">ExpiredJwtException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">,</span> <span class="s">"Token expired"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">JwtException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">,</span> <span class="s">"Invalid token"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Key design choices in this filter:</strong></p> <ol> <li> <p><strong>Missing token is not an error</strong> — the filter passes through if no <code class="language-plaintext highlighter-rouge">Authorization</code> header is present. Whether the endpoint requires auth is decided by Spring Security’s <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> annotations downstream (Layer 5). This separation means the JWT filter doesn’t need to know which endpoints are public.</p> </li> <li> <p><strong>Token domain cross-check</strong> — compares the <code class="language-plaintext highlighter-rouge">domain</code> claim against the tenant context set in Layer 2. A valid token from one tenant cannot be used against another tenant’s API subdomain.</p> </li> <li> <p><strong>Specific error handling</strong> — <code class="language-plaintext highlighter-rouge">ExpiredJwtException</code> and <code class="language-plaintext highlighter-rouge">JwtException</code> produce <code class="language-plaintext highlighter-rouge">401</code>, not <code class="language-plaintext highlighter-rouge">500</code>. This matters for client error handling and also prevents exception stack traces from leaking internal details in the response body.</p> </li> </ol> <hr/> <h2 id="token-issuance">Token Issuance</h2> <p>Tokens are issued at login:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">String</span> <span class="nf">generateToken</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">,</span> <span class="nc">String</span> <span class="n">tenantDomain</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Date</span> <span class="n">now</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">();</span>
    <span class="nc">Date</span> <span class="n">expiry</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Date</span><span class="o">(</span><span class="n">now</span><span class="o">.</span><span class="na">getTime</span><span class="o">()</span> <span class="o">+</span> <span class="nc">Duration</span><span class="o">.</span><span class="na">ofHours</span><span class="o">(</span><span class="mi">1</span><span class="o">).</span><span class="na">toMillis</span><span class="o">());</span>

    <span class="k">return</span> <span class="nc">Jwts</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
        <span class="o">.</span><span class="na">setSubject</span><span class="o">(</span><span class="n">user</span><span class="o">.</span><span class="na">getId</span><span class="o">().</span><span class="na">toString</span><span class="o">())</span>
        <span class="o">.</span><span class="na">claim</span><span class="o">(</span><span class="s">"email"</span><span class="o">,</span> <span class="n">user</span><span class="o">.</span><span class="na">getEmail</span><span class="o">())</span>
        <span class="o">.</span><span class="na">claim</span><span class="o">(</span><span class="s">"domain"</span><span class="o">,</span> <span class="n">tenantDomain</span><span class="o">)</span>
        <span class="o">.</span><span class="na">claim</span><span class="o">(</span><span class="s">"roles"</span><span class="o">,</span> <span class="n">user</span><span class="o">.</span><span class="na">getRoles</span><span class="o">())</span>
        <span class="o">.</span><span class="na">setIssuedAt</span><span class="o">(</span><span class="n">now</span><span class="o">)</span>
        <span class="o">.</span><span class="na">setExpiration</span><span class="o">(</span><span class="n">expiry</span><span class="o">)</span>
        <span class="o">.</span><span class="na">signWith</span><span class="o">(</span><span class="n">secretKey</span><span class="o">,</span> <span class="nc">SignatureAlgorithm</span><span class="o">.</span><span class="na">HS256</span><span class="o">)</span>
        <span class="o">.</span><span class="na">compact</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Secret key management:</strong> The signing secret must never be in source code. Load it from an environment variable at startup:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Value</span><span class="o">(</span><span class="s">"${jwt.secret}"</span><span class="o">)</span>
<span class="kd">private</span> <span class="nc">String</span> <span class="n">jwtSecret</span><span class="o">;</span>

<span class="nd">@Bean</span>
<span class="kd">public</span> <span class="nc">SecretKey</span> <span class="nf">jwtSecretKey</span><span class="o">()</span> <span class="o">{</span>
    <span class="kt">byte</span><span class="o">[]</span> <span class="n">keyBytes</span> <span class="o">=</span> <span class="nc">Decoders</span><span class="o">.</span><span class="na">BASE64</span><span class="o">.</span><span class="na">decode</span><span class="o">(</span><span class="n">jwtSecret</span><span class="o">);</span>
    <span class="k">return</span> <span class="nc">Keys</span><span class="o">.</span><span class="na">hmacShaKeyFor</span><span class="o">(</span><span class="n">keyBytes</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p>The secret should be at least 256 bits (32 bytes) of random data, base64-encoded. Generate with:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl rand <span class="nt">-base64</span> 32
</code></pre></div></div> <p>Store in your environment or secrets manager, never in <code class="language-plaintext highlighter-rouge">application.properties</code> or committed to git.</p> <hr/> <h2 id="browser-clients--the-session-cookie-layer">Browser Clients — The Session Cookie Layer</h2> <p>Browser clients face a UX problem with 1-hour JWT expiry: users get logged out mid-session. We solve this with a parallel session cookie:</p> <ul> <li>On login, the server sets a <code class="language-plaintext highlighter-rouge">HttpOnly; Secure; SameSite=Lax</code> cookie with a sliding expiry (e.g., 7 days)</li> <li>On subsequent requests, the browser sends the cookie automatically</li> <li>If the JWT has expired, the server can issue a fresh JWT using the session cookie</li> <li>The API key filter (Layer 4) respects an existing valid session and does not interfere with it</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Browser login flow:
  POST /api/auth/login
  ← 200 OK
  ← Set-Cookie: session=&lt;id&gt;; HttpOnly; Secure; SameSite=Lax; Max-Age=604800
  ← Body: { token: "&lt;jwt&gt;" }

Subsequent requests (JWT still valid):
  GET /api/...
  Authorization: Bearer &lt;jwt&gt;
  Cookie: session=&lt;id&gt;
  → JWT filter authenticates from Bearer token

After JWT expiry (session still valid):
  GET /api/...
  Cookie: session=&lt;id&gt;  (no Authorization header)
  → Session filter issues fresh JWT transparently
</code></pre></div></div> <p><strong>SameSite=Lax</strong> protects against CSRF by default: the cookie is only sent on same-site requests and top-level cross-site GET navigations. State-changing requests (POST, PATCH, DELETE) from cross-origin pages don’t include the cookie.</p> <hr/> <h2 id="google-oauth2--external-identity-provider">Google OAuth2 — External Identity Provider</h2> <p>For “Sign in with Google”, the flow produces a Google-issued ID token (RS256), which we validate against Google’s public keys:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Validate Google ID token</span>
<span class="nc">GoogleIdToken</span> <span class="n">idToken</span> <span class="o">=</span> <span class="n">verifier</span><span class="o">.</span><span class="na">verify</span><span class="o">(</span><span class="n">idTokenString</span><span class="o">);</span>
<span class="k">if</span> <span class="o">(</span><span class="n">idToken</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">throw</span> <span class="k">new</span> <span class="nf">UnauthorizedException</span><span class="o">(</span><span class="s">"Invalid Google token"</span><span class="o">);</span>
<span class="o">}</span>

<span class="nc">GoogleIdToken</span><span class="o">.</span><span class="na">Payload</span> <span class="n">payload</span> <span class="o">=</span> <span class="n">idToken</span><span class="o">.</span><span class="na">getPayload</span><span class="o">();</span>
<span class="nc">String</span> <span class="n">email</span> <span class="o">=</span> <span class="n">payload</span><span class="o">.</span><span class="na">getEmail</span><span class="o">();</span>
<span class="kt">boolean</span> <span class="n">emailVerified</span> <span class="o">=</span> <span class="n">payload</span><span class="o">.</span><span class="na">getEmailVerified</span><span class="o">();</span>

<span class="c1">// Map to internal user and issue our own JWT</span>
<span class="nc">User</span> <span class="n">user</span> <span class="o">=</span> <span class="n">userService</span><span class="o">.</span><span class="na">findOrCreateFromGoogle</span><span class="o">(</span><span class="n">email</span><span class="o">,</span> <span class="n">tenantDomain</span><span class="o">);</span>
<span class="nc">String</span> <span class="n">internalJwt</span> <span class="o">=</span> <span class="n">jwtUtil</span><span class="o">.</span><span class="na">generateToken</span><span class="o">(</span><span class="n">user</span><span class="o">,</span> <span class="n">tenantDomain</span><span class="o">);</span>
</code></pre></div></div> <p><strong>Never trust a Google token without verifying it.</strong> The <code class="language-plaintext highlighter-rouge">verifier.verify()</code> call:</p> <ol> <li>Checks the signature against Google’s current public keys (fetched and cached from <code class="language-plaintext highlighter-rouge">https://www.googleapis.com/oauth2/v3/certs</code>)</li> <li>Validates the <code class="language-plaintext highlighter-rouge">aud</code> claim matches your registered client ID</li> <li>Validates the <code class="language-plaintext highlighter-rouge">exp</code> claim</li> </ol> <p>After verification, we issue our own internal JWT — downstream systems only need to know about one token format.</p> <hr/> <h2 id="testing">Testing</h2> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">JwtAuthenticationFilterTest</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">validToken_authenticatesAndSetsSecurityContext</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">token</span> <span class="o">=</span> <span class="n">jwtUtil</span><span class="o">.</span><span class="na">generateToken</span><span class="o">(</span><span class="n">testUser</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">token</span><span class="o">);</span>
        <span class="c1">// Assume TenantContext already set by Layer 2 filter</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="nc">Authentication</span> <span class="n">auth</span> <span class="o">=</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">();</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">auth</span><span class="o">).</span><span class="na">isNotNull</span><span class="o">();</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">auth</span><span class="o">.</span><span class="na">isAuthenticated</span><span class="o">()).</span><span class="na">isTrue</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">expiredToken_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">expiredToken</span> <span class="o">=</span> <span class="n">generateExpiredToken</span><span class="o">(</span><span class="n">testUser</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">expiredToken</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">tokenDomainMismatch_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// Token issued for tenant A, but request is for tenant B</span>
        <span class="nc">String</span> <span class="n">token</span> <span class="o">=</span> <span class="n">jwtUtil</span><span class="o">.</span><span class="na">generateToken</span><span class="o">(</span><span class="n">testUser</span><span class="o">,</span> <span class="s">"tenant-a.example.com"</span><span class="o">);</span>

        <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">token</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">);</span>  <span class="c1">// different tenant</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">noAuthHeader_passesThroughToNextFilter</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
        <span class="c1">// No Authorization header</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">verify</span><span class="o">(</span><span class="n">chain</span><span class="o">).</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()).</span><span class="na">isNull</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">algNoneToken_isRejected</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">forgedToken</span> <span class="o">=</span> <span class="n">buildAlgNoneToken</span><span class="o">(</span><span class="n">testUser</span><span class="o">);</span>

        <span class="nc">MockHttpServletRequest</span> <span class="n">request</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">MockHttpServletRequest</span><span class="o">();</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">forgedToken</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Algorithm
  [ ] Signing algorithm is explicitly whitelisted in the parser
  [ ] alg:none token is rejected (test exists)
  [ ] HS256 secret is &gt;= 256 bits of entropy
  [ ] RS256 public keys are cached with TTL for external IdP

Claims
  [ ] exp claim is validated on every parse
  [ ] domain claim is cross-checked against TenantContext
  [ ] No sensitive data (passwords, PII) in token payload
  [ ] sub is a stable UUID, not email

Secret Management
  [ ] JWT secret loaded from environment variable, never from source code
  [ ] Secret is not logged (check logging config)
  [ ] Secret rotation procedure is documented

Session Cookie
  [ ] HttpOnly flag set (JS cannot read the cookie)
  [ ] Secure flag set (only sent over HTTPS)
  [ ] SameSite=Lax set (CSRF protection)
  [ ] Max-Age is set (cookie expires; not a session-only cookie)

Tests
  [ ] Valid token → 200, SecurityContext populated
  [ ] Expired token → 401
  [ ] Token domain mismatch → 401
  [ ] No auth header → passes through (endpoint decides)
  [ ] alg:none token → 401
  [ ] Tampered signature → 401
</code></pre></div></div> <hr/> <p><em>Next in the series: <a href="/blog/2026/layer4-api-key-authentication">Layer 4 — API Key Authentication: Securing Machine-to-Machine Requests</a></em></p>]]></content><author><name></name></author><category term="architecture"/><category term="jwt"/><category term="authentication"/><category term="spring-boot"/><category term="security"/><category term="oauth2"/><category term="java"/><summary type="html"><![CDATA[A deep dive into JWT authentication in a Spring Boot multi-tenant API — covering algorithm choice, claim design, expiry, token validation order, and the tests that prove it's working.]]></summary></entry><entry><title type="html">Security Auditing in a Multi-Tenant SaaS: Layered Filters, JWT, and API Keys</title><link href="https://crystalhansen.ca/blog/2026/security-auditing-layers-multitenant-saas/" rel="alternate" type="text/html" title="Security Auditing in a Multi-Tenant SaaS: Layered Filters, JWT, and API Keys"/><published>2026-05-12T00:00:00+00:00</published><updated>2026-05-12T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/security-auditing-layers-multitenant-saas</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/security-auditing-layers-multitenant-saas/"><![CDATA[<p>When you run a single API instance serving multiple tenants, security isn’t a single gate — it’s a chain of responsibility. Each layer in the chain handles one concern and hands off to the next. This post walks through how we structured that chain in a Spring Boot 3 application sitting behind nginx, and what auditing each layer looks like in practice.</p> <hr/> <h2 id="the-problem-space">The Problem Space</h2> <p>A multi-tenant SaaS API has a harder security job than a single-tenant one. Every request must answer two questions before touching business logic:</p> <ol> <li><strong>Who is this?</strong> (authentication)</li> <li><strong>Which tenant do they belong to — and are they allowed to act here?</strong> (tenant isolation + authorization)</li> </ol> <p>Get either wrong and you risk data leakage across tenant boundaries, which is often worse than a simple auth bypass.</p> <p>Our stack: <strong>nginx → Spring Boot 3 / Java 21</strong>, with Supabase (PostgreSQL) as the backing store and AWS SES for email. The filter chain runs on every inbound HTTP request.</p> <hr/> <h2 id="the-filter-chain">The Filter Chain</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Incoming Request
  └─▶ nginx (SSL termination, Let's Encrypt)
        └─▶ TenantResolverFilter  (domain → tenant context)
              └─▶ JwtAuthenticationFilter  (Bearer token)
                    └─▶ ApiKeyFilter  (X-API-KEY header)
                          └─▶ Spring Security (@PreAuthorize, CORS)
                                └─▶ Controller
</code></pre></div></div> <p>Each filter has a single job. Let’s audit each one.</p> <hr/> <h2 id="layer-1--ssl-termination-at-nginx">Layer 1 — SSL Termination at nginx</h2> <p><strong>What it does:</strong> All traffic arrives at nginx on port 443. nginx terminates TLS using Let’s Encrypt certificates (auto-renewed via certbot every 90 days) and proxies plain HTTP to the backend on an internal port.</p> <p><strong>Why it matters for auditing:</strong></p> <ul> <li>You can inspect TLS version and cipher suite configuration in one place (<code class="language-plaintext highlighter-rouge">ssl_protocols</code>, <code class="language-plaintext highlighter-rouge">ssl_ciphers</code> directives).</li> <li>Certificate expiry is the #1 cause of embarrassing production outages. Automating renewal and adding a monitoring alert (e.g., check cert expiry &gt; 14 days) closes that gap.</li> <li>All access logs at this layer capture real client IPs before any forwarding happens — important for rate limiting and abuse detection.</li> </ul> <p><strong>Audit checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>TLS 1.2 minimum, TLS 1.3 preferred</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Weak ciphers (<code class="language-plaintext highlighter-rouge">RC4</code>, <code class="language-plaintext highlighter-rouge">DES</code>, <code class="language-plaintext highlighter-rouge">3DES</code>) explicitly disabled</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">X-Forwarded-For</code> and <code class="language-plaintext highlighter-rouge">X-Real-IP</code> headers set correctly so the app sees the real client IP</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">certbot renew --dry-run</code> passes cleanly</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Access log format includes <code class="language-plaintext highlighter-rouge">$request_time</code> and <code class="language-plaintext highlighter-rouge">$upstream_response_time</code> for latency auditing</li> </ul> <hr/> <h2 id="layer-2--tenant-resolution">Layer 2 — Tenant Resolution</h2> <p><strong>What it does:</strong> A servlet filter reads the <code class="language-plaintext highlighter-rouge">Host</code> header from every request and sets a thread-local <code class="language-plaintext highlighter-rouge">TenantContext</code> with the resolved domain. Downstream, Hibernate applies a <code class="language-plaintext highlighter-rouge">@Filter</code> that appends <code class="language-plaintext highlighter-rouge">WHERE domain = :tenantDomain</code> to every query.</p> <p><strong>Why it matters for auditing:</strong> This is the layer most unique to multi-tenant systems and the one most likely to be overlooked. If tenant context is missing or incorrectly set, a query could return rows from the wrong tenant — a silent data breach.</p> <p><strong>What to verify:</strong></p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Every repository query should be scoped — never bare</span>
<span class="c1">// Bad:  SELECT * FROM appointments</span>
<span class="c1">// Good: SELECT * FROM appointments WHERE domain = 'tenant-a.example.com'</span>
</code></pre></div></div> <p><strong>Audit checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Unit test: request with <code class="language-plaintext highlighter-rouge">Host: tenant-a.example.com</code> never returns data belonging to <code class="language-plaintext highlighter-rouge">tenant-b.example.com</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Integration test: filter chain with no <code class="language-plaintext highlighter-rouge">Host</code> header returns <code class="language-plaintext highlighter-rouge">400 Bad Request</code>, never falls through to a default tenant</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Hibernate filter is enabled on <em>all</em> entities that carry tenant data (easy to miss a new entity)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Thread-local context is cleared in a <code class="language-plaintext highlighter-rouge">finally</code> block — not clearing it in a thread pool causes context bleed between requests</li> </ul> <hr/> <h2 id="layer-3--jwt-authentication">Layer 3 — JWT Authentication</h2> <p><strong>What it does:</strong> Reads the <code class="language-plaintext highlighter-rouge">Authorization: Bearer &lt;token&gt;</code> header, validates the signature (HS256 or RS256 depending on issuer), checks expiry, and sets the Spring <code class="language-plaintext highlighter-rouge">SecurityContext</code>.</p> <p><strong>Key decisions worth auditing:</strong></p> <table> <thead> <tr> <th>Setting</th> <th>Our Choice</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>Algorithm</td> <td>HS256 (internal) / RS256 (OAuth2)</td> <td>Symmetric for speed internally; asymmetric for external IdP</td> </tr> <tr> <td>Expiry</td> <td>1 hour</td> <td>Short enough to limit blast radius on token leak</td> </tr> <tr> <td>Refresh</td> <td>Sliding session cookie</td> <td>Browser clients don’t re-auth every hour</td> </tr> <tr> <td>Claims</td> <td><code class="language-plaintext highlighter-rouge">sub</code>, <code class="language-plaintext highlighter-rouge">domain</code>, <code class="language-plaintext highlighter-rouge">roles</code></td> <td>Tenant domain baked into the token — double-checks TenantContext</td> </tr> </tbody> </table> <p><strong>Audit checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Algorithm is explicitly whitelisted — never <code class="language-plaintext highlighter-rouge">"alg": "none"</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Expiry (<code class="language-plaintext highlighter-rouge">exp</code>) claim is validated, not just parsed</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Token domain claim is cross-checked against the resolved tenant context (defense in depth)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Failed validation returns <code class="language-plaintext highlighter-rouge">401</code>, not <code class="language-plaintext highlighter-rouge">403</code> — the distinction matters for client error handling</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Secrets are in environment variables, never in source code or <code class="language-plaintext highlighter-rouge">application.properties</code></li> </ul> <hr/> <h2 id="layer-4--api-key-filter">Layer 4 — API Key Filter</h2> <p><strong>What it does:</strong> Some endpoints (internal automation, admin tooling, scheduled jobs) authenticate with a static <code class="language-plaintext highlighter-rouge">X-API-KEY</code> header instead of a JWT. The filter checks for this header and, if valid, marks the request as authenticated and skips JWT validation.</p> <p><strong>The subtle bug we fixed — session passthrough:</strong></p> <p>Early versions of this filter had a precedence issue: a valid session cookie could be overridden by an invalid API key header, producing a confusing <code class="language-plaintext highlighter-rouge">401</code> for legitimately authenticated browser sessions. The fix was explicit: if a valid session already exists in the <code class="language-plaintext highlighter-rouge">SecurityContext</code>, the API key filter is a no-op.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Pseudocode — check existing auth before evaluating API key</span>
<span class="k">if</span> <span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()</span> <span class="o">!=</span> <span class="kc">null</span>
    <span class="o">&amp;&amp;</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">().</span><span class="na">isAuthenticated</span><span class="o">())</span> <span class="o">{</span>
    <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="k">return</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Audit checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>API keys are hashed at rest — never stored in plaintext</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Key rotation procedure is documented and tested</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Endpoints accepting API key auth are explicitly enumerated — not a blanket allowance</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Test case: valid session + invalid API key header → request succeeds (not <code class="language-plaintext highlighter-rouge">401</code>)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Test case: no session + valid API key header → request succeeds</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Test case: no session + invalid API key header → <code class="language-plaintext highlighter-rouge">401</code></li> </ul> <hr/> <h2 id="layer-5--spring-security-roles-and-cors">Layer 5 — Spring Security: Roles and CORS</h2> <p><strong>What it does:</strong> <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> annotations on controller methods enforce role-based access. CORS configuration whitelists specific frontend origins.</p> <p><strong>Roles in a multi-tenant context:</strong> Roles need to carry tenant scope — a <code class="language-plaintext highlighter-rouge">ROLE_ADMIN</code> for tenant A must not grant admin access to tenant B’s endpoints. We enforce this at two levels: the JWT <code class="language-plaintext highlighter-rouge">roles</code> claim is tenant-scoped, and <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> expressions check both role and tenant context.</p> <p><strong>CORS audit:</strong> CORS misconfigurations are a common finding in security reviews. The pattern to avoid:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// BAD — reflects any origin</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOriginPatterns</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"*"</span><span class="o">));</span>

<span class="c1">// GOOD — explicit whitelist</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOrigins</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span>
    <span class="s">"https://www.tenant-a.example.com"</span><span class="o">,</span>
    <span class="s">"https://www.tenant-b.example.com"</span>
<span class="o">));</span>
</code></pre></div></div> <p><strong>Audit checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>CORS allowed origins are a static whitelist, not a regex that can be tricked (e.g., <code class="language-plaintext highlighter-rouge">evil-tenant-a.example.com</code>)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">@PreAuthorize</code> is present on all state-changing endpoints (POST, PATCH, DELETE)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Read endpoints that return sensitive data also carry <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> — not just writes</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>A dedicated integration test hits each secured endpoint without auth and expects <code class="language-plaintext highlighter-rouge">401</code>/<code class="language-plaintext highlighter-rouge">403</code></li> </ul> <hr/> <h2 id="putting-it-together--the-audit-matrix">Putting It Together — The Audit Matrix</h2> <p>Here’s a condensed view of what to test at each layer:</p> <table> <thead> <tr> <th>Layer</th> <th>Tool</th> <th>Test Type</th> </tr> </thead> <tbody> <tr> <td>SSL / nginx</td> <td><code class="language-plaintext highlighter-rouge">testssl.sh</code>, <code class="language-plaintext highlighter-rouge">nmap --script ssl-enum-ciphers</code></td> <td>External scan</td> </tr> <tr> <td>Tenant isolation</td> <td>JUnit + <code class="language-plaintext highlighter-rouge">MockMvc</code>, custom <code class="language-plaintext highlighter-rouge">UserRepositoryDomainIsolationTest</code></td> <td>Unit + integration</td> </tr> <tr> <td>JWT</td> <td>JUnit <code class="language-plaintext highlighter-rouge">JwtAuthenticationFilterTest</code>, <code class="language-plaintext highlighter-rouge">JwtUtilTest</code></td> <td>Unit</td> </tr> <tr> <td>API Key</td> <td>JUnit <code class="language-plaintext highlighter-rouge">ApiKeyFilterTest</code> (valid key, invalid key, session passthrough)</td> <td>Unit</td> </tr> <tr> <td>Roles / CORS</td> <td>Spring Security integration tests (<code class="language-plaintext highlighter-rouge">SecurityConfigIT</code>, <code class="language-plaintext highlighter-rouge">AdminTokenIT</code>)</td> <td>Integration</td> </tr> </tbody> </table> <p>Running these as part of CI means every pull request proves the security chain is intact — not just that business logic works.</p> <hr/> <h2 id="a-note-on-auth-modes">A Note on Auth Modes</h2> <p>Our API supports four authentication paths that can coexist:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JWT (HS256, 1hr)         — stateless API clients, mobile
Session Cookie           — browser sessions (SameSite: Lax)
Admin API Key            — internal tooling, scheduled jobs
Google OAuth2            — Sign in with Google (external IdP)
</code></pre></div></div> <p>The filter chain handles all four without conflict because each filter checks for its own credential type and short-circuits cleanly if it finds a different (already valid) auth mode in the <code class="language-plaintext highlighter-rouge">SecurityContext</code>.</p> <hr/> <h2 id="key-takeaway">Key Takeaway</h2> <p>Security in a multi-tenant system is not one thing you add — it’s a sequence of concerns, each handled in the right place:</p> <ul> <li><strong>nginx</strong> handles TLS and access logging</li> <li><strong>Tenant filter</strong> handles data isolation</li> <li><strong>JWT filter</strong> handles user identity</li> <li><strong>API key filter</strong> handles machine-to-machine auth</li> <li><strong>Spring Security</strong> handles authorization</li> </ul> <p>When each layer has clear, testable responsibility, auditing becomes a checklist rather than a mystery. And when something breaks, the layer structure tells you exactly where to look.</p> <hr/> <p><em>Next up: how we handle email template overrides per tenant without leaking template data across tenant boundaries.</em></p>]]></content><author><name></name></author><category term="architecture"/><category term="security"/><category term="spring-boot"/><category term="multi-tenant"/><category term="jwt"/><category term="saas"/><category term="devops"/><summary type="html"><![CDATA[A practical walkthrough of how a layered security filter chain protects a multi-tenant Spring Boot API — covering SSL termination, tenant isolation, JWT validation, API key auth, and role-based access.]]></summary></entry><entry><title type="html">SNS Bounce &amp;amp; Complaint Webhook Handling with AWS SES</title><link href="https://crystalhansen.ca/blog/2026/ses-bounce-complaint-handling/" rel="alternate" type="text/html" title="SNS Bounce &amp;amp; Complaint Webhook Handling with AWS SES"/><published>2026-04-15T13:30:00+00:00</published><updated>2026-04-15T13:30:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/ses-bounce-complaint-handling</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/ses-bounce-complaint-handling/"><![CDATA[<blockquote> <p>📬 <strong>Series: Email Infrastructure with AWS SES</strong></p> <ol> <li><a href="/blog/2026/aws-ses-email-setup/">Overview, SMTP Setup, DNS &amp; Marketing</a></li> <li><a href="/blog/2026/ses-multi-tenant-email-service/">Multi-Tenant FROM Address Resolution &amp; Spring EmailService</a></li> <li><strong>You are here</strong> — SNS Bounce &amp; Complaint Webhook Handling</li> <li><a href="/blog/2026/ses-scheduled-nurture-sequences/">Scheduled Email Triggers &amp; Nurture Sequence Architecture</a></li> </ol> </blockquote> <hr/> <p>Sending an email is only half the job. When that email bounces, or the recipient marks it as spam, AWS SES knows about it — but your application doesn’t unless you wire up notifications. This post covers building the full feedback loop: SNS topic configuration, a webhook endpoint that receives and validates notifications, and the suppression list that keeps your reputation clean.</p> <hr/> <h2 id="why-bounce-handling-is-non-negotiable">Why Bounce Handling Is Non-Negotiable</h2> <p>AWS SES monitors two metrics that directly affect your ability to send:</p> <ul> <li><strong>Bounce rate:</strong> If this exceeds <strong>5%</strong>, SES will pause sending on your account. Above 10% risks permanent suspension.</li> <li><strong>Complaint rate:</strong> If this exceeds <strong>0.1%</strong> (one complaint per thousand sends), you’ll see degraded deliverability. Above 0.5% triggers review.</li> </ul> <p>These aren’t arbitrary thresholds — they reflect what major inbox providers (Gmail, Outlook) use to decide whether to accept your mail at all. If you ignore bounces and keep sending to invalid addresses, you’re not just risking your SES account; you’re damaging the domain reputation that your DNS records (SPF, DKIM, DMARC) took time to build.</p> <p>The fix is simple in principle: <strong>when SES tells you an address bounced or complained, never send to it again.</strong> The implementation is a webhook endpoint, a suppression table, and a pre-send check.</p> <hr/> <h2 id="ses-feedback-notifications-overview">SES Feedback Notifications Overview</h2> <p>SES delivers feedback through <strong>Amazon SNS (Simple Notification Service)</strong>. The flow is:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SES sends email
  → Delivery fails (bounce) or recipient reports spam (complaint)
    → SES publishes notification to SNS topic
      → SNS delivers to your webhook endpoint (HTTP/S subscription)
        → Your app processes event and updates suppression list
</code></pre></div></div> <p>There are three notification types:</p> <ul> <li><strong>Bounce</strong> — the email could not be delivered. Two subtypes: <em>hard</em> (permanent, e.g. address doesn’t exist) and <em>soft</em> (temporary, e.g. mailbox full).</li> <li><strong>Complaint</strong> — the recipient marked the email as spam via their inbox client.</li> <li><strong>Delivery</strong> — optional confirmation that the email was successfully delivered (useful for audit logs, not required for reputation management).</li> </ul> <p>Hard bounces and complaints must result in permanent suppression. Soft bounces can be retried with a backoff strategy, but after repeated soft bounces on the same address, suppress it as well.</p> <hr/> <h2 id="setting-up-the-sns-topic">Setting Up the SNS Topic</h2> <p><strong>Step 1: Create an SNS topic</strong></p> <p>In the AWS Console, go to <strong>SNS → Topics → Create topic</strong>. Choose <code class="language-plaintext highlighter-rouge">Standard</code> type. Name it something like <code class="language-plaintext highlighter-rouge">ses-feedback-notifications</code>.</p> <p><strong>Step 2: Subscribe your endpoint</strong></p> <p>Add an HTTPS subscription pointing to your application’s webhook URL:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Protocol: HTTPS
Endpoint: https://yourdomain.com/webhooks/ses-feedback
</code></pre></div></div> <p>AWS will send a confirmation request to your endpoint — your handler must respond by fetching the <code class="language-plaintext highlighter-rouge">SubscribeURL</code> in the payload. (See the webhook implementation below.)</p> <p><strong>Step 3: Connect the topic to SES</strong></p> <p>In <strong>SES → Verified Identities → your domain → Notifications</strong>:</p> <ul> <li>Set <strong>Bounces</strong> → your SNS topic</li> <li>Set <strong>Complaints</strong> → your SNS topic</li> <li>Optionally set <strong>Deliveries</strong> → your SNS topic</li> </ul> <p>Check <strong>“Include original headers”</strong> — this makes it easier to correlate notifications back to specific sends in your logs.</p> <hr/> <h2 id="the-webhook-endpoint">The Webhook Endpoint</h2> <p>SNS delivers notifications as HTTP POST requests with a <code class="language-plaintext highlighter-rouge">Content-Type: text/plain</code> body that contains a JSON payload. There are two categories of request to handle:</p> <ol> <li><strong>SubscriptionConfirmation</strong> — sent once when you add the HTTPS subscription; you must fetch the <code class="language-plaintext highlighter-rouge">SubscribeURL</code> to activate</li> <li><strong>Notification</strong> — the actual bounce or complaint event</li> </ol> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/webhooks"</span><span class="o">)</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="nd">@Slf4j</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SesFeedbackWebhookController</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">SesFeedbackProcessor</span> <span class="n">feedbackProcessor</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">RestTemplate</span> <span class="n">restTemplate</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ObjectMapper</span> <span class="n">objectMapper</span><span class="o">;</span>

    <span class="nd">@PostMapping</span><span class="o">(</span><span class="n">value</span> <span class="o">=</span> <span class="s">"/ses-feedback"</span><span class="o">,</span> <span class="n">consumes</span> <span class="o">=</span> <span class="s">"text/plain"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ResponseEntity</span><span class="o">&lt;</span><span class="nc">Void</span><span class="o">&gt;</span> <span class="nf">handleSesNotification</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">String</span> <span class="n">rawBody</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">JsonNode</span> <span class="n">root</span> <span class="o">=</span> <span class="n">objectMapper</span><span class="o">.</span><span class="na">readTree</span><span class="o">(</span><span class="n">rawBody</span><span class="o">);</span>
            <span class="nc">String</span> <span class="n">messageType</span> <span class="o">=</span> <span class="n">root</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"Type"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>

            <span class="k">switch</span> <span class="o">(</span><span class="n">messageType</span><span class="o">)</span> <span class="o">{</span>
                <span class="k">case</span> <span class="s">"SubscriptionConfirmation"</span> <span class="o">-&gt;</span> <span class="o">{</span>
                    <span class="c1">// Auto-confirm the SNS subscription</span>
                    <span class="nc">String</span> <span class="n">subscribeUrl</span> <span class="o">=</span> <span class="n">root</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"SubscribeURL"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>
                    <span class="n">restTemplate</span><span class="o">.</span><span class="na">getForObject</span><span class="o">(</span><span class="n">subscribeUrl</span><span class="o">,</span> <span class="nc">String</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
                    <span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"Confirmed SNS subscription for SES feedback"</span><span class="o">);</span>
                <span class="o">}</span>
                <span class="k">case</span> <span class="s">"Notification"</span> <span class="o">-&gt;</span> <span class="o">{</span>
                    <span class="nc">String</span> <span class="n">message</span> <span class="o">=</span> <span class="n">root</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"Message"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>
                    <span class="n">feedbackProcessor</span><span class="o">.</span><span class="na">process</span><span class="o">(</span><span class="n">objectMapper</span><span class="o">.</span><span class="na">readTree</span><span class="o">(</span><span class="n">message</span><span class="o">));</span>
                <span class="o">}</span>
                <span class="k">default</span> <span class="o">-&gt;</span> <span class="n">log</span><span class="o">.</span><span class="na">warn</span><span class="o">(</span><span class="s">"Unhandled SNS message type: {}"</span><span class="o">,</span> <span class="n">messageType</span><span class="o">);</span>
            <span class="o">}</span>

            <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">ok</span><span class="o">().</span><span class="na">build</span><span class="o">();</span>

        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">log</span><span class="o">.</span><span class="na">error</span><span class="o">(</span><span class="s">"Failed to process SNS notification"</span><span class="o">,</span> <span class="n">e</span><span class="o">);</span>
            <span class="c1">// Return 200 anyway to prevent SNS from retrying a malformed payload</span>
            <span class="k">return</span> <span class="nc">ResponseEntity</span><span class="o">.</span><span class="na">ok</span><span class="o">().</span><span class="na">build</span><span class="o">();</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <blockquote> <p><strong>Always return HTTP 200</strong> from your webhook, even on processing errors. If you return a 4xx or 5xx, SNS will retry delivery — which for malformed payloads just creates noise. Log the error and return 200; let your monitoring catch the anomaly.</p> </blockquote> <h3 id="validating-sns-message-signatures">Validating SNS Message Signatures</h3> <p>In production, you should validate that the notification genuinely came from AWS. SNS signs each message and includes the signature in the payload. The AWS Java SDK provides <code class="language-plaintext highlighter-rouge">SnsMessageManager</code> for this:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">SnsMessageManager</span> <span class="n">manager</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">SnsMessageManager</span><span class="o">(</span><span class="s">"us-east-1"</span><span class="o">);</span>
<span class="n">manager</span><span class="o">.</span><span class="na">parseMessage</span><span class="o">(</span><span class="n">rawBody</span><span class="o">.</span><span class="na">getBytes</span><span class="o">());</span> <span class="c1">// throws if signature invalid</span>
</code></pre></div></div> <p>Add this before processing any payload in a security-conscious environment.</p> <hr/> <h2 id="processing-bounce-and-complaint-events">Processing Bounce and Complaint Events</h2> <p>The <code class="language-plaintext highlighter-rouge">Message</code> field inside a Notification contains the actual SES feedback JSON. Its structure depends on the notification type.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Service</span>
<span class="nd">@RequiredArgsConstructor</span>
<span class="nd">@Slf4j</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SesFeedbackProcessor</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">EmailSuppressionRepository</span> <span class="n">suppressionRepo</span><span class="o">;</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">process</span><span class="o">(</span><span class="nc">JsonNode</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">notificationType</span> <span class="o">=</span> <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"notificationType"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>

        <span class="k">switch</span> <span class="o">(</span><span class="n">notificationType</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">case</span> <span class="s">"Bounce"</span> <span class="o">-&gt;</span> <span class="n">processBounce</span><span class="o">(</span><span class="n">message</span><span class="o">);</span>
            <span class="k">case</span> <span class="s">"Complaint"</span> <span class="o">-&gt;</span> <span class="n">processComplaint</span><span class="o">(</span><span class="n">message</span><span class="o">);</span>
            <span class="k">case</span> <span class="s">"Delivery"</span> <span class="o">-&gt;</span> <span class="n">log</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"Delivery confirmed for message: {}"</span><span class="o">,</span>
                <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"mail"</span><span class="o">).</span><span class="na">path</span><span class="o">(</span><span class="s">"messageId"</span><span class="o">).</span><span class="na">asText</span><span class="o">());</span>
            <span class="k">default</span> <span class="o">-&gt;</span> <span class="n">log</span><span class="o">.</span><span class="na">warn</span><span class="o">(</span><span class="s">"Unknown SES notification type: {}"</span><span class="o">,</span> <span class="n">notificationType</span><span class="o">);</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="kt">void</span> <span class="nf">processBounce</span><span class="o">(</span><span class="nc">JsonNode</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">bounceType</span> <span class="o">=</span> <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"bounce"</span><span class="o">).</span><span class="na">path</span><span class="o">(</span><span class="s">"bounceType"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>
        <span class="nc">JsonNode</span> <span class="n">recipients</span> <span class="o">=</span> <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"bounce"</span><span class="o">).</span><span class="na">path</span><span class="o">(</span><span class="s">"bouncedRecipients"</span><span class="o">);</span>

        <span class="kt">boolean</span> <span class="n">isPermanent</span> <span class="o">=</span> <span class="s">"Permanent"</span><span class="o">.</span><span class="na">equals</span><span class="o">(</span><span class="n">bounceType</span><span class="o">);</span>

        <span class="k">for</span> <span class="o">(</span><span class="nc">JsonNode</span> <span class="n">recipient</span> <span class="o">:</span> <span class="n">recipients</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">String</span> <span class="n">email</span> <span class="o">=</span> <span class="n">recipient</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"emailAddress"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>
            <span class="nc">String</span> <span class="n">diagnosticCode</span> <span class="o">=</span> <span class="n">recipient</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"diagnosticCode"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>

            <span class="n">log</span><span class="o">.</span><span class="na">warn</span><span class="o">(</span><span class="s">"Bounce [{}] for address: {} — {}"</span><span class="o">,</span> <span class="n">bounceType</span><span class="o">,</span> <span class="n">email</span><span class="o">,</span> <span class="n">diagnosticCode</span><span class="o">);</span>

            <span class="k">if</span> <span class="o">(</span><span class="n">isPermanent</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">suppressionRepo</span><span class="o">.</span><span class="na">upsert</span><span class="o">(</span><span class="nc">EmailSuppression</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
                    <span class="o">.</span><span class="na">email</span><span class="o">(</span><span class="n">email</span><span class="o">)</span>
                    <span class="o">.</span><span class="na">reason</span><span class="o">(</span><span class="nc">SuppressionReason</span><span class="o">.</span><span class="na">HARD_BOUNCE</span><span class="o">)</span>
                    <span class="o">.</span><span class="na">diagnosticCode</span><span class="o">(</span><span class="n">diagnosticCode</span><span class="o">)</span>
                    <span class="o">.</span><span class="na">suppressedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">())</span>
                    <span class="o">.</span><span class="na">build</span><span class="o">());</span>
            <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
                <span class="c1">// Soft bounce — record it; suppress after threshold</span>
                <span class="n">suppressionRepo</span><span class="o">.</span><span class="na">recordSoftBounce</span><span class="o">(</span><span class="n">email</span><span class="o">);</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>

    <span class="kd">private</span> <span class="kt">void</span> <span class="nf">processComplaint</span><span class="o">(</span><span class="nc">JsonNode</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">JsonNode</span> <span class="n">recipients</span> <span class="o">=</span> <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"complaint"</span><span class="o">).</span><span class="na">path</span><span class="o">(</span><span class="s">"complainedRecipients"</span><span class="o">);</span>
        <span class="nc">String</span> <span class="n">feedbackType</span> <span class="o">=</span> <span class="n">message</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"complaint"</span><span class="o">)</span>
            <span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"complaintFeedbackType"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>

        <span class="k">for</span> <span class="o">(</span><span class="nc">JsonNode</span> <span class="n">recipient</span> <span class="o">:</span> <span class="n">recipients</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">String</span> <span class="n">email</span> <span class="o">=</span> <span class="n">recipient</span><span class="o">.</span><span class="na">path</span><span class="o">(</span><span class="s">"emailAddress"</span><span class="o">).</span><span class="na">asText</span><span class="o">();</span>

            <span class="n">log</span><span class="o">.</span><span class="na">warn</span><span class="o">(</span><span class="s">"Complaint [{}] from address: {}"</span><span class="o">,</span> <span class="n">feedbackType</span><span class="o">,</span> <span class="n">email</span><span class="o">);</span>

            <span class="n">suppressionRepo</span><span class="o">.</span><span class="na">upsert</span><span class="o">(</span><span class="nc">EmailSuppression</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
                <span class="o">.</span><span class="na">email</span><span class="o">(</span><span class="n">email</span><span class="o">)</span>
                <span class="o">.</span><span class="na">reason</span><span class="o">(</span><span class="nc">SuppressionReason</span><span class="o">.</span><span class="na">COMPLAINT</span><span class="o">)</span>
                <span class="o">.</span><span class="na">suppressedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">())</span>
                <span class="o">.</span><span class="na">build</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="suppression-list-management">Suppression List Management</h2> <p>The suppression list is a simple table that stores addresses that must not receive email:</p> <div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">email_suppression</span> <span class="p">(</span>
    <span class="n">id</span>            <span class="nb">BIGINT</span> <span class="n">AUTO_INCREMENT</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">email</span>         <span class="nb">VARCHAR</span><span class="p">(</span><span class="mi">255</span><span class="p">)</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
    <span class="n">reason</span>        <span class="nb">ENUM</span><span class="p">(</span><span class="s1">'HARD_BOUNCE'</span><span class="p">,</span> <span class="s1">'SOFT_BOUNCE'</span><span class="p">,</span> <span class="s1">'COMPLAINT'</span><span class="p">,</span> <span class="s1">'MANUAL'</span><span class="p">)</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
    <span class="n">diagnostic</span>    <span class="nb">TEXT</span><span class="p">,</span>
    <span class="n">soft_bounce_count</span> <span class="nb">INT</span> <span class="k">DEFAULT</span> <span class="mi">0</span><span class="p">,</span>
    <span class="n">suppressed_at</span> <span class="nb">TIMESTAMP</span> <span class="k">NOT</span> <span class="k">NULL</span><span class="p">,</span>
    <span class="k">UNIQUE</span> <span class="k">KEY</span> <span class="n">uq_email</span> <span class="p">(</span><span class="n">email</span><span class="p">)</span>
<span class="p">);</span>
</code></pre></div></div> <h3 id="pre-send-check-in-emailservice">Pre-Send Check in EmailService</h3> <p>Before every send, check the suppression list:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">sendEmail</span><span class="o">(</span><span class="nc">EmailRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="c1">// Guard: never send to suppressed addresses</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">suppressionRepo</span><span class="o">.</span><span class="na">isSuppressed</span><span class="o">(</span><span class="n">request</span><span class="o">.</span><span class="na">getRecipientEmail</span><span class="o">()))</span> <span class="o">{</span>
        <span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"Skipping suppressed address: {}"</span><span class="o">,</span> <span class="n">request</span><span class="o">.</span><span class="na">getRecipientEmail</span><span class="o">());</span>
        <span class="k">return</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="c1">// ... rest of send logic</span>
<span class="o">}</span>
</code></pre></div></div> <p>This should be a fast indexed lookup — put an index on the <code class="language-plaintext highlighter-rouge">email</code> column and keep it in memory if your list is large enough to warrant it.</p> <h3 id="soft-bounce-threshold">Soft Bounce Threshold</h3> <p>Track soft bounces and auto-suppress after a configurable number of failures:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kt">void</span> <span class="nf">recordSoftBounce</span><span class="o">(</span><span class="nc">String</span> <span class="n">email</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">EmailSuppression</span> <span class="n">existing</span> <span class="o">=</span> <span class="n">findByEmail</span><span class="o">(</span><span class="n">email</span><span class="o">);</span>
    <span class="kt">int</span> <span class="n">newCount</span> <span class="o">=</span> <span class="o">(</span><span class="n">existing</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">?</span> <span class="n">existing</span><span class="o">.</span><span class="na">getSoftBounceCount</span><span class="o">()</span> <span class="o">:</span> <span class="mi">0</span><span class="o">)</span> <span class="o">+</span> <span class="mi">1</span><span class="o">;</span>

    <span class="k">if</span> <span class="o">(</span><span class="n">newCount</span> <span class="o">&gt;=</span> <span class="n">softBounceThreshold</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// configurable, e.g. 3</span>
        <span class="n">upsert</span><span class="o">(</span><span class="nc">EmailSuppression</span><span class="o">.</span><span class="na">builder</span><span class="o">()</span>
            <span class="o">.</span><span class="na">email</span><span class="o">(</span><span class="n">email</span><span class="o">)</span>
            <span class="o">.</span><span class="na">reason</span><span class="o">(</span><span class="nc">SuppressionReason</span><span class="o">.</span><span class="na">SOFT_BOUNCE</span><span class="o">)</span>
            <span class="o">.</span><span class="na">softBounceCount</span><span class="o">(</span><span class="n">newCount</span><span class="o">)</span>
            <span class="o">.</span><span class="na">suppressedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">())</span>
            <span class="o">.</span><span class="na">build</span><span class="o">());</span>
    <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
        <span class="n">updateSoftBounceCount</span><span class="o">(</span><span class="n">email</span><span class="o">,</span> <span class="n">newCount</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="monitoring-your-sending-reputation">Monitoring Your Sending Reputation</h2> <p>Beyond suppression, set up visibility into your sending health:</p> <p><strong>SES Account Dashboard</strong> — In the AWS Console under SES, the account overview shows your current bounce and complaint rates. Check this weekly when you’re ramping up volume.</p> <p><strong>CloudWatch Alarms</strong> — SES publishes metrics to CloudWatch. Create alarms for:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Reputation.BounceRate</code> &gt; 3% → SNS alert</li> <li><code class="language-plaintext highlighter-rouge">Reputation.ComplaintRate</code> &gt; 0.08% → SNS alert</li> </ul> <p>These give you early warning before you approach the thresholds that trigger SES action.</p> <p><strong>Postmaster Tools</strong> — Both <a href="https://postmaster.google.com">Gmail Postmaster Tools</a> and <a href="https://sendersupport.olc.protection.outlook.com/snds/">Microsoft SNDS</a> offer domain-level reputation dashboards that show how the inbox providers themselves perceive your sending. These are free and worth setting up once you’re sending meaningful volume.</p> <hr/> <h2 id="whats-next">What’s Next</h2> <p>With bounce and complaint handling in place, your sending infrastructure is now self-protecting. The final piece is building the <strong>scheduled trigger and nurture sequence system</strong> that drives your marketing engine:</p> <ul> <li><a href="/blog/2026/ses-scheduled-nurture-sequences/">Part 4: Scheduled Email Triggers &amp; Nurture Sequence Architecture</a></li> <li><a href="/blog/2026/aws-ses-email-setup/">Back to Part 1: Overview &amp; DNS Setup</a></li> </ul>]]></content><author><name>your_name</name></author><category term="engineering"/><category term="infrastructure"/><category term="aws"/><category term="ses"/><category term="sns"/><category term="email"/><category term="spring-boot"/><category term="java"/><category term="deliverability"/><summary type="html"><![CDATA[How to configure AWS SNS to receive SES bounce and complaint notifications, expose a webhook endpoint, and maintain a suppression list that protects your sending reputation.]]></summary></entry><entry><title type="html">Building Sovereign Social Automation with Meta’s System Users</title><link href="https://crystalhansen.ca/blog/2026/building-sovereign-social-automation/" rel="alternate" type="text/html" title="Building Sovereign Social Automation with Meta’s System Users"/><published>2026-04-15T00:00:00+00:00</published><updated>2026-04-15T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/building-sovereign-social-automation</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/building-sovereign-social-automation/"><![CDATA[<h2 id="building-sovereign-social-automation-with-metas-system-users">Building Sovereign Social Automation with Meta’s System Users</h2> <p>After fighting with Meta’s disappearing browser sessions and flaky personal account tokens, I successfully migrated the <strong>Fluxo Social Engine</strong> to a “Sovereign Infrastructure” model.</p> <p>If you are building a multi-tenant SaaS that needs to post to Instagram and Facebook on behalf of users, this is the blueprint you need.</p> <h2 id="the-problem-session-flakes">The Problem: Session Flakes</h2> <p>Standard User Access Tokens expire every 60 days and break if the user changes their password or security settings. For a SaaS like <strong>Fluxo</strong>, we need permanent, “headless” authority.</p> <h2 id="the-solution-the-system-user-triple-link">The Solution: The System User “Triple-Link”</h2> <p>By using a <strong>System User</strong> (a non-human bot user within Business Manager), we achieve permanent stability.</p> <h3 id="phase-1-internal-admin-setup">Phase 1: Internal Admin Setup</h3> <p>To set this up, you must establish three distinct links in the Meta Business Suite:</p> <ol> <li><strong>System User → Assets:</strong> Create an Admin System User and assign it to your Facebook Page, Instagram Account, and Meta App with <strong>Full Control</strong>.</li> <li><strong>App → Assets:</strong> Add your Meta App as a <em>Connected Asset</em> to the Facebook Page.</li> <li><strong>App → Use Cases:</strong> In the Developer Portal, add the “Facebook Page” use case to unlock <code class="language-plaintext highlighter-rouge">pages_manage_posts</code>.</li> </ol> <h3 id="phase-2-the-two-key-protocol">Phase 2: The “Two-Key” Protocol</h3> <p>Facebook’s Feed API is stricter than Instagram’s. While IG accepts a System User token, the FB Feed requires a <strong>Page Access Token</strong>.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Exchange System Token for Page Token</span>
curl <span class="nt">-X</span> GET <span class="s2">"[https://graph.facebook.com/v25.0/](https://graph.facebook.com/v25.0/){page_id}?fields=access_token&amp;access_token={SYSTEM_TOKEN}"</span>
</code></pre></div></div> <p>Implementation Logic Our marketingPy engine follows this asynchronous dispatch flow:</p> <p>Fetch the ephemeral Page Token using the permanent System Token.</p> <p>Post to Instagram: (2-step container process with a 45s sleep).</p> <p>Post to Facebook Feed: Direct POST to /{page_id}/feed using the Page Token.</p> <figure> <picture> <img src="/assets/img/posts/social-flow.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="Automation Workflow" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>For Tenants (The Handshake) When a new user joins the platform, they simply:</p> <p>Link their IG Business account to their FB Page.</p> <p>Accept our Tester Invite.</p> <p>Confirm the connection in their Page Settings.</p> <p>This creates a seamless “Sovereign” bridge where the user owns their data, and Fluxo provides the power.</p>]]></content><author><name></name></author><category term="engineering"/><category term="saas"/><category term="automation"/><category term="api"/><category term="meta"/><summary type="html"><![CDATA[A technical guide on bypassing session flakes using Meta System Users for multi-tenant SaaS.]]></summary></entry></feed>