<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://crystalhansen.ca/feed.xml" rel="self" type="application/atom+xml"/><link href="https://crystalhansen.ca/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-08T20:07:20+00:00</updated><id>https://crystalhansen.ca/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Migrating marketingPy from Docker Compose to AWS Lambda</title><link href="https://crystalhansen.ca/blog/2026/migrating-marketingpy-to-aws-lambda/" rel="alternate" type="text/html" title="Migrating marketingPy from Docker Compose to AWS Lambda"/><published>2026-07-08T00:00:00+00:00</published><updated>2026-07-08T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/migrating-marketingpy-to-aws-lambda</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/migrating-marketingpy-to-aws-lambda/"><![CDATA[<p><code class="language-plaintext highlighter-rouge">marketingPy</code> is the automation service behind <a href="https://github.com/">fluxo</a> — it polls trends, schedules social posts, scores leads, and runs the email nurture sequences for tenant accounts. It started as four Docker containers on a single EC2 box. This post walks through moving it to three container-image Lambda functions, and the one piece — delayed one-time email sends — that doesn’t have an obvious serverless equivalent until you go looking for it.</p> <hr/> <h2 id="the-docker-compose-baseline">The Docker Compose Baseline</h2> <p>The original stack was a <code class="language-plaintext highlighter-rouge">docker-compose.yml</code> with five services:</p> <table> <thead> <tr> <th>Service</th> <th>Role</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">marketing-api</code></td> <td>FastAPI app, plus an in-process APScheduler running four cron-style jobs</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">arq-worker</code></td> <td>Consumes an async task queue (publish jobs, trend processing, engagement checks)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">redis</code></td> <td>Broker for <code class="language-plaintext highlighter-rouge">arq</code>, with <code class="language-plaintext highlighter-rouge">--appendonly yes</code> so queued jobs survived restarts</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">db</code></td> <td>MySQL 8</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">lead-scorer</code></td> <td>A separate scoring microservice</td> </tr> </tbody> </table> <p>Everything ran on one <code class="language-plaintext highlighter-rouge">t3.medium</code> EC2 instance behind an ALB — the same shape covered in <a href="/blog/2026/mvn-build-test-deploy-ec2/">Running Maven Builds: From Testing to EC2 Deployment</a>, just for a Python service instead of Java. Documented cost for that box alone was roughly $40–80/month, running 24/7 regardless of load.</p> <hr/> <h2 id="why-move-at-all">Why Move at All</h2> <p>The workload doesn’t justify a box that’s always on:</p> <ul> <li>The scheduler jobs are a poll every 30 minutes, a calendar check every 5, an hourly analytics pass, and one weekly digest. That’s idle most of the second.</li> <li>The worker queue is bursty — a flurry of publish/scoring tasks around content creation, then nothing.</li> <li>Redis existed purely to give <code class="language-plaintext highlighter-rouge">arq</code> a broker. It wasn’t doing anything else.</li> <li>One EC2 instance is also one failure domain. If it goes down, the API, the scheduler, and the worker all go down together.</li> </ul> <p>None of that is a criticism of Compose — it’s still the right call for <a href="/blog/2026/designing-multi-tenant-saas-where-industries-feel-brand/">bookit</a>, which has steadier traffic. For a service that’s mostly waiting, pay-per-invocation fits better than pay-per-hour.</p> <hr/> <h2 id="one-image-three-entry-points">One Image, Three Entry Points</h2> <p>Rather than three separate deployable packages, <code class="language-plaintext highlighter-rouge">marketingPy</code> builds <strong>one</strong> container image and points three Lambda functions at different entry points inside it:</p> <div class="language-dockerfile highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Dockerfile.lambda</span>
<span class="k">FROM</span><span class="s"> public.ecr.aws/lambda/python:3.10</span>

<span class="k">COPY</span><span class="s"> requirements-lambda.txt .</span>
<span class="k">RUN </span>pip <span class="nb">install</span> <span class="nt">--no-cache-dir</span> <span class="nt">-r</span> requirements-lambda.txt

<span class="k">COPY</span><span class="s"> . .</span>

<span class="c"># Default entry point — overridden per function in template.yaml</span>
<span class="k">CMD</span><span class="s"> ["lambda_handler.api_handler"]</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">template.yaml</code> (AWS SAM) then overrides the command per function:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">ApiFunction</span><span class="pi">:</span>
  <span class="na">Type</span><span class="pi">:</span> <span class="s">AWS::Serverless::Function</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">PackageType</span><span class="pi">:</span> <span class="s">Image</span>
    <span class="na">ImageUri</span><span class="pi">:</span> <span class="kt">!Ref</span> <span class="s">EcrImageUri</span>
    <span class="na">ImageConfig</span><span class="pi">:</span>
      <span class="na">Command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">lambda_handler.api_handler"</span><span class="pi">]</span>
    <span class="na">Timeout</span><span class="pi">:</span> <span class="m">29</span>          <span class="c1"># API Gateway's hard cap is 30s</span>
    <span class="na">ReservedConcurrentExecutions</span><span class="pi">:</span> <span class="m">5</span>

<span class="na">SchedulerFunction</span><span class="pi">:</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">ImageConfig</span><span class="pi">:</span>
      <span class="na">Command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">lambda_handler.scheduler_handler"</span><span class="pi">]</span>
    <span class="na">Timeout</span><span class="pi">:</span> <span class="m">300</span>

<span class="na">WorkerFunction</span><span class="pi">:</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">ImageConfig</span><span class="pi">:</span>
      <span class="na">Command</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">lambda_handler.worker_handler"</span><span class="pi">]</span>
    <span class="na">Timeout</span><span class="pi">:</span> <span class="m">300</span>
    <span class="na">ReservedConcurrentExecutions</span><span class="pi">:</span> <span class="m">3</span>
</code></pre></div></div> <p>Same dependencies, same code, one build — just three different handler functions in <code class="language-plaintext highlighter-rouge">lambda_handler.py</code> doing three different jobs:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">api_handler</span><span class="p">(</span><span class="n">event</span><span class="p">:</span> <span class="nb">dict</span><span class="p">,</span> <span class="n">context</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">API Gateway HTTP API → FastAPI via Mangum. All 30+ endpoints work unchanged.</span><span class="sh">"""</span>
    <span class="nf">_load_secrets</span><span class="p">()</span>
    <span class="kn">from</span> <span class="n">mangum</span> <span class="kn">import</span> <span class="n">Mangum</span>
    <span class="kn">from</span> <span class="n">main</span> <span class="kn">import</span> <span class="n">app</span>
    <span class="n">adapter</span> <span class="o">=</span> <span class="nc">Mangum</span><span class="p">(</span><span class="n">app</span><span class="p">,</span> <span class="n">lifespan</span><span class="o">=</span><span class="sh">"</span><span class="s">off</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">adapter</span><span class="p">(</span><span class="n">event</span><span class="p">,</span> <span class="n">context</span><span class="p">)</span>
</code></pre></div></div> <p>Wrapping the existing FastAPI app in <a href="https://mangum.io/">Mangum</a> meant every route handler — all 30-plus of them — needed zero changes. The API surface <code class="language-plaintext highlighter-rouge">bookit</code> calls into is identical; only what’s behind it changed.</p> <p>Container images over zip deployment was the only real option here: <code class="language-plaintext highlighter-rouge">Pillow</code> needs native image libraries for the visual generator, and packaging those correctly in a zip layer is more fragile than just shipping the whole image through ECR.</p> <p>Image-level timeout and concurrency limits matter differently per function. <code class="language-plaintext highlighter-rouge">ApiFunction</code> is capped at 29 seconds because API Gateway itself hangs up at 30 — anything the API does that might run long needs to hand off to the worker instead of blocking a request. <code class="language-plaintext highlighter-rouge">SchedulerFunction</code> and <code class="language-plaintext highlighter-rouge">WorkerFunction</code> aren’t gated by API Gateway, so they get a full 5 minutes. <code class="language-plaintext highlighter-rouge">ReservedConcurrentExecutions</code> on <code class="language-plaintext highlighter-rouge">WorkerFunction</code> (3) exists because the tasks it runs hit rate-limited social platform APIs — more concurrency there just means more 429s, not more throughput.</p> <hr/> <h2 id="replacing-apscheduler-with-eventbridge">Replacing APScheduler with EventBridge</h2> <p><code class="language-plaintext highlighter-rouge">main.py</code>’s <code class="language-plaintext highlighter-rouge">lifespan</code> used to start an <code class="language-plaintext highlighter-rouge">AsyncIOScheduler</code> and register four jobs at boot. That doesn’t work in Lambda — there’s no long-lived process for APScheduler to run inside. The four jobs moved to EventBridge Scheduler rules that invoke <code class="language-plaintext highlighter-rouge">scheduler_handler</code> directly:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">SchedulerFunction</span><span class="pi">:</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">Events</span><span class="pi">:</span>
      <span class="na">TrendPoller</span><span class="pi">:</span>
        <span class="na">Type</span><span class="pi">:</span> <span class="s">ScheduleV2</span>
        <span class="na">Properties</span><span class="pi">:</span>
          <span class="na">ScheduleExpression</span><span class="pi">:</span> <span class="s2">"</span><span class="s">rate(30</span><span class="nv"> </span><span class="s">minutes)"</span>
          <span class="na">Input</span><span class="pi">:</span> <span class="s1">'</span><span class="s">{"job":</span><span class="nv"> </span><span class="s">"trend_poller"}'</span>
      <span class="na">CalendarCheck</span><span class="pi">:</span>
        <span class="na">Type</span><span class="pi">:</span> <span class="s">ScheduleV2</span>
        <span class="na">Properties</span><span class="pi">:</span>
          <span class="na">ScheduleExpression</span><span class="pi">:</span> <span class="s2">"</span><span class="s">rate(5</span><span class="nv"> </span><span class="s">minutes)"</span>
          <span class="na">Input</span><span class="pi">:</span> <span class="s1">'</span><span class="s">{"job":</span><span class="nv"> </span><span class="s">"calendar_check"}'</span>
      <span class="na">WeeklyDigest</span><span class="pi">:</span>
        <span class="na">Type</span><span class="pi">:</span> <span class="s">ScheduleV2</span>
        <span class="na">Properties</span><span class="pi">:</span>
          <span class="na">ScheduleExpression</span><span class="pi">:</span> <span class="s2">"</span><span class="s">cron(0</span><span class="nv"> </span><span class="s">9</span><span class="nv"> </span><span class="s">?</span><span class="nv"> </span><span class="s">*</span><span class="nv"> </span><span class="s">MON</span><span class="nv"> </span><span class="s">*)"</span>
          <span class="na">Input</span><span class="pi">:</span> <span class="s1">'</span><span class="s">{"job":</span><span class="nv"> </span><span class="s">"weekly_digest"}'</span>
      <span class="na">AnalyticsCollector</span><span class="pi">:</span>
        <span class="na">Type</span><span class="pi">:</span> <span class="s">ScheduleV2</span>
        <span class="na">Properties</span><span class="pi">:</span>
          <span class="na">ScheduleExpression</span><span class="pi">:</span> <span class="s2">"</span><span class="s">rate(1</span><span class="nv"> </span><span class="s">hour)"</span>
          <span class="na">Input</span><span class="pi">:</span> <span class="s1">'</span><span class="s">{"job":</span><span class="nv"> </span><span class="s">"analytics_collector"}'</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">scheduler_handler</code> just dispatches on the <code class="language-plaintext highlighter-rouge">job</code> key EventBridge passes in:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_SCHEDULER_JOBS</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">trend_poller</span><span class="sh">"</span><span class="p">:</span>        <span class="sh">"</span><span class="s">_run_trend_poller</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">calendar_check</span><span class="sh">"</span><span class="p">:</span>      <span class="sh">"</span><span class="s">_run_calendar_check</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">weekly_digest</span><span class="sh">"</span><span class="p">:</span>       <span class="sh">"</span><span class="s">_run_weekly_digest</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">analytics_collector</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">_run_analytics_collector</span><span class="sh">"</span><span class="p">,</span>
<span class="p">}</span>

<span class="k">def</span> <span class="nf">scheduler_handler</span><span class="p">(</span><span class="n">event</span><span class="p">:</span> <span class="nb">dict</span><span class="p">,</span> <span class="n">context</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="nf">_load_secrets</span><span class="p">()</span>
    <span class="n">job</span> <span class="o">=</span> <span class="n">event</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">job</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">job</span> <span class="ow">or</span> <span class="n">job</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">_SCHEDULER_JOBS</span><span class="p">:</span>
        <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">error</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">job</span><span class="sh">"</span><span class="p">:</span> <span class="n">job</span><span class="p">}</span>
    <span class="kn">import</span> <span class="n">asyncio</span>
    <span class="n">handler_fn</span> <span class="o">=</span> <span class="nf">globals</span><span class="p">()[</span><span class="n">_SCHEDULER_JOBS</span><span class="p">[</span><span class="n">job</span><span class="p">]]</span>
    <span class="n">asyncio</span><span class="p">.</span><span class="nf">get_event_loop</span><span class="p">().</span><span class="nf">run_until_complete</span><span class="p">(</span><span class="nf">handler_fn</span><span class="p">())</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">status</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">ok</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">job</span><span class="sh">"</span><span class="p">:</span> <span class="n">job</span><span class="p">}</span>
</code></pre></div></div> <p>APScheduler didn’t disappear entirely — it’s kept for local dev, gated behind an <code class="language-plaintext highlighter-rouge">AWS_LAMBDA_FUNCTION_NAME</code> check so the same codebase runs identically with <code class="language-plaintext highlighter-rouge">uvicorn</code> on a laptop and as three Lambda functions in production:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">is_lambda</span> <span class="o">=</span> <span class="nf">bool</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">AWS_LAMBDA_FUNCTION_NAME</span><span class="sh">"</span><span class="p">))</span>
<span class="k">if</span> <span class="ow">not</span> <span class="n">is_lambda</span><span class="p">:</span>
    <span class="kn">from</span> <span class="n">apscheduler.schedulers.asyncio</span> <span class="kn">import</span> <span class="n">AsyncIOScheduler</span>
    <span class="n">scheduler</span> <span class="o">=</span> <span class="nc">AsyncIOScheduler</span><span class="p">()</span>
    <span class="n">scheduler</span><span class="p">.</span><span class="nf">add_job</span><span class="p">(</span><span class="n">poll_all_sources</span><span class="p">,</span> <span class="sh">"</span><span class="s">interval</span><span class="sh">"</span><span class="p">,</span> <span class="n">minutes</span><span class="o">=</span><span class="mi">30</span><span class="p">,</span> <span class="nb">id</span><span class="o">=</span><span class="sh">"</span><span class="s">trend_poller</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1"># ...
</span>    <span class="n">scheduler</span><span class="p">.</span><span class="nf">start</span><span class="p">()</span>
</code></pre></div></div> <hr/> <h2 id="the-hard-part--delayed-one-time-sends">The Hard Part — Delayed, One-Time Sends</h2> <p>The recurring jobs above were the easy 80%. The harder problem: nurture emails need to fire once, at a specific future timestamp, per-recipient — not on a shared cron. The old code used <code class="language-plaintext highlighter-rouge">arq</code>’s <code class="language-plaintext highlighter-rouge">_defer_by</code> to push a delayed job onto Redis. There’s no Redis anymore.</p> <p>EventBridge Scheduler turns out to support exactly this — one-time schedules with an <code class="language-plaintext highlighter-rouge">at()</code> expression, not just recurring <code class="language-plaintext highlighter-rouge">rate()</code>/<code class="language-plaintext highlighter-rouge">cron()</code> ones. Each drip step becomes its own disposable schedule that invokes the worker Lambda directly and deletes itself once it fires:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">async</span> <span class="k">def</span> <span class="nf">_schedule_drip_eventbridge</span><span class="p">(</span>
    <span class="n">email</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span>
    <span class="n">emails_with_delays</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">tuple</span><span class="p">[</span><span class="nb">int</span><span class="p">,</span> <span class="nb">str</span><span class="p">,</span> <span class="nb">str</span><span class="p">]],</span>
<span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">scheduler</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">"</span><span class="s">scheduler</span><span class="sh">"</span><span class="p">,</span> <span class="n">region_name</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">AWS_REGION</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">ca-central-1</span><span class="sh">"</span><span class="p">))</span>
    <span class="n">worker_arn</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">WORKER_LAMBDA_ARN</span><span class="sh">"</span><span class="p">,</span> <span class="sh">""</span><span class="p">)</span>
    <span class="n">scheduler_role_arn</span> <span class="o">=</span> <span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">SCHEDULER_ROLE_ARN</span><span class="sh">"</span><span class="p">,</span> <span class="sh">""</span><span class="p">)</span>

    <span class="n">safe_email</span> <span class="o">=</span> <span class="n">email</span><span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sh">"</span><span class="s">@</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">_at_</span><span class="sh">"</span><span class="p">).</span><span class="nf">replace</span><span class="p">(</span><span class="sh">"</span><span class="s">.</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">_</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="p">(</span><span class="n">delay_hours</span><span class="p">,</span> <span class="n">subject</span><span class="p">,</span> <span class="n">body_html</span><span class="p">)</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">emails_with_delays</span><span class="p">):</span>
        <span class="n">fire_at</span> <span class="o">=</span> <span class="n">datetime</span><span class="p">.</span><span class="nf">now</span><span class="p">(</span><span class="n">timezone</span><span class="p">.</span><span class="n">utc</span><span class="p">)</span> <span class="o">+</span> <span class="nf">timedelta</span><span class="p">(</span><span class="n">hours</span><span class="o">=</span><span class="n">delay_hours</span><span class="p">)</span>
        <span class="n">schedule_name</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">drip-</span><span class="si">{</span><span class="n">safe_email</span><span class="si">}</span><span class="s">-step</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span>

        <span class="n">payload</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">({</span>
            <span class="sh">"</span><span class="s">task</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">task_send_email</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">args</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span><span class="sh">"</span><span class="s">to</span><span class="sh">"</span><span class="p">:</span> <span class="n">email</span><span class="p">,</span> <span class="sh">"</span><span class="s">subject</span><span class="sh">"</span><span class="p">:</span> <span class="n">subject</span><span class="p">,</span> <span class="sh">"</span><span class="s">body_html</span><span class="sh">"</span><span class="p">:</span> <span class="n">body_html</span><span class="p">},</span>
        <span class="p">})</span>

        <span class="k">try</span><span class="p">:</span>
            <span class="n">scheduler</span><span class="p">.</span><span class="nf">create_schedule</span><span class="p">(</span>
                <span class="n">Name</span><span class="o">=</span><span class="n">schedule_name</span><span class="p">,</span>
                <span class="n">GroupName</span><span class="o">=</span><span class="sh">"</span><span class="s">fluxo-drip</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">ScheduleExpression</span><span class="o">=</span><span class="sa">f</span><span class="sh">"</span><span class="s">at(</span><span class="si">{</span><span class="n">fire_at</span><span class="p">.</span><span class="n">strftime</span><span class="p">(</span><span class="sh">'</span><span class="s">%Y-%m-%dT%H</span><span class="si">:</span><span class="o">%</span><span class="n">M</span><span class="si">:</span><span class="o">%</span><span class="n">S</span><span class="sh">'</span><span class="s">)</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">ScheduleExpressionTimezone</span><span class="o">=</span><span class="sh">"</span><span class="s">UTC</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">Target</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">Arn</span><span class="sh">"</span><span class="p">:</span> <span class="n">worker_arn</span><span class="p">,</span> <span class="sh">"</span><span class="s">RoleArn</span><span class="sh">"</span><span class="p">:</span> <span class="n">scheduler_role_arn</span><span class="p">,</span> <span class="sh">"</span><span class="s">Input</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">},</span>
                <span class="n">ActionAfterCompletion</span><span class="o">=</span><span class="sh">"</span><span class="s">DELETE</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">FlexibleTimeWindow</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">Mode</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">FLEXIBLE</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">MaximumWindowInMinutes</span><span class="sh">"</span><span class="p">:</span> <span class="mi">15</span><span class="p">},</span>
            <span class="p">)</span>
        <span class="k">except</span> <span class="n">scheduler</span><span class="p">.</span><span class="n">exceptions</span><span class="p">.</span><span class="n">ConflictException</span><span class="p">:</span>
            <span class="c1"># Same contact re-enrolled in the sequence — overwrite the existing step
</span>            <span class="n">scheduler</span><span class="p">.</span><span class="nf">update_schedule</span><span class="p">(</span>
                <span class="n">Name</span><span class="o">=</span><span class="n">schedule_name</span><span class="p">,</span> <span class="n">GroupName</span><span class="o">=</span><span class="sh">"</span><span class="s">fluxo-drip</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">ScheduleExpression</span><span class="o">=</span><span class="sa">f</span><span class="sh">"</span><span class="s">at(</span><span class="si">{</span><span class="n">fire_at</span><span class="p">.</span><span class="n">strftime</span><span class="p">(</span><span class="sh">'</span><span class="s">%Y-%m-%dT%H</span><span class="si">:</span><span class="o">%</span><span class="n">M</span><span class="si">:</span><span class="o">%</span><span class="n">S</span><span class="sh">'</span><span class="s">)</span><span class="si">}</span><span class="s">)</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">ScheduleExpressionTimezone</span><span class="o">=</span><span class="sh">"</span><span class="s">UTC</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">Target</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">Arn</span><span class="sh">"</span><span class="p">:</span> <span class="n">worker_arn</span><span class="p">,</span> <span class="sh">"</span><span class="s">RoleArn</span><span class="sh">"</span><span class="p">:</span> <span class="n">scheduler_role_arn</span><span class="p">,</span> <span class="sh">"</span><span class="s">Input</span><span class="sh">"</span><span class="p">:</span> <span class="n">payload</span><span class="p">},</span>
                <span class="n">ActionAfterCompletion</span><span class="o">=</span><span class="sh">"</span><span class="s">DELETE</span><span class="sh">"</span><span class="p">,</span>
                <span class="n">FlexibleTimeWindow</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">Mode</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">FLEXIBLE</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">MaximumWindowInMinutes</span><span class="sh">"</span><span class="p">:</span> <span class="mi">15</span><span class="p">},</span>
            <span class="p">)</span>
</code></pre></div></div> <p>Three details make this hold up:</p> <ul> <li><strong>Deterministic naming</strong> (<code class="language-plaintext highlighter-rouge">drip-{email}-step{n}</code>) means re-enrolling a contact overwrites their existing pending step instead of stacking duplicate sends — the <code class="language-plaintext highlighter-rouge">ConflictException</code> branch handles that.</li> <li><strong><code class="language-plaintext highlighter-rouge">ActionAfterCompletion: DELETE</code></strong> means schedules don’t accumulate forever. Without it, every contact who ever went through onboarding would leave permanent EventBridge Scheduler entries behind.</li> <li><strong><code class="language-plaintext highlighter-rouge">FlexibleTimeWindow</code></strong> gives EventBridge up to 15 minutes of slack to fire the target, which spreads load instead of every schedule racing to fire at the exact same second.</li> </ul> <p>This pairs with the suppression-list logic from <a href="/blog/2026/ses-bounce-complaint-handling/">Part 3 of the SES series</a> — a schedule firing doesn’t mean the email should still go out; <code class="language-plaintext highlighter-rouge">task_send_email</code> still needs to check the recipient isn’t suppressed before it sends.</p> <p>There’s a graceful fallback, too: if <code class="language-plaintext highlighter-rouge">WORKER_LAMBDA_ARN</code> or <code class="language-plaintext highlighter-rouge">SCHEDULER_ROLE_ARN</code> aren’t set (local dev, or a misconfigured environment), it just sends the first email in the sequence immediately instead of silently dropping the rest.</p> <hr/> <h2 id="replacing-arq--redis-with-sqs">Replacing arq + Redis with SQS</h2> <p>The rest of the task queue — publish jobs, trend processing, engagement checks — moved to SQS, which is simpler than the delayed-send case because these tasks run as soon as possible rather than at a scheduled time:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">TaskQueue</span><span class="pi">:</span>
  <span class="na">Type</span><span class="pi">:</span> <span class="s">AWS::SQS::Queue</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">QueueName</span><span class="pi">:</span> <span class="s">marketing-tasks</span>
    <span class="na">VisibilityTimeout</span><span class="pi">:</span> <span class="m">360</span>
    <span class="na">RedrivePolicy</span><span class="pi">:</span>
      <span class="na">deadLetterTargetArn</span><span class="pi">:</span> <span class="kt">!GetAtt</span> <span class="s">TaskDlq.Arn</span>
      <span class="na">maxReceiveCount</span><span class="pi">:</span> <span class="m">3</span>

<span class="na">WorkerFunction</span><span class="pi">:</span>
  <span class="na">Properties</span><span class="pi">:</span>
    <span class="na">Events</span><span class="pi">:</span>
      <span class="na">SqsTrigger</span><span class="pi">:</span>
        <span class="na">Type</span><span class="pi">:</span> <span class="s">SQS</span>
        <span class="na">Properties</span><span class="pi">:</span>
          <span class="na">Queue</span><span class="pi">:</span> <span class="kt">!GetAtt</span> <span class="s">TaskQueue.Arn</span>
          <span class="na">BatchSize</span><span class="pi">:</span> <span class="m">1</span>
          <span class="na">FunctionResponseTypes</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">ReportBatchItemFailures</span><span class="pi">]</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">BatchSize: 1</code> keeps things simple — one message, one task, one success/failure outcome, rather than reasoning about partial batch failures. A message that fails 3 times lands in <code class="language-plaintext highlighter-rouge">TaskDlq</code> instead of retrying forever:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">worker_handler</span><span class="p">(</span><span class="n">event</span><span class="p">:</span> <span class="nb">dict</span><span class="p">,</span> <span class="n">context</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="nf">_load_secrets</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">record</span> <span class="ow">in</span> <span class="n">event</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">Records</span><span class="sh">"</span><span class="p">,</span> <span class="p">[]):</span>
        <span class="n">body</span> <span class="o">=</span> <span class="n">json</span><span class="p">.</span><span class="nf">loads</span><span class="p">(</span><span class="n">record</span><span class="p">[</span><span class="sh">"</span><span class="s">body</span><span class="sh">"</span><span class="p">])</span>
        <span class="n">task</span><span class="p">,</span> <span class="n">args</span> <span class="o">=</span> <span class="n">body</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">task</span><span class="sh">"</span><span class="p">),</span> <span class="n">body</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">args</span><span class="sh">"</span><span class="p">,</span> <span class="p">{})</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">handler_fn</span> <span class="o">=</span> <span class="nf">globals</span><span class="p">()[</span><span class="n">_TASK_HANDLERS</span><span class="p">[</span><span class="n">task</span><span class="p">]]</span>
            <span class="n">asyncio</span><span class="p">.</span><span class="nf">get_event_loop</span><span class="p">().</span><span class="nf">run_until_complete</span><span class="p">(</span><span class="nf">handler_fn</span><span class="p">(</span><span class="o">**</span><span class="n">args</span><span class="p">))</span>
        <span class="k">except</span> <span class="nb">Exception</span><span class="p">:</span>
            <span class="n">logger</span><span class="p">.</span><span class="nf">exception</span><span class="p">(</span><span class="sh">"</span><span class="s">[worker] Task %s failed</span><span class="sh">"</span><span class="p">,</span> <span class="n">task</span><span class="p">)</span>
            <span class="k">raise</span>  <span class="c1"># re-raise so SQS returns the message for retry
</span>    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">batchItemFailures</span><span class="sh">"</span><span class="p">:</span> <span class="p">[],</span> <span class="sh">"</span><span class="s">results</span><span class="sh">"</span><span class="p">:</span> <span class="n">results</span><span class="p">}</span>
</code></pre></div></div> <p>Letting the exception propagate is what tells SQS to retry the message — swallowing it here would silently drop failed tasks instead of sending them to the DLQ.</p> <hr/> <h2 id="storage-had-to-split-in-two">Storage Had to Split in Two</h2> <p>Lambda’s filesystem is <code class="language-plaintext highlighter-rouge">/tmp</code>, capped, and gone between cold starts — the app’s SQLite files and on-disk brand assets couldn’t stay as local files. They split by access pattern rather than moving everything to one place:</p> <p><strong>Binary blobs</strong> (brand logos, generated visuals) → S3, with a small shim that keeps the exact same function signatures whether running locally or in Lambda:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">IS_LAMBDA</span> <span class="o">=</span> <span class="nf">bool</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">AWS_LAMBDA_FUNCTION_NAME</span><span class="sh">"</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">download_asset</span><span class="p">(</span><span class="n">tenant_id</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">filename</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Path</span> <span class="o">|</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">if</span> <span class="ow">not</span> <span class="n">IS_LAMBDA</span> <span class="ow">or</span> <span class="ow">not</span> <span class="n">S3_BUCKET</span><span class="p">:</span>
        <span class="n">local</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">TENANT_ASSETS_DIR</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">./data/assets</span><span class="sh">"</span><span class="p">))</span> <span class="o">/</span> <span class="n">tenant_id</span> <span class="o">/</span> <span class="n">filename</span>
        <span class="k">return</span> <span class="n">local</span> <span class="k">if</span> <span class="n">local</span><span class="p">.</span><span class="nf">exists</span><span class="p">()</span> <span class="k">else</span> <span class="bp">None</span>

    <span class="n">local</span> <span class="o">=</span> <span class="nc">Path</span><span class="p">(</span><span class="sh">"</span><span class="s">/tmp</span><span class="sh">"</span><span class="p">)</span> <span class="o">/</span> <span class="sh">"</span><span class="s">assets</span><span class="sh">"</span> <span class="o">/</span> <span class="n">tenant_id</span> <span class="o">/</span> <span class="n">filename</span>
    <span class="n">local</span><span class="p">.</span><span class="n">parent</span><span class="p">.</span><span class="nf">mkdir</span><span class="p">(</span><span class="n">parents</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">exist_ok</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="k">try</span><span class="p">:</span>
        <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">"</span><span class="s">s3</span><span class="sh">"</span><span class="p">).</span><span class="nf">download_file</span><span class="p">(</span><span class="n">S3_BUCKET</span><span class="p">,</span> <span class="sa">f</span><span class="sh">"</span><span class="s">assets/</span><span class="si">{</span><span class="n">tenant_id</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">filename</span><span class="si">}</span><span class="sh">"</span><span class="p">,</span> <span class="nf">str</span><span class="p">(</span><span class="n">local</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">local</span>
    <span class="k">except</span> <span class="nb">Exception</span><span class="p">:</span>
        <span class="k">return</span> <span class="bp">None</span>
</code></pre></div></div> <p>Every call site (<code class="language-plaintext highlighter-rouge">social_engine/tenant_assets.py</code>) stayed a one-line change — swap a direct filesystem check for a call into this module — because the branching lives in one place.</p> <p><strong>Relational data</strong> moved off the MySQL container to a managed Postgres instance, accessed through <code class="language-plaintext highlighter-rouge">asyncpg</code> instead of the old <code class="language-plaintext highlighter-rouge">aiosqlite</code>/MySQL calls. The connection pool is created once per Lambda execution environment and reused across warm invocations, guarded by a lock so concurrent cold-start requests don’t race to create two pools:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_pool</span> <span class="o">=</span> <span class="bp">None</span>
<span class="n">_pool_lock</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nc">Lock</span><span class="p">()</span>

<span class="k">async</span> <span class="k">def</span> <span class="nf">get_pool</span><span class="p">():</span>
    <span class="k">global</span> <span class="n">_pool</span>
    <span class="k">if</span> <span class="n">_pool</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">_pool</span>
    <span class="k">async</span> <span class="k">with</span> <span class="n">_pool_lock</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">_pool</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span> <span class="n">_pool</span>
        <span class="n">_pool</span> <span class="o">=</span> <span class="k">await</span> <span class="n">asyncpg</span><span class="p">.</span><span class="nf">create_pool</span><span class="p">(</span>
            <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">[</span><span class="sh">"</span><span class="s">DATABASE_URL</span><span class="sh">"</span><span class="p">],</span> <span class="n">min_size</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">max_size</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
            <span class="n">command_timeout</span><span class="o">=</span><span class="mi">30</span><span class="p">,</span> <span class="n">ssl</span><span class="o">=</span><span class="sh">"</span><span class="s">require</span><span class="sh">"</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="k">return</span> <span class="n">_pool</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">min_size=1, max_size=5</code> keeps this cheap per Lambda instance — with <code class="language-plaintext highlighter-rouge">ReservedConcurrentExecutions: 5</code> on the API function, worst case is 5 concurrent Lambda instances each holding up to 5 connections, which is a bound you can actually reason about against your database’s connection limit.</p> <p>Migrating existing data was a one-time script (<code class="language-plaintext highlighter-rouge">scripts/migrate_data_to_s3.py</code>), run from the still-live EC2 box before cutover — walks the old <code class="language-plaintext highlighter-rouge">data/</code> directory, uploads SQLite files, tenant assets, and generated visuals into their new S3 prefixes, and skips anything already present so it’s safe to re-run.</p> <hr/> <h2 id="secrets-without-a-env-file">Secrets Without a .env File</h2> <p>The old stack read a <code class="language-plaintext highlighter-rouge">.env</code> file mounted into the container. Lambda functions don’t have a persistent filesystem to mount a secrets file into, so secrets moved to SSM Parameter Store and get pulled in bulk on cold start:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">_secrets_loaded</span> <span class="o">=</span> <span class="bp">False</span>

<span class="k">def</span> <span class="nf">_load_secrets</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="k">global</span> <span class="n">_secrets_loaded</span>
    <span class="k">if</span> <span class="n">_secrets_loaded</span><span class="p">:</span>
        <span class="k">return</span>
    <span class="n">ssm</span> <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">"</span><span class="s">ssm</span><span class="sh">"</span><span class="p">,</span> <span class="n">region_name</span><span class="o">=</span><span class="n">os</span><span class="p">.</span><span class="nf">getenv</span><span class="p">(</span><span class="sh">"</span><span class="s">AWS_REGION</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">ca-central-1</span><span class="sh">"</span><span class="p">))</span>
    <span class="n">paginator</span> <span class="o">=</span> <span class="n">ssm</span><span class="p">.</span><span class="nf">get_paginator</span><span class="p">(</span><span class="sh">"</span><span class="s">get_parameters_by_path</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">page</span> <span class="ow">in</span> <span class="n">paginator</span><span class="p">.</span><span class="nf">paginate</span><span class="p">(</span><span class="n">Path</span><span class="o">=</span><span class="sh">"</span><span class="s">/fluxo/marketing</span><span class="sh">"</span><span class="p">,</span> <span class="n">WithDecryption</span><span class="o">=</span><span class="bp">True</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">param</span> <span class="ow">in</span> <span class="n">page</span><span class="p">[</span><span class="sh">"</span><span class="s">Parameters</span><span class="sh">"</span><span class="p">]:</span>
            <span class="n">key</span> <span class="o">=</span> <span class="n">param</span><span class="p">[</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">].</span><span class="nf">split</span><span class="p">(</span><span class="sh">"</span><span class="s">/</span><span class="sh">"</span><span class="p">)[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
            <span class="n">os</span><span class="p">.</span><span class="n">environ</span><span class="p">.</span><span class="nf">setdefault</span><span class="p">(</span><span class="n">key</span><span class="p">,</span> <span class="n">param</span><span class="p">[</span><span class="sh">"</span><span class="s">Value</span><span class="sh">"</span><span class="p">])</span>
    <span class="n">_secrets_loaded</span> <span class="o">=</span> <span class="bp">True</span>
</code></pre></div></div> <p>The point of <code class="language-plaintext highlighter-rouge">setdefault</code> and the <code class="language-plaintext highlighter-rouge">_secrets_loaded</code> module-level flag together: every downstream <code class="language-plaintext highlighter-rouge">os.getenv(...)</code> call in the rest of the codebase didn’t need to change at all, and a warm Lambda instance doesn’t re-fetch from SSM on every invocation — only on cold start.</p> <hr/> <h2 id="the-cutover">The Cutover</h2> <p>In order:</p> <ol> <li>Run <code class="language-plaintext highlighter-rouge">migrate_data_to_s3.py</code> from the live EC2 box to backfill S3 with existing assets and databases.</li> <li>Load secrets into SSM under <code class="language-plaintext highlighter-rouge">/fluxo/marketing/*</code>.</li> <li><code class="language-plaintext highlighter-rouge">sam build &amp;&amp; sam deploy --guided</code> — builds the image, pushes to ECR, stands up the three functions, the SQS queue + DLQ, and the EventBridge Scheduler group.</li> <li>Point <code class="language-plaintext highlighter-rouge">bookit</code>’s <code class="language-plaintext highlighter-rouge">MARKETING_API_URL</code> at the new API Gateway URL from the stack output.</li> <li>Stop the old <code class="language-plaintext highlighter-rouge">marketing-api</code>, <code class="language-plaintext highlighter-rouge">arq-worker</code>, and <code class="language-plaintext highlighter-rouge">redis</code> containers.</li> </ol> <p>Each function gets one shared IAM role (<code class="language-plaintext highlighter-rouge">MarketingLambdaRole</code>) rather than three separate ones — scoped to the S3 bucket, the SSM path prefix, the SQS queue, and <code class="language-plaintext highlighter-rouge">scheduler:CreateSchedule</code>/<code class="language-plaintext highlighter-rouge">UpdateSchedule</code>/<code class="language-plaintext highlighter-rouge">DeleteSchedule</code>. One wrinkle in <code class="language-plaintext highlighter-rouge">template.yaml</code>: the scheduler’s invoke role needs to reference the worker Lambda’s ARN, and the Lambda’s own role needs <code class="language-plaintext highlighter-rouge">iam:PassRole</code> on the scheduler’s invoke role — a naive <code class="language-plaintext highlighter-rouge">!GetAtt</code> on either side creates a circular dependency between the two IAM resources. The fix was using <code class="language-plaintext highlighter-rouge">!Sub</code> with the hardcoded, known function/role names instead of <code class="language-plaintext highlighter-rouge">!GetAtt</code>, since SAM can resolve a <code class="language-plaintext highlighter-rouge">Sub</code> string without needing the other resource to exist first.</p> <hr/> <h2 id="what-id-flag-before-you-copy-this">What I’d Flag Before You Copy This</h2> <ul> <li><strong>Cold starts matter more here than for a typical CRUD API.</strong> The container image bundles Pillow and other native deps, so first-invocation latency after a period of no traffic is real. Fine for background jobs; worth measuring if you route interactive traffic through the same <code class="language-plaintext highlighter-rouge">ApiFunction</code>.</li> <li><strong>API Gateway’s 29–30s ceiling is a hard constraint</strong>, not a Lambda one — if a request-path operation might run long, it needs to hand off to <code class="language-plaintext highlighter-rouge">WorkerFunction</code> via SQS rather than trying to raise the API function’s timeout past what API Gateway will tolerate.</li> <li><strong>EventBridge Scheduler’s one-time schedules are billed and limited per-account</strong> — fine at the volume a single-tenant nurture sequence produces, but if you’re enrolling thousands of contacts a day into multi-step sequences, model out the schedule count before assuming this scales the same way a queue would.</li> <li><strong><code class="language-plaintext highlighter-rouge">ReservedConcurrentExecutions</code> is a blunt tool.</strong> It protects downstream rate-limited APIs, but it also means requests queue or throttle once you hit the cap — set it from the constraint you’re protecting (the social platform’s rate limit, the database’s connection limit), not an arbitrary number.</li> </ul> <p>The net effect: <code class="language-plaintext highlighter-rouge">marketingPy</code> now costs whatever it’s actually invoked, instead of a flat monthly charge for a box that’s mostly idle — and the three-functions-one-image pattern means there’s still only one thing to build and deploy.</p>]]></content><author><name>your_name</name></author><category term="engineering"/><category term="architecture"/><category term="aws"/><category term="lambda"/><category term="serverless-arch"/><category term="python"/><category term="fastapi"/><category term="sqs"/><category term="eventbridge"/><category term="multi-tenant"/><summary type="html"><![CDATA[How a 4-container Docker Compose stack (FastAPI + APScheduler + arq worker + Redis) became three purpose-built Lambda functions behind API Gateway, SQS, and EventBridge Scheduler — and the trick for delayed, self-cleaning email drip schedules.]]></summary></entry><entry><title type="html">Bridging the Gap: Connecting React (Azure SWA) to a Secured .NET 8 API</title><link href="https://crystalhansen.ca/blog/2026/connecting-react-swa-dontnet-api-azure/" rel="alternate" type="text/html" title="Bridging the Gap: Connecting React (Azure SWA) to a Secured .NET 8 API"/><published>2026-06-29T00:00:00+00:00</published><updated>2026-06-29T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/connecting-react-swa-dontnet-api-azure</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/connecting-react-swa-dontnet-api-azure/"><![CDATA[<p>Deploying a modern full-stack web application into production requires a clean separation of concerns. In <a href="/blog/2026/building-gatekeeper-rbac-azure/">Part 1</a>, we hardened a .NET 8 Minimal API backend using Role-Based Access Control (RBAC) and deployed it to Azure App Service via GitHub Actions.</p> <p>Today, we take the architecture full-stack by introducing a highly responsive React user interface styled with Material-UI, hosted globally via <strong>Azure Static Web Apps (SWA)</strong>, and bound directly to our live Azure cloud engine.</p> <blockquote> <p><strong>This is Part 2 of a two-part series.</strong> Start with Part 1 — <a href="/blog/2026/building-gatekeeper-rbac-azure/">Hardening .NET Minimal APIs: Shifting from Local Dev to Azure-backed RBAC</a> — to follow the backend setup from scratch.</p> </blockquote> <hr/> <h2 id="the-full-stack-demo">The Full-Stack Demo</h2> <p>The integrated environment is live and communicating across cloud domains. Explore the running build or inspect the split-repository source pipelines below:</p> <div class="text-center" style="margin: 25px 0; display: flex; justify-content: center; gap: 15px; flex-wrap: wrap;"> <a href="https://proud-moss-0cfd07110.7.azurestaticapps.net" alt="GatekeeperAuth" title="GatekeeperAuth" class="btn btn-sm z-depth-1" target="_blank" rel="noopener noreferrer" style="background-color: #0078d4; color: white; padding: 10px 15px;"> <i class="fa-solid fa-rocket"></i> Launch Live App </a> <a href="https://github.com/cryshansen/Gatekeeper-ui" class="btn btn-sm z-depth-1" target="_blank" rel="noopener noreferrer" style="padding: 10px 15px;"> <i class="fa-brands fa-github"></i> Frontend Repository </a> <a href="https://github.com/cryshansen/GateKeeperAuth" class="btn btn-sm z-depth-1" target="_blank" rel="noopener noreferrer" style="padding: 10px 15px;"> <i class="fa-solid fa-code-branch"></i> Backend Repository </a> </div> <hr/> <h2 id="distributed-cloud-architecture">Distributed Cloud Architecture</h2> <p>To maximize efficiency and eliminate unnecessary server overhead, we decoupled our client layers from our logic compute blocks. The system operates across two isolated Azure environments:</p> <pre><code class="language-mermaid">graph LR
    subgraph Client Space (CDN Edge)
        A[React SWA UI] --&gt;|HTTPS Requests + Credentials| B(.NET 8 App Service)
    end
    subgraph Compute Space (Azure Core)
        B --&gt;|Validates Session| C[Secure Endpoints]
        B -.-&gt;|SameSite=None Cookie| A
    end
    style A fill:#61dafb,stroke:#333,stroke-width:2px,color:#000
    style B fill:#512bd4,stroke:#333,stroke-width:2px,color:#fff
</code></pre> <hr/> <h2 id="the-cross-domain-cookie-challenge">The Cross-Domain Cookie Challenge</h2> <p>Because our frontend and backend run on distinct Azure service structures, they communicate across entirely separate domains. By default, standard security contexts block authentication tokens from tracking across unmapped endpoints.</p> <p>To overcome this without resorting to unsecure local storage tokens, we shifted our .NET architecture to enforce explicit trust guidelines:</p> <p>SameSiteMode.None + forced SSL to permit cross-domain browser cookie persistence.</p> <p>AllowCredentials() within CORS to authorize Axios asynchronous calls to inherit background session authorization rules natively.</p> <ul> <li> <ol> <li>Updating the .NET Security Gates To safely handshake with our remote React build, we modified Program.cs to handle incoming cross-origin headers gracefully:</li> </ol> </li> </ul> <pre><code class="language-C#">builder.Services.AddCors(options =&gt;
{
    options.AddPolicy("ReactAppPolicy", policy =&gt;
    {
        policy.WithOrigins("[https://your-react-app-name.azurestaticapps.net](https://your-react-app-name.azurestaticapps.net)")
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials(); // Essential for cross-origin cookie passing
    });
});

builder.Services.AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)
    .AddCookie(options =&gt;
    {
        options.Cookie.Name = "GatekeeperSession";
        options.Cookie.HttpOnly = true;
        options.Cookie.SameSite = SameSiteMode.None; // Required for cross-domain
        options.Cookie.SecurePolicy = CookieSecurePolicy.Always; // Required for SameSite=None
    });
</code></pre> <ul> <li> <ol> <li>Injecting Compilation Variables via GitHub Actions Because static web environments compile application scripts completely down to minimized browser assets prior to runtime distribution, standard server-side configuration mapping is unreachable. Instead, we injected our backend variables directly into our delivery pipelines via encrypted GitHub Repository Secrets.</li> </ol> </li> </ul> <p>Our automated workflow file (.github/workflows/azure-static-web-apps.yml) intercepts these fields smoothly during build execution:</p> <pre><code class="language-YAML">      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: $
          action: "upload"
          app_location: "/"
          output_location: "build"
        env:
          REACT_APP_API_BASE: $
</code></pre> <p>Now, when our Axios wrapper calls process.env.REACT_APP_API_BASE, it hooks directly into our cloud architecture instantly.</p> <h2 id="key-takeaways">Key Takeaways</h2> <p>By isolating our stateless UI layers from our analytical engines, we achieve:</p> <ul> <li> <p>Near-Zero Hosting Costs: Running client distributions globally over free CDN edges.</p> </li> <li> <p>Hardened Security: Retaining stateful cookie boundaries while preventing manual script intervention or security leaks.</p> </li> <li> <p>Continuous Delivery: Automated pipeline testing maps, builds, and distributes downstream adjustments every single time code drops onto a tracked repository.</p> </li> </ul> <hr/> <h2 id="the-full-series">The Full Series</h2> <blockquote> <p><strong>Part 1:</strong> <a href="/blog/2026/building-gatekeeper-rbac-azure/">Hardening .NET Minimal APIs: Shifting from Local Dev to Azure-backed RBAC</a> — RBAC middleware, cookie auth, and GitHub Actions deployment to Azure App Service.</p> <p><strong>Part 2:</strong> You are here — React on Azure SWA, CORS configuration, and cross-domain cookie authentication.</p> </blockquote>]]></content><author><name></name></author><category term="engineering"/><category term="devops-posts"/><category term="csharp"/><category term="dotnet"/><category term="azure"/><category term="react"/><category term="swa"/><category term="architecture"/><category term="cors"/><category term="design-in-code"/><summary type="html"><![CDATA[A architectural breakdown of resolving Cross-Origin (CORS) cookie authentication constraints between Azure Static Web Apps and Azure App Service.]]></summary></entry><entry><title type="html">Hardening .NET Minimal APIs: Shifting from Local Dev to Azure-backed RBAC</title><link href="https://crystalhansen.ca/blog/2026/building-gatekeeper-rbac-azure/" rel="alternate" type="text/html" title="Hardening .NET Minimal APIs: Shifting from Local Dev to Azure-backed RBAC"/><published>2026-06-28T12:00:00+00:00</published><updated>2026-06-28T12:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/building-gatekeeper-rbac-azure</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/building-gatekeeper-rbac-azure/"><![CDATA[<p>Building a local proof-of-concept is easy, but bringing enterprise-grade security to the cloud requires intentional architecture. In this write-up, we walk through scaling a .NET 8 Minimal API authentication engine—codenamed <strong>GatekeeperAuth</strong>—from a local Mac sandbox to an automated, multi-role deployment running securely on Azure App Service.</p> <blockquote> <p><strong>This is Part 1 of a two-part series.</strong> Part 2 — <a href="/blog/2026/connecting-react-swa-dontnet-api-azure/">Bridging the Gap: Connecting React (Azure SWA) to a Secured .NET 8 API</a> — covers wiring a React frontend hosted on Azure Static Web Apps to this secured backend.</p> </blockquote> <div class="text-center" style="margin: 20px 0;"> <a href="https://gatekeeperauth-gsevezf3d8awc8fj.centralus-01.azurewebsites.net/api/secure/dashboard" class="btn btn-sm z-depth-1" target="_blank" rel="noopener noreferrer" style="background-color: #0078d4; color: white;"> <i class="fa-solid fa-cloud"></i> View Live Azure API </a> <a href="https://github.com/cryshansen/GateKeeperAuth" class="btn btn-sm z-depth-1" target="_blank" rel="noopener noreferrer"> <i class="fa-brands fa-github"></i> View Repository </a> </div> <h2 id="the-architectural-blueprint">The Architectural Blueprint</h2> <p>Moving code out of a local sandbox means structuring authorization logic to handle granular permissions. Rather than using an all-or-nothing authentication gate, we implemented <strong>Role-Based Access Control (RBAC)</strong> via Claims-Based Authorization directly inside the middleware pipeline.</p> <pre><code class="language-mermaid">graph TD
    A[Client Request] --&gt; B[Authentication Middleware]
    B --&gt;|Cookie Valid?| C[Authorization Layer]
    B --&gt;|No Cookie| D[401 Unauthorized]
    C --&gt;|Has Role: Practitioner| E[/api/secure/dashboard]
    C --&gt;|Has Role: Admin| F[/api/secure/delete-record]
    C --&gt;|Role Insufficient| G[403 Forbidden]
</code></pre> <h3 id="1-the-net-8-rbac-implementation">1. The .NET 8 RBAC Implementation</h3> <p>By utilizing standard cookie authentication events, we intercepted unauthorized access explicitly to return native API status codes (401 Unauthorized and 403 Forbidden) instead of automated MVC webpage redirects.</p> <p>Here is how the core pipeline is configured in Program.cs:</p> <pre><code class="language-Bash">C#
builder.Services.AddAuthentication(CookieAuthenticationDefaults.AuthenticationScheme)
    .AddCookie(options =&gt;
    {
        options.Cookie.Name = "GatekeeperSession";
        options.Cookie.HttpOnly = true;
        options.Cookie.SameSite = SameSiteMode.Strict;
        options.Cookie.SecurePolicy = CookieSecurePolicy.Always; // Forced SSL in Cloud
        options.ExpireTimeSpan = TimeSpan.FromMinutes(20);

        // Return 401 instead of redirecting to a login page
        options.Events.OnRedirectToLogin = context =&gt; {
            context.Response.StatusCode = StatusCodes.Status401Unauthorized;
            return Task.CompletedTask;
        };

        // Return 403 when identities are known but permissions are insufficient
        options.Events.OnRedirectToAccessDenied = context =&gt; {
            context.Response.StatusCode = StatusCodes.Status403Forbidden;
            return Task.CompletedTask;
        };
    });
</code></pre> <h4 id="guarding-the-endpoints">Guarding the Endpoints</h4> <p>With the authorization middleware injected, declaring standard endpoints vs. administrative-only destinations becomes perfectly clean using the .RequireAuthorization() policy modifiers:</p> <pre><code class="language-Bash">C#
// Standard Protected Endpoint: Accessible by any logged-in user
app.MapGet("/api/secure/dashboard", () =&gt; Results.Ok("Welcome back!"))
   .RequireAuthorization();

// Destructive RBAC Endpoint: Strictly locked down to Administrators
app.MapDelete("/api/secure/delete-record/{id}", (int id) =&gt;
   Results.Ok($"Record {id} permanently purged."))
   .RequireAuthorization(policy =&gt; policy.RequireRole("Admin"));
</code></pre> <h3 id="2-automating-the-pipeline-to-azure">2. Automating the Pipeline to Azure</h3> <p>Hardcoding operations via localized CLI commands scales poorly. To achieve a zero-friction workflow, we established a CI/CD Pipeline linking GitHub repositories directly with Azure App Service utilizing an isolated Linux-based container runtime.</p> <h4 id="resolving-local-and-remote-divergences">Resolving Local and Remote Divergences</h4> <p>During automated configuration handshakes, Git repositories can diverge when remote configuration assets (like automated workflow files generated by Azure) match local commits asynchronously.</p> <p>To merge distinct structural timelines without forcing unnecessary workspace rewrites, we can safely reset a locked MERGE_HEAD state and force a non-rebased downstream pull:</p> <pre><code class="language-Bash"># Abort any background-stuck merge blocks
git merge --abort

# Capture remote configuration changes without linear rewrite conflicts
git pull origin main --no-rebase

# Safely push the combined local feature branches
git push origin main
</code></pre> <h3 id="3-the-result">3. The Result</h3> <p>With this architecture implemented:</p> <p>Every standard git push triggers an isolated GitHub Runner to rebuild, test, and host the binary to Azure seamlessly.</p> <p>The authentication context seamlessly isolates roles, shifting from validation gates to explicit claim authorization blocks automatically.</p> <p>The next evolutionary iteration for this module involves wrapping custom IMiddleware pre-request hooks to achieve domain-routed, multi-tenant isolation.</p> <h2 id="try-it-live">Try It Live</h2> <p>The production API is actively running on Azure. You can execute requests directly against the live environment to test the RBAC rules using a tool like <strong>Postman</strong>, <strong>Thunder Client</strong>, or <code class="language-plaintext highlighter-rouge">curl</code>:</p> <ul> <li><strong>Base Live URL:</strong> <code class="language-plaintext highlighter-rouge">https://gatekeeperauth-gsevezf3d8awc8fj.centralus-01.azurewebsites.net</code></li> <li><strong>Test the restriction (GET):</strong> <code class="language-plaintext highlighter-rouge">https://gatekeeperauth-gsevezf3d8awc8fj.centralus-01.azurewebsites.net/api/secure/dashboard</code></li> </ul> <blockquote> <p>💡 <strong>Quick Test:</strong> Fire a POST request to <code class="language-plaintext highlighter-rouge">/api/auth/login</code> using the email <code class="language-plaintext highlighter-rouge">admin@dev.ca</code> and password <code class="language-plaintext highlighter-rouge">Password123</code> to watch the Azure server dynamically append the <code class="language-plaintext highlighter-rouge">Admin</code> claim and drop your encrypted session cookie live!</p> </blockquote> <hr/> <h2 id="whats-next">What’s Next</h2> <p>The backend is production-ready — but an API without a UI is only half the story. In Part 2 we introduce a React frontend hosted on Azure Static Web Apps and wire it directly to this secured API, solving the cross-domain cookie challenge along the way.</p> <blockquote> <p><strong>Continue reading:</strong> <a href="/blog/2026/connecting-react-swa-dontnet-api-azure/">Bridging the Gap: Connecting React (Azure SWA) to a Secured .NET 8 API</a></p> </blockquote>]]></content><author><name></name></author><category term="engineering"/><category term="devops-posts"/><category term="csharp"/><category term="dotnet"/><category term="azure"/><category term="rbac"/><category term="security"/><category term="github-actions"/><summary type="html"><![CDATA[A practical deep-dive into configuring Role-Based Access Control (RBAC) in .NET 8 Minimal APIs and automating production cloud deployments via GitHub Actions.]]></summary></entry><entry><title type="html">Google Calendar API Bidirectionally with Full Calendar in a Multi-Tenant Booking Engines</title><link href="https://crystalhansen.ca/blog/2026/google-calendar-and-full-calendar-bidirection/" rel="alternate" type="text/html" title="Google Calendar API Bidirectionally with Full Calendar in a Multi-Tenant Booking Engines"/><published>2026-05-25T00:00:00+00:00</published><updated>2026-05-25T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/google-calendar-and-full-calendar-bidirection</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/google-calendar-and-full-calendar-bidirection/"><![CDATA[<h1 id="setting-up-google-calendar-api-bidirectionally-with-full-calendar-in-a-multi-tenant-booking-engines">Setting Up Google Calendar API Bidirectionally with Full Calendar in a Multi-Tenant Booking Engines</h1> <p>This article is the third in a three-part series on building architectural booking syncs:</p> <ol> <li><a href="/blog/2026/setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines/">Part 1: Google Cloud Infrastructure &amp; OAuth Configuration</a></li> <li><a href="/blog/2026/restrict-multi-tenant-bookings-and-google-accounts/">Part 2: Managing Tenant Isolation &amp; Shared Team Calendars</a></li> <li>[Part 3: Bidirectional Client Syncing with FullCalendar &amp; Java] (This Post)</li> </ol> <p>When connecting Google Calendar API to a client-side interface like FullCalendar via a Java backend (Spring Boot, etc.) for a read/write bidirectional sync, choosing the right scope depends on your application’s architecture.For creating, reading, updating, and deleting events without over-provisioning your permissions, use the following guidelines:</p> <h2 id="the-recommended-scope">The Recommended Scope</h2> <pre><code class="language-Plaintext"> https://www.googleapis.com/auth/calendar.events
</code></pre> <h3 id="why-this-is-the-best-choice">Why this is the best choice:</h3> <ul> <li>Capabilities: It grants full read/write permission to all events on all calendars the authenticated user has write-access to. This means your Java backend can run events.insert(), events.update(), events.patch(), and events.delete().</li> <li>Security Principle of Least Privilege: It stops short of giving your app global control over the user’s entire calendar account. For instance, your backend cannot delete entire secondary calendars, clear a calendar completely, or alter global sharing permissions (ACLs).</li> <li>User Friction: When your users go through the OAuth consent screen, they see a warning explicitly limited to “View and edit events on all your calendars” rather than the much more alarming “See, edit, share, and permanently delete all the calendars you can access”.</li> </ul> <h3 id="when-to-use-alternate-scopes">When to use Alternate Scopes</h3> <p>Depending on your microservice architecture, you might need to lean narrower or broader.</p> <table> <tbody> <tr> <td>Scope String</td> <td>Use Case for FullCalendar + Java</td> <td>Data Access Level</td> </tr> <tr> <td>…/auth/calendar.events.readonly</td> <td>Read-Only Feed: If your FullCalendar implementation only displays a read-only dashboard of a doctor’s or practitioner’s availability and doesn’t allow adding or updating events.</td> <td>Sensitive</td> </tr> <tr> <td>…/auth/calendar.events</td> <td>Standard Read/Write (Best Choice): Perfect for two-way sync. Moving an event in FullCalendar fires an update to Java, which calls events.patch() on Google. Creating a booking inside your app inserts an event onto Google Calendar.</td> <td>Sensitive</td> </tr> <tr> <td>…/auth/calendar</td> <td>Full Management: Only use this if your multi-tenant engine needs to programmatically create entirely new secondary calendars for a specific team or workspace inside the tenant’s account, or modify calendar colors/metadata.</td> <td>Restricted (Requires heavy verification)</td> </tr> </tbody> </table> <h2 id="implementation-in-java">Implementation in Java</h2> <p>If you are using the official Google APIs Client Library for Java (google-api-services-calendar), do not hardcode the string. Use the native SDK constants provided inside the library to eliminate typos:</p> <pre><code class="language-Java">import com.google.api.services.calendar.CalendarScopes;
import java.util.Collections;
import java.util.List;

public class GoogleCalendarConfig {
    // This resolves directly to: "https://www.googleapis.com/auth/calendar.events"
    private static final List&lt;String&gt; SCOPES =
        Collections.singletonList(CalendarScopes.CALENDAR_EVENTS);
}
</code></pre> <h2 id="architecture-pro-tip-for-fullcalendar-bidirectional-sync">Architecture Pro-Tip for FullCalendar Bidirectional Sync</h2> <p>When connecting FullCalendar to Google Calendar through Java, avoid matching your primary database keys directly with random string increments.</p> <p>When calling Google’s events.insert(), generate a custom string ID in Java (or use your internal event table’s UUID stripped of hyphens) and assign it to the Google Event object via .setId(). This creates a clean 1:1 immutable binding between your local database entity, FullCalendar’s internal id property, and Google Calendar. When a user drags and drops a block on the UI, your React/Vue frontend can immediately send that ID down to Java to execute a fast target update.</p> <h2 id="conclusion">Conclusion</h2> <p>This setup allows your app to 2026-05-24-restrict-multi-tenant-bookings-and-google-accounts</p>]]></content><author><name></name></author><category term="backend"/><category term="architecture"/><category term="google-api"/><category term="oauth2"/><category term="java"/><category term="spring-boot"/><category term="security"/><category term="multi-tenant"/><summary type="html"><![CDATA[Setting Up Google Calendar API Bidirectionally with Full Calendar in a Multi-Tenant Booking Engines]]></summary></entry><entry><title type="html">How to restrict Multi-Tenant Bookings + Google Accounts Separation</title><link href="https://crystalhansen.ca/blog/2026/restrict-multi-tenant-bookings-and-google-accounts/" rel="alternate" type="text/html" title="How to restrict Multi-Tenant Bookings + Google Accounts Separation"/><published>2026-05-24T00:00:00+00:00</published><updated>2026-05-24T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/restrict-multi-tenant-bookings-and-google-accounts</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/restrict-multi-tenant-bookings-and-google-accounts/"><![CDATA[<h1 id="how-to-restrict-multi-tenant-bookings--google-accounts">How to restrict Multi-Tenant Bookings + Google Accounts</h1> <p>This article is the second in a three-part series on building architectural booking syncs:</p> <ol> <li><a href="/blog/2026/setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines/">Part 1: Google Cloud Infrastructure &amp; OAuth Configuration</a></li> <li><strong>Part 2: Managing Tenant Isolation &amp; Shared Team Calendars</strong> (This Post)</li> <li>[Part 3: Bidirectional Client Syncing with FullCalendar &amp; Java] (/blog/2026/google-calendar-and-full-calendar-bidirection/)</li> </ol> <p>When building a multi-tenant booking engine that integrates with Google Calendar, a common challenge is ensuring that each tenant’s bookings are properly isolated while allowing them to connect their Google accounts for calendar syncing. In this article, we will explore how to restrict multi-tenant bookings and manage Google account connections effectively in a Java backend architecture.</p> <h2 id="the-challenge-of-multi-tenancy-and-google-accounts">The Challenge of Multi-Tenancy and Google Accounts</h2> <p>In a multi-tenant booking engine where the need to connect Google accounts to a pre-existing tenancy, you need to separate application tenants from Google accounts. Building from the previous post ()[2026-05-23-setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines]</p> <p>In this scenario, you don’t restrict Google’s permissions directly via OAuth scopes (since Google has no concept of your application’s database boundaries). Instead, you enforce tenant isolation and calendar consistency programmatically inside your Java backend database schema and logic.</p> <p>Here is the exact architecture, database design, and code pattern to achieve this workflow smoothly.</p> <h2 id="1-the-multi-tenant-calendar-schema">1. The Multi-Tenant Calendar Schema</h2> <p>To allow an Admin to authorize a single Google account for the entire workspace while ensuring team members are synced to the exact same calendar name, your database needs to map tenant_id to its respective Google OAuth credentials and a unique google_calendar_id.</p> <p>+——————+ 1 : 1 +————————–+ | tenants | ——————–&gt; | tenant<em>google_configs | +——————+ +————————–+ | * tenant</em>id (PK) | | * tenant_id (FK, PK) | | - company_name | | - encrypted_refresh_token| | | | - google_email | | | | - target_calendar_id | +——————+ +————————–+ | | | 1 : N | 1 : 1 (Contextual) v v +——————+ +————————–+ | users / team | | Google Workspace | +——————+ +————————–+ | * user_id (PK) | | - “Company Schedule” | | - tenant_id (FK) | | (Shared Calendar ID) | | - email | +————————–+ Your data table layout should look like this:</p> <pre><code class="language-SQL">CREATE TABLE tenant_google_configs (
    tenant_id VARCHAR(255) PRIMARY KEY,
    google_email VARCHAR(255) NOT NULL,
    encrypted_refresh_token TEXT NOT NULL, -- Stored securely
    target_calendar_id VARCHAR(255),       -- The specific unique calendar ID generated by Google
    calendar_name VARCHAR(100) DEFAULT 'Company Bookings'
);
</code></pre> <h2 id="2-step-by-step-implementation-flow">2. Step-by-Step Implementation Flow</h2> <h3 id="step-1-admin-authorizes-the-app">Step 1: Admin Authorizes the App</h3> <p>When the Admin connects their Google Account, they go through the standard OAuth loop. Your Java backend intercepts the callback, receives the refresh_token, and stores it under that specific tenant_id.</p> <h3 id="step-2-programmatically-create-the-named-calendar">Step 2: Programmatically Create the “Named” Calendar</h3> <p>Instead of pushing bookings to the Admin’s raw primary calendar (which would mix personal and business data), your Java backend uses the Admin’s token to create a secondary calendar with your standardized application name.</p> <pre><code class="language-Java">import com.google.api.services.calendar.Calendar;
import com.google.api.services.calendar.model.Calendar;

public class GoogleCalendarProvisioner {

    public String provisionSharedTenantCalendar(Calendar googleCalendarService, String companyName) throws IOException {
        // 1. Define the uniform calendar layout
        com.google.api.services.calendar.model.Calendar secondaryCalendar =
            new com.google.api.services.calendar.model.Calendar();

        secondaryCalendar.setSummary(companyName + " Bookings");
        secondaryCalendar.setTimeZone("America/New_York"); // Adjust contextually

        // 2. Execute creation on Google's Infrastructure
        com.google.api.services.calendar.model.Calendar createdCalendar =
            googleCalendarService.calendars().insert(secondaryCalendar).execute();

        // 3. Return the generated ID (looks like: c_xxxxxxxxx@group.calendar.google.com)
        return createdCalendar.getId();
    }
}
</code></pre> <p>You save this returned target_calendar_id directly into your tenant_google_configs table.</p> <h3 id="step-3-pushing-bookings-for-team-members">Step 3: Pushing Bookings for Team Members</h3> <p>When a customer schedules an appointment with Team Member B, Team Member B does not need to connect their personal Google account.</p> <p>Your Java scheduling service looks up the workspace context:</p> <ol> <li> <p>Fetch the encrypted_refresh_token and the target_calendar_id using the current tenant_id.</p> </li> <li> <p>Decrypt the token and build your authorized Google Calendar client.</p> </li> <li> <p>Construct the event, adding the team member’s name or corporate email directly inside the Event Description or Title (e.g., “Massage Therapy By [Name]”).</p> </li> <li> <p>Execute events.insert(targetCalendarId, event).</p> </li> </ol> <p>Because it is targeted directly to the saved target_calendar_id, the booking automatically routes straight into the corporate calendar the Admin established.</p> <h2 id="3-restricting-scopes-safely-for-this-model">3. Restricting Scopes Safely for this Model</h2> <p>Because you are creating and managing a custom secondary calendar on behalf of the business workspace, you can safely use the broader event scope discussed previously:</p> <pre><code class="language-Plaintext">https://www.googleapis.com/auth/calendar.events

</code></pre> <p>This allows your application to execute management actions inside that specific custom secondary calendar box without hitting permission blocks, while keeping the rest of the Admin’s personal schedule completely isolated from your code execution pathways.</p> <h2 id="conclusion">Conclusion</h2> <p>By decoupling your calendar generation from individual team member credentials and linking it straight to the tenant configuration, you eliminate a significant amount of user friction. Team members don’t have to navigate complex individual authentication screens, and administrators maintain total centralized visibility over their company’s availability.</p> <p>When establishing this data relationship in your Java backend, always ensure the refresh_token is strongly encrypted at rest and that your application isolates the specific target_calendar_id on every query. This setup guarantees a secure, reliable, and scalable sync architecture that grows perfectly alongside your multi-tenant ecosystem.</p>]]></content><author><name></name></author><category term="backend"/><category term="architecture"/><category term="google-api"/><category term="oauth2"/><category term="java"/><category term="spring-boot"/><category term="security"/><category term="multi-tenant"/><summary type="html"><![CDATA[How to restrict Multi-Tenant Bookings + Google Accounts]]></summary></entry><entry><title type="html">Setting Up Google Calendar API &amp;amp; OAuth 2.0 for Multi-Tenant Booking Engines</title><link href="https://crystalhansen.ca/blog/2026/setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines/" rel="alternate" type="text/html" title="Setting Up Google Calendar API &amp;amp; OAuth 2.0 for Multi-Tenant Booking Engines"/><published>2026-05-23T00:00:00+00:00</published><updated>2026-05-23T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/setting-up-google-calendar-api-oauth2-for-multi-tenant-booking-engines/"><![CDATA[<h1 id="setting-up-google-calendar-api--oauth-20-for-multi-tenant-booking-engines">Setting Up Google Calendar API &amp; OAuth 2.0 for Multi-Tenant Booking Engines</h1> <p>This guide covers the end-to-end configuration within the Google Cloud Console, detailing how to establish scopes, structure redirect patterns, and configure things cleanly so your Java backend can securely obtain refresh tokens.</p> <p>This article is the first in a three-part series on building architectural booking syncs:</p> <ol> <li><strong>Part 1: Google Cloud Infrastructure &amp; OAuth Configuration</strong> (This Post)</li> <li><a href="/blog/2026/restrict-multi-tenant-bookings-and-google-accounts/">Part 2: Managing Tenant Isolation &amp; Shared Team Calendars</a></li> <li><a href="/blog/2026/google-calendar-and-full-calendar-bidirection/">Part 3: Bidirectional Client Syncing with FullCalendar &amp; Java</a></li> </ol> <p>When building a multi-tenant booking or scheduling engine, syncing appointments directly to a provider’s personal calendar is a core requirement. Relying on simple service accounts won’t cut it here—service accounts are designed for accessing resources owned by your application, not your end-users. For private data, you need explicit user delegation via <strong>OAuth 2.0</strong>.</p> <p>This guide covers the end-to-end configuration within the Google Cloud Console, detailing how to establish scopes, structure redirect patterns, and configure things cleanly so your Java backend can securely obtain and rotate refresh tokens.</p> <hr/> <h2 id="1-the-oauth-20-architecture">1. The OAuth 2.0 Architecture</h2> <p>Before clicking through consoles, let’s look at how the token exchange flow operates between your client frontend, your Java backend, and Google’s authorization servers.</p> <p>+————+ Authorization Request +—————–+ | | ————————————–&gt; | | | | | Google OAuth | | End-User | &lt;————————————– | Server | | (Browser) | User Grants Consent | | | | +—————–+ | | | | | —————————-\ | Auth Code +————+ Redirects with Auth Code \ v ——–&gt; +—————–+ | | | Java Backend | | (Spring Boot) | | | +—————–+ | Exchange Code | &amp; Client Secret v +—————–+ | | | Google Token | | Server | | | +—————–+</p> <hr/> <h2 id="2-google-cloud-project-setup">2. Google Cloud Project Setup</h2> <p>To start interacting with Google Workspace APIs, you need an active Google Cloud Platform (GCP) project.</p> <h3 id="step-1-enable-the-calendar-api">Step 1: Enable the Calendar API</h3> <ol> <li>Open the <a href="https://console.cloud.google.com/">Google Cloud Console</a>.</li> <li>Create a new project or select an existing one from the top project dropdown.</li> <li>In the left navigation sidebar, navigate to <strong>APIs &amp; Services</strong> &gt; <strong>Library</strong>.</li> <li>Search for <code class="language-plaintext highlighter-rouge">Google Calendar API</code> and select it.</li> <li>Click <strong>Enable</strong>.</li> </ol> <hr/> <h2 id="3-configuring-the-oauth-consent-screen">3. Configuring the OAuth Consent Screen</h2> <p>Google requires an explicit consent screen setup before you can issue OAuth client credentials. This screen defines what the user sees when your application requests access to their account.</p> <h3 id="step-1-app-branding--audience">Step 1: App Branding &amp; Audience</h3> <ol> <li>Navigate to <strong>APIs &amp; Services</strong> &gt; <strong>OAuth consent screen</strong> (or <strong>Google Auth platform</strong> &gt; <strong>Branding</strong> in newer console interfaces).</li> <li>Choose your <strong>User Type</strong>: <ul> <li><strong>Internal:</strong> Restricts access strictly to users within your own Google Workspace organization.</li> <li><strong>External:</strong> Essential if your multi-tenant app handles external clients, contractors, or independent practitioners.</li> </ul> </li> <li>Click <strong>Create</strong>.</li> <li>Fill in the required <strong>App information</strong>: <ul> <li><strong>App name:</strong> The name displayed to users during authorization.</li> <li><strong>User support email:</strong> For user inquiries regarding consent.</li> <li><strong>Developer contact information:</strong> Emails for Google system alerts.</li> </ul> </li> </ol> <h3 id="step-2-define-data-access-scopes">Step 2: Define Data Access Scopes</h3> <p>Scopes define the specific permissions your application is requesting. For a deep calendar booking integration, avoid requesting broad read/write access to the user’s entire drive or profile if you don’t need it.</p> <ol> <li>In the <strong>Data Access</strong> / <strong>Scopes</strong> section, click <strong>Add or Remove Scopes</strong>.</li> <li>Filter or search for the Calendar API and explicitly add the following scope:</li> </ol> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[https://www.googleapis.com/auth/calendar.events](https://www.googleapis.com/auth/calendar.events)

</code></pre></div></div> <p>⚠️ Scope Granularity: The …/auth/calendar.events scope allows your app to read and write events on the user’s calendars. If your backend only needs to create and modify bookings but shouldn’t manage secondary calendars or change global calendar settings, this is preferred over the wider …/auth/calendar scope.Step 3: Add Test UsersIf your publishing status is set to Testing (unverified), Google blocks access to all accounts except those explicitly whitelisted here.Under Audience &gt; Test users, click Add Users.Enter the Gmail or Google Workspace email addresses you plan to use for local development and testing.</p> <h2 id="4-creating-oauth-20-web-client-credentials">4. Creating OAuth 2.0 Web Client Credentials</h2> <p>With the consent screen established, you can now generate the credentials that identify your Java server application to Google.</p> <div class="mb-5 position-relative"> <div class="position-absolute d-flex align-items-center justify-content-center text-white font-weight-bold" style="left: -42px; top: 0; width: 24px; height: 24px; background-color: var(--global-theme-color, #4a4a4a); border-radius: 50%; font-size: 11pt; box-shadow: 0 0 0 4px var(--global-bg-color, #fff);"> 1 </div> <h5 class="font-weight-bold mb-1" style="color: var(--global-text-color);"> Select Client Type <span class="text-muted font-weight-light" style="font-size: 10pt; margin-left: 8px;">— Credentials Management</span> </h5> <p class="text-justify" style="font-size: 10.5pt; color: var(--global-text-color);"> Go to <strong>APIs &amp; Services &gt; Credentials</strong>. Click <strong>+ Create Credentials</strong> at the top of the interface and select <strong>OAuth client ID</strong>. From the dropdown menu, choose <strong>Web application</strong> as your application type. </p> </div> <div class="mb-5 position-relative"> <div class="position-absolute d-flex align-items-center justify-content-center text-white font-weight-bold" style="left: -42px; top: 0; width: 24px; height: 24px; background-color: var(--global-theme-color, #4a4a4a); border-radius: 50%; font-size: 11pt; box-shadow: 0 0 0 4px var(--global-bg-color, #fff);"> 2 </div> <h5 class="font-weight-bold mb-1" style="color: var(--global-text-color);"> Configure Authorized Origins <span class="text-muted font-weight-light" style="font-size: 10pt; margin-left: 8px;">— Security Restraints</span> </h5> <p class="text-justify" style="font-size: 10.5pt; color: var(--global-text-color);"> Under the <strong>Authorized JavaScript origins</strong> section, add the base origin URL of your client-side application. For local development environments, add <code>http://localhost:3000</code>. For production, add your top-level domain name. </p> </div> <div class="mb-2 position-relative"> <div class="position-absolute d-flex align-items-center justify-content-center text-white font-weight-bold" style="left: -42px; top: 0; width: 24px; height: 24px; background-color: var(--global-theme-color, #4a4a4a); border-radius: 50%; font-size: 11pt; box-shadow: 0 0 0 4px var(--global-bg-color, #fff);"> 3 </div> <h5 class="font-weight-bold mb-1" style="color: var(--global-text-color);"> Set Redirect URIs <span class="text-muted font-weight-light" style="font-size: 10pt; margin-left: 8px;">— Callback Endpoint</span> </h5> <p class="text-justify" style="font-size: 10.5pt; color: var(--global-text-color);"> Under <strong>Authorized redirect URIs</strong>, explicitly declare the absolute routing endpoint exposed by your Java backend that handles incoming authorization codes. For example: </p> <div class="card p-2 bg-light border-0" style="font-family: monospace; font-size: 10pt; color: #d63384; border-left: 4px solid var(--global-theme-color) !important;"> http://localhost:8080/api/v1/oauth2/callback/google </div> </div> <hr/> <h2 id="5-backend-parameters-for-token-long-term-retention">5. Backend Parameters for Token Long-Term Retention</h2> <p>When routing your user to Google’s authorization endpoint from your Java application (or when constructing the authorization URL using tools like the google-api-client or Spring Security OAuth2), you must pass two specific parameters to guarantee a long-term architecture works.By default, Google only returns an access token that expires in one hour. To get a permanent key (a refresh token) to update calendars while the user is offline, construct your request parameters like this:</p> <p>Parameter Key Required Value Purpose access_typeoffline Explicitly tells Google your backend needs to execute tasks when the user is not actively interacting with the browser. Crucial for receiving a refresh token.</p> <p>prompt consent Forces the consent screen to show up again even if the user has already approved your application. Google only sends the refresh_token on the very first authorization unless this parameter forces it.</p> <h3 id="handling-token-expiry-in-java">Handling Token Expiry in Java</h3> <p>Once your callback receives the auth_code, you post it back to Google’s token endpoint to receive the token bundle. Store the resulting payload securely:</p> <ol> <li>Access Token: Valid for 3600 seconds. Keep this in a temporary short-lived cache or session context.</li> <li>Refresh Token: Valid indefinitely (unless revoked by the user). Store this encrypted in your database linked directly to your tenant’s account ID.</li> </ol> <p>When your booking application fires an automated sync event and encounters a 401 Unauthorized response from the Google Calendar API, your Java error handler should intercept the exception, fetch the encrypted refresh_token, make an isolated token refresh request, update the tenant record, and retry the booking creation seamlessly.</p> <h2 id="6-token-management-best-practices">6. Token Management Best Practices</h2> <p>When building the data model on your Java backend, store the tokens securely.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Force Offline Access: When routing your user to Google's authorization endpoint, pass the query parameter access_type=offline and prompt=consent. This forces Google to supply a refresh_token alongside the initial access_token.

Handle Token Expiry: Access tokens automatically expire after 3,600 seconds (1 hour).
Your Java backend should intercept 401 Unauthorized responses from the Calendar API, use the saved refresh_token to fetch a fresh access token from https://oauth2.googleapis.com/token, update the tenant's record, and retry the scheduling payload.
</code></pre></div></div> <h3 id="next-steps">Next Steps</h3> <p>Now that your Google Cloud Platform project is successfully issuing credentials and securely passing back refresh tokens, you need to decide how those credentials map to your actual application data layer.</p> <h2 id="conclusion">Conclusion</h2> <p>To see how to isolate these tokens inside a business ecosystem without forcing every single staff member to sign into Google, head over to <strong><a href="/blog/2026/restrict-multi-tenant-bookings-and-google-accounts/">Part 2: Managing Tenant Isolation &amp; Shared Team Calendars</a></strong>.</p>]]></content><author><name></name></author><category term="backend"/><category term="architecture"/><category term="google-api"/><category term="oauth2"/><category term="java"/><category term="spring-boot"/><category term="security"/><category term="multi-tenant"/><summary type="html"><![CDATA[Setting Up Google Calendar API &amp; OAuth 2.0 for Multi-Tenant Booking Engines]]></summary></entry><entry><title type="html">Cloudflare DNS + GitHub Pages: Custom Domain and SEO for Your al-folio Site</title><link href="https://crystalhansen.ca/blog/2026/cloudflare-dns-github-pages-seo-al-folio/" rel="alternate" type="text/html" title="Cloudflare DNS + GitHub Pages: Custom Domain and SEO for Your al-folio Site"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/cloudflare-dns-github-pages-seo-al-folio</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/cloudflare-dns-github-pages-seo-al-folio/"><![CDATA[<p>Running an al-folio site on GitHub Pages is a great setup — fast, free, and version-controlled. But by default your site lives at <code class="language-plaintext highlighter-rouge">username.github.io</code>, which isn’t great for branding or SEO. This post walks through connecting a custom domain via Cloudflare DNS, then configuring al-folio’s built-in SEO features to make sure search engines actually find and rank your content.</p> <hr/> <h2 id="part-1-cloudflare-dns-setup">Part 1: Cloudflare DNS Setup</h2> <h3 id="prerequisites">Prerequisites</h3> <ul> <li>A registered domain (from any registrar — Namecheap, Google Domains, Porkbun, etc.)</li> <li>A Cloudflare account (free tier is fine)</li> <li>Your al-folio site deployed to GitHub Pages at <code class="language-plaintext highlighter-rouge">username.github.io</code></li> </ul> <hr/> <h3 id="step-1-add-your-domain-to-cloudflare">Step 1: Add Your Domain to Cloudflare</h3> <ol> <li>Log in to <a href="https://cloudflare.com">cloudflare.com</a> and click <strong>Add a Site</strong>.</li> <li>Enter your domain (e.g. <code class="language-plaintext highlighter-rouge">yourname.com</code>) and choose the <strong>Free</strong> plan.</li> <li>Cloudflare will scan your existing DNS records. Review them, then continue.</li> <li>Cloudflare will give you two nameserver addresses, something like: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aria.ns.cloudflare.com
bob.ns.cloudflare.com
</code></pre></div> </div> </li> <li>Go to your domain registrar and <strong>replace the existing nameservers</strong> with Cloudflare’s. This step hands DNS control to Cloudflare. Propagation can take up to 24 hours, but is usually much faster.</li> </ol> <hr/> <h3 id="step-2-add-dns-records-in-cloudflare">Step 2: Add DNS Records in Cloudflare</h3> <p>Once your domain is active in Cloudflare, navigate to <strong>DNS → Records</strong> and add the following.</p> <h4 id="a-records-apex-domain">A Records (apex domain)</h4> <p>Add four A records pointing your root domain (<code class="language-plaintext highlighter-rouge">@</code>) to GitHub Pages’ IP addresses:</p> <table> <thead> <tr> <th style="text-align: left">Type</th> <th style="text-align: left">Name</th> <th style="text-align: left">Content</th> <th style="text-align: left">Proxy Status</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">A</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">185.199.108.153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">A</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">185.199.109.153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">A</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">185.199.110.153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">A</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">185.199.111.153</code></td> <td style="text-align: left">DNS only</td> </tr> </tbody> </table> <blockquote> <p><strong>Important:</strong> Set Proxy Status to <strong>DNS only</strong> (grey cloud), not Proxied (orange cloud). If you proxy through Cloudflare, GitHub Pages cannot provision a Let’s Encrypt SSL certificate for your domain, which you need for the enforced HTTPS option.</p> </blockquote> <h4 id="cname-record-www-subdomain">CNAME Record (www subdomain)</h4> <table> <thead> <tr> <th style="text-align: left">Type</th> <th style="text-align: left">Name</th> <th style="text-align: left">Content</th> <th style="text-align: left">Proxy Status</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">CNAME</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">www</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">username.github.io</code></td> <td style="text-align: left">DNS only</td> </tr> </tbody> </table> <p>This lets <code class="language-plaintext highlighter-rouge">www.yourname.com</code> resolve to your site. Replace <code class="language-plaintext highlighter-rouge">username</code> with your actual GitHub username.</p> <h4 id="optional-ipv6-aaaa-records">Optional: IPv6 AAAA Records</h4> <p>GitHub Pages also supports IPv6. Adding these is good practice:</p> <table> <thead> <tr> <th style="text-align: left">Type</th> <th style="text-align: left">Name</th> <th style="text-align: left">Content</th> <th style="text-align: left">Proxy Status</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">AAAA</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">2606:50c0:8000::153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">AAAA</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">2606:50c0:8001::153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">AAAA</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">2606:50c0:8002::153</code></td> <td style="text-align: left">DNS only</td> </tr> <tr> <td style="text-align: left">AAAA</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">@</code></td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">2606:50c0:8003::153</code></td> <td style="text-align: left">DNS only</td> </tr> </tbody> </table> <hr/> <h3 id="step-3-configure-the-custom-domain-in-github">Step 3: Configure the Custom Domain in GitHub</h3> <ol> <li>In your GitHub repository, go to <strong>Settings → Pages</strong>.</li> <li>Under <strong>Custom domain</strong>, enter your apex domain (e.g. <code class="language-plaintext highlighter-rouge">yourname.com</code>) and click <strong>Save</strong>.</li> <li>GitHub will create (or update) a <code class="language-plaintext highlighter-rouge">CNAME</code> file at the root of your repo. Make sure this file is committed and not gitignored. For al-folio sites deployed via GitHub Actions, you may need to add this file manually to your repo root.</li> <li>Wait a few minutes, then check the <strong>Enforce HTTPS</strong> checkbox. If it’s greyed out, DNS hasn’t fully propagated yet — give it another 10–30 minutes.</li> </ol> <h4 id="verify-dns-with-dig">Verify DNS with <code class="language-plaintext highlighter-rouge">dig</code></h4> <p>Once propagation is done, confirm your records are correct:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check A records</span>
dig yourname.com +noall +answer <span class="nt">-t</span> A

<span class="c"># Expected output:</span>
<span class="c"># yourname.com. 3600 IN A 185.199.108.153</span>
<span class="c"># yourname.com. 3600 IN A 185.199.109.153</span>
<span class="c"># yourname.com. 3600 IN A 185.199.110.153</span>
<span class="c"># yourname.com. 3600 IN A 185.199.111.153</span>

<span class="c"># Check CNAME for www</span>
dig www.yourname.com +noall +answer <span class="nt">-t</span> CNAME
</code></pre></div></div> <hr/> <h3 id="step-4-update-_configyml-with-your-custom-domain">Step 4: Update <code class="language-plaintext highlighter-rouge">_config.yml</code> with Your Custom Domain</h3> <p>Open <code class="language-plaintext highlighter-rouge">_config.yml</code> in your al-folio repo and update the <code class="language-plaintext highlighter-rouge">url</code> field:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">url</span><span class="pi">:</span> <span class="s">https://yourname.com</span> <span class="c1"># your custom domain, NOT username.github.io</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span> <span class="c1"># leave empty for apex domains</span>
</code></pre></div></div> <p>This is important — al-folio uses <code class="language-plaintext highlighter-rouge">url</code> and <code class="language-plaintext highlighter-rouge">baseurl</code> to generate canonical URLs, Open Graph tags, and the sitemap. If these are wrong, your SEO metadata will point to the wrong domain.</p> <hr/> <h2 id="part-2-seo-configuration-in-al-folio">Part 2: SEO Configuration in al-folio</h2> <p>Al-folio ships with several SEO features built in. Most of them just need the right values in <code class="language-plaintext highlighter-rouge">_config.yml</code> and your post front matter.</p> <h3 id="what-al-folio-gives-you-out-of-the-box">What al-folio gives you out of the box</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">jekyll-seo-tag</code></strong> — generates <code class="language-plaintext highlighter-rouge">&lt;title&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;meta name="description"&gt;</code>, Open Graph (<code class="language-plaintext highlighter-rouge">og:</code>), and Twitter Card tags on every page automatically.</li> <li><strong><code class="language-plaintext highlighter-rouge">jekyll-sitemap</code></strong> — generates <code class="language-plaintext highlighter-rouge">/sitemap.xml</code> for search engine crawlers.</li> <li><strong><code class="language-plaintext highlighter-rouge">robots.txt</code></strong> — included at the root with permissive crawl rules.</li> <li><strong>Canonical URLs</strong> — built into the SEO tag plugin, preventing duplicate content penalties.</li> </ul> <hr/> <h3 id="_configyml-seo-settings"><code class="language-plaintext highlighter-rouge">_config.yml</code> SEO settings</h3> <p>Here are the key fields to fill in:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Site identity</span>
<span class="na">title</span><span class="pi">:</span> <span class="s">Your Name</span>
<span class="na">first_name</span><span class="pi">:</span> <span class="s">First</span>
<span class="na">last_name</span><span class="pi">:</span> <span class="s">Last</span>
<span class="na">email</span><span class="pi">:</span> <span class="s">you@yourname.com</span>
<span class="na">description</span><span class="pi">:</span> <span class="pi">&gt;</span>
  <span class="s">Academic researcher and engineer. Writing about systems,</span>
  <span class="s">machine learning, and the occasional DevOps rabbit hole.</span>

<span class="c1"># URL — must be your custom domain once Cloudflare is set up</span>
<span class="na">url</span><span class="pi">:</span> <span class="s">https://yourname.com</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>

<span class="c1"># Keywords used in &lt;meta name="keywords"&gt;</span>
<span class="na">keywords</span><span class="pi">:</span> <span class="s">jekyll, academic, portfolio, machine-learning</span>

<span class="c1"># Language</span>
<span class="na">lang</span><span class="pi">:</span> <span class="s">en</span>

<span class="c1"># Social — used in SEO tags and footer</span>
<span class="na">social</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">Your Full Name</span>
  <span class="na">links</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">https://twitter.com/yourhandle</span>
    <span class="pi">-</span> <span class="s">https://github.com/yourusername</span>
    <span class="pi">-</span> <span class="s">https://linkedin.com/in/yourprofile</span>

<span class="c1"># Google Analytics (optional but useful for tracking search performance)</span>
<span class="na">google_analytics</span><span class="pi">:</span> <span class="s">G-XXXXXXXXXX</span> <span class="c1"># your Measurement ID</span>

<span class="c1"># Google Search Console verification</span>
<span class="na">google_site_verification</span><span class="pi">:</span> <span class="s">your-verification-token</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">social.links</code> array is picked up by <code class="language-plaintext highlighter-rouge">jekyll-seo-tag</code> and rendered as <code class="language-plaintext highlighter-rouge">sameAs</code> JSON-LD structured data, which helps Google associate your profiles across the web.</p> <hr/> <h3 id="per-post-front-matter-for-seo">Per-post front matter for SEO</h3> <p>For each blog post, these front matter fields directly influence how the page is indexed and displayed in search results:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">layout</span><span class="pi">:</span> <span class="s">post</span>
<span class="na">title</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Your</span><span class="nv"> </span><span class="s">Post</span><span class="nv"> </span><span class="s">Title"</span> <span class="c1"># &lt;title&gt; tag and og:title</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2026-05-17</span>
<span class="na">description</span><span class="pi">:</span> <span class="s2">"</span><span class="s">A</span><span class="nv"> </span><span class="s">concise</span><span class="nv"> </span><span class="s">summary</span><span class="nv"> </span><span class="s">of</span><span class="nv"> </span><span class="s">what</span><span class="nv"> </span><span class="s">this</span><span class="nv"> </span><span class="s">post</span><span class="nv"> </span><span class="s">covers.</span><span class="nv"> </span><span class="s">Aim</span><span class="nv"> </span><span class="s">for</span>
  <span class="s">150–160</span><span class="nv"> </span><span class="s">characters</span><span class="nv"> </span><span class="s">—</span><span class="nv"> </span><span class="s">this</span><span class="nv"> </span><span class="s">is</span><span class="nv"> </span><span class="s">what</span><span class="nv"> </span><span class="s">appears</span><span class="nv"> </span><span class="s">in</span><span class="nv"> </span><span class="s">Google's</span>
  <span class="s">search</span><span class="nv"> </span><span class="s">result</span><span class="nv"> </span><span class="s">snippets."</span>
<span class="na">tags</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">tag1</span><span class="pi">,</span> <span class="nv">tag2</span><span class="pi">,</span> <span class="nv">tag3</span><span class="pi">]</span>
<span class="na">categories</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">category</span><span class="pi">]</span>
<span class="na">og_image</span><span class="pi">:</span> <span class="s">/assets/img/posts/your-preview-image.jpg</span> <span class="c1"># og:image for link previews</span>
<span class="nn">---</span>
</code></pre></div></div> <blockquote> <p><strong>Tip on descriptions:</strong> Write these as if they’re ad copy. They don’t directly affect ranking, but a good description improves click-through rate from search results, which does.</p> </blockquote> <hr/> <h3 id="enabling-google-search-console">Enabling Google Search Console</h3> <p>Once your domain is live:</p> <ol> <li>Go to <a href="https://search.google.com/search-console">search.google.com/search-console</a>.</li> <li>Add a new property using your custom domain.</li> <li>Google will ask you to verify ownership. The easiest method is the <strong>HTML tag</strong> approach — copy the <code class="language-plaintext highlighter-rouge">content</code> value from the meta tag Google provides, and paste it into <code class="language-plaintext highlighter-rouge">_config.yml</code>:</li> </ol> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">google_site_verification</span><span class="pi">:</span> <span class="s">abc123yourtokenhere</span>
</code></pre></div></div> <p>Al-folio’s <code class="language-plaintext highlighter-rouge">_includes/head.html</code> already outputs this tag when the field is set. Rebuild and push, then click Verify in Search Console.</p> <ol> <li>Once verified, submit your sitemap:</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://yourname.com/sitemap.xml
</code></pre></div></div> <hr/> <h3 id="checking-your-sitemap-and-robotstxt">Checking your sitemap and robots.txt</h3> <p>After deploying with your custom domain set in <code class="language-plaintext highlighter-rouge">_config.yml</code>, verify these are generating correctly:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://yourname.com/sitemap.xml
https://yourname.com/robots.txt
</code></pre></div></div> <p>The default <code class="language-plaintext highlighter-rouge">robots.txt</code> in al-folio looks like:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>User-agent: *
Allow: /
Sitemap: https://yourname.com/sitemap.xml
</code></pre></div></div> <p>If you want to exclude certain pages (e.g. a <code class="language-plaintext highlighter-rouge">/drafts/</code> path or <code class="language-plaintext highlighter-rouge">/admin/</code>), you can add <code class="language-plaintext highlighter-rouge">Disallow</code> rules here.</p> <hr/> <h3 id="structured-data-json-ld">Structured Data (JSON-LD)</h3> <p>Al-folio doesn’t output rich structured data by default beyond what <code class="language-plaintext highlighter-rouge">jekyll-seo-tag</code> provides. If you want to add <code class="language-plaintext highlighter-rouge">BlogPosting</code> schema to individual posts (which can enable rich results in Google), add a <code class="language-plaintext highlighter-rouge">_layouts/post.html</code> override with a JSON-LD block:</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;script </span><span class="na">type=</span><span class="s">"application/ld+json"</span><span class="nt">&gt;</span>
  <span class="p">{</span>
    <span class="dl">"</span><span class="s2">@context</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">https://schema.org</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">@type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">BlogPosting</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">headline</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Cloudflare DNS + GitHub Pages: Custom Domain and SEO for Your al-folio Site</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">datePublished</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">2026-05-17T00:00:00+00:00</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">description</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Step-by-step guide to pointing a custom domain at your GitHub Pages site using Cloudflare DNS, and tuning your al-folio Jekyll theme for solid SEO.</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">url</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">https://crystalhansen.ca/blog/2026/cloudflare-dns-github-pages-seo-al-folio/</span><span class="dl">"</span><span class="p">,</span>
    <span class="dl">"</span><span class="s2">author</span><span class="dl">"</span><span class="p">:</span> <span class="p">{</span>
      <span class="dl">"</span><span class="s2">@type</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Person</span><span class="dl">"</span><span class="p">,</span>
      <span class="dl">"</span><span class="s2">name</span><span class="dl">"</span><span class="p">:</span> <span class="dl">"</span><span class="s2">Crystal Hansen</span><span class="dl">"</span>
    <span class="p">}</span>
  <span class="p">}</span>
<span class="nt">&lt;/script&gt;</span>
</code></pre></div></div> <hr/> <h2 id="quick-reference">Quick Reference</h2> <table> <thead> <tr> <th style="text-align: left">Task</th> <th style="text-align: left">Where</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Add A records for apex domain</td> <td style="text-align: left">Cloudflare DNS → Records</td> </tr> <tr> <td style="text-align: left">Add CNAME for <code class="language-plaintext highlighter-rouge">www</code></td> <td style="text-align: left">Cloudflare DNS → Records</td> </tr> <tr> <td style="text-align: left">Set Proxy Status</td> <td style="text-align: left">DNS only (grey cloud) for all GitHub Pages records</td> </tr> <tr> <td style="text-align: left">Set custom domain</td> <td style="text-align: left">GitHub repo → Settings → Pages</td> </tr> <tr> <td style="text-align: left">Update <code class="language-plaintext highlighter-rouge">url</code> field</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">_config.yml</code></td> </tr> <tr> <td style="text-align: left">Set site description &amp; keywords</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">_config.yml</code></td> </tr> <tr> <td style="text-align: left">Enable Google Search Console</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">google_site_verification</code> in <code class="language-plaintext highlighter-rouge">_config.yml</code></td> </tr> <tr> <td style="text-align: left">Submit sitemap</td> <td style="text-align: left">Google Search Console → Sitemaps</td> </tr> <tr> <td style="text-align: left">Per-post SEO</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">description</code> and <code class="language-plaintext highlighter-rouge">og_image</code> in post front matter</td> </tr> </tbody> </table> <hr/> <h2 id="wrapping-up">Wrapping Up</h2> <p>The Cloudflare + GitHub Pages combination is hard to beat for a personal or academic site: free CDN, DDoS protection, and a clean DNS dashboard. The key gotcha is keeping records in <strong>DNS only</strong> mode so GitHub can handle HTTPS itself — don’t let Cloudflare proxy the connection until you have the SSL cert provisioned.</p> <p>On the SEO side, al-folio handles the heavy lifting through <code class="language-plaintext highlighter-rouge">jekyll-seo-tag</code> and <code class="language-plaintext highlighter-rouge">jekyll-sitemap</code>. The main things left for you to do are fill in <code class="language-plaintext highlighter-rouge">_config.yml</code> properly, write good post <code class="language-plaintext highlighter-rouge">description</code> fields, and register with Google Search Console so you can actually track how your content performs in search.</p>]]></content><author><name></name></author><category term="devops"/><category term="devops-posts"/><category term="cloudflare"/><category term="github-pages"/><category term="dns"/><category term="seo"/><category term="al-folio"/><category term="jekyll"/><summary type="html"><![CDATA[Step-by-step guide to pointing a custom domain at your GitHub Pages site using Cloudflare DNS, and tuning your al-folio Jekyll theme for solid SEO.]]></summary></entry><entry><title type="html">Running Maven Builds: From Testing to EC2 Deployment</title><link href="https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2/" rel="alternate" type="text/html" title="Running Maven Builds: From Testing to EC2 Deployment"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/mvn-build-test-deploy-ec2/"><![CDATA[<p>Whether you’re shipping a microservice or a full-stack Java application, Maven is the backbone of most JVM build pipelines. This guide walks through the full lifecycle — running tests, packaging your app, and deploying it to an AWS EC2 instance — with practical commands you can drop straight into your workflow.</p> <hr/> <h2 id="prerequisites">Prerequisites</h2> <p>Before you start, make sure you have:</p> <ul> <li><strong>Java 11+</strong> installed (<code class="language-plaintext highlighter-rouge">java -version</code>)</li> <li><strong>Maven 3.6+</strong> installed (<code class="language-plaintext highlighter-rouge">mvn -version</code>)</li> <li>An AWS EC2 instance running (Amazon Linux 2 or Ubuntu)</li> <li>SSH access to the instance (your <code class="language-plaintext highlighter-rouge">.pem</code> key file)</li> <li>Java installed on the EC2 instance</li> </ul> <hr/> <h2 id="part-1-running-tests-with-maven">Part 1: Running Tests with Maven</h2> <p>Maven’s test lifecycle is straightforward. The key phases you’ll use are <code class="language-plaintext highlighter-rouge">test</code>, <code class="language-plaintext highlighter-rouge">verify</code>, and <code class="language-plaintext highlighter-rouge">surefire:test</code>.</p> <h3 id="run-all-unit-tests">Run all unit tests</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span>
</code></pre></div></div> <p>This compiles your source code and test code, then runs all unit tests found under <code class="language-plaintext highlighter-rouge">src/test/java</code>. Test reports land in <code class="language-plaintext highlighter-rouge">target/surefire-reports/</code>.</p> <h3 id="run-tests-and-continue-on-failure">Run tests and continue on failure</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dmaven</span>.test.failure.ignore<span class="o">=</span><span class="nb">true</span>
</code></pre></div></div> <p>Useful in CI when you want to collect all test failures rather than stopping at the first one.</p> <h3 id="run-a-single-test-class">Run a single test class</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dtest</span><span class="o">=</span>UserServiceTest
</code></pre></div></div> <p>Or target a specific method:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn <span class="nb">test</span> <span class="nt">-Dtest</span><span class="o">=</span>UserServiceTest#shouldReturnUserById
</code></pre></div></div> <h3 id="run-integration-tests">Run integration tests</h3> <p>Integration tests typically live in the <code class="language-plaintext highlighter-rouge">verify</code> phase, often powered by the Maven Failsafe plugin:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn verify
</code></pre></div></div> <p>This runs both unit tests (<code class="language-plaintext highlighter-rouge">*Test.java</code>) and integration tests (<code class="language-plaintext highlighter-rouge">*IT.java</code>) in sequence.</p> <h3 id="skip-tests-entirely">Skip tests entirely</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn package <span class="nt">-DskipTests</span>
</code></pre></div></div> <blockquote> <p><strong>Note:</strong> <code class="language-plaintext highlighter-rouge">-DskipTests</code> skips test <em>execution</em> but still compiles test classes. Use <code class="language-plaintext highlighter-rouge">-Dmaven.test.skip=true</code> to skip compilation too.</p> </blockquote> <hr/> <h2 id="part-2-building-the-artifact">Part 2: Building the Artifact</h2> <p>Once tests pass, package your application into a deployable artifact — typically a <code class="language-plaintext highlighter-rouge">.jar</code> or <code class="language-plaintext highlighter-rouge">.war</code>.</p> <h3 id="build-a-jar">Build a JAR</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">clean</code> wipes the <code class="language-plaintext highlighter-rouge">target/</code> directory first. <code class="language-plaintext highlighter-rouge">package</code> compiles, tests, and packages. Your artifact ends up at:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>target/your-app-1.0.0.jar
</code></pre></div></div> <h3 id="build-a-fatuber-jar-spring-boot">Build a fat/uber JAR (Spring Boot)</h3> <p>If you’re using Spring Boot or the Maven Shade plugin, <code class="language-plaintext highlighter-rouge">mvn package</code> produces a self-contained executable JAR with all dependencies bundled:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package <span class="nt">-DskipTests</span>
</code></pre></div></div> <p>Run it directly:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java <span class="nt">-jar</span> target/your-app-1.0.0.jar
</code></pre></div></div> <h3 id="build-for-a-specific-maven-profile">Build for a specific Maven profile</h3> <p>Maven profiles let you swap configs — for example, <code class="language-plaintext highlighter-rouge">dev</code> vs <code class="language-plaintext highlighter-rouge">prod</code> database URLs:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package <span class="nt">-Pprod</span> <span class="nt">-DskipTests</span>
</code></pre></div></div> <hr/> <h2 id="part-3-deploying-to-ec2">Part 3: Deploying to EC2</h2> <p>With your artifact built, the next step is getting it onto the EC2 instance. Here are the most practical deployment options.</p> <h3 id="option-a-scp--ssh-simple-and-direct">Option A: SCP + SSH (Simple and Direct)</h3> <p>The most straightforward approach for smaller teams or manual deployments.</p> <p><strong>Step 1 — Copy the JAR to EC2:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>scp <span class="nt">-i</span> ~/.ssh/your-key.pem <span class="se">\</span>
  target/your-app-1.0.0.jar <span class="se">\</span>
  ec2-user@&lt;YOUR_EC2_IP&gt;:/home/ec2-user/app/
</code></pre></div></div> <blockquote> <p>On Ubuntu instances, replace <code class="language-plaintext highlighter-rouge">ec2-user</code> with <code class="language-plaintext highlighter-rouge">ubuntu</code>.</p> </blockquote> <p><strong>Step 2 — SSH into the instance:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ssh <span class="nt">-i</span> ~/.ssh/your-key.pem ec2-user@&lt;YOUR_EC2_IP&gt;
</code></pre></div></div> <p><strong>Step 3 — Run the application:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /home/ec2-user/app
<span class="nb">nohup </span>java <span class="nt">-jar</span> your-app-1.0.0.jar <span class="o">&gt;</span> app.log 2&gt;&amp;1 &amp;
</code></pre></div></div> <hr/> <h3 id="option-b-maven-wagon-plugin">Option B: Maven Wagon Plugin</h3> <p>Wire the SCP step directly into your Maven build. Add this to your <code class="language-plaintext highlighter-rouge">pom.xml</code>:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;build&gt;</span>
  <span class="nt">&lt;extensions&gt;</span>
    <span class="nt">&lt;extension&gt;</span>
      <span class="nt">&lt;groupId&gt;</span>org.apache.maven.wagon<span class="nt">&lt;/groupId&gt;</span>
      <span class="nt">&lt;artifactId&gt;</span>wagon-ssh<span class="nt">&lt;/artifactId&gt;</span>
      <span class="nt">&lt;version&gt;</span>3.5.3<span class="nt">&lt;/version&gt;</span>
    <span class="nt">&lt;/extension&gt;</span>
  <span class="nt">&lt;/extensions&gt;</span>

  <span class="nt">&lt;plugins&gt;</span>
    <span class="nt">&lt;plugin&gt;</span>
      <span class="nt">&lt;groupId&gt;</span>org.codehaus.mojo<span class="nt">&lt;/groupId&gt;</span>
      <span class="nt">&lt;artifactId&gt;</span>wagon-maven-plugin<span class="nt">&lt;/artifactId&gt;</span>
      <span class="nt">&lt;version&gt;</span>2.0.2<span class="nt">&lt;/version&gt;</span>
      <span class="nt">&lt;configuration&gt;</span>
        <span class="nt">&lt;serverId&gt;</span>ec2-server<span class="nt">&lt;/serverId&gt;</span>
        <span class="nt">&lt;url&gt;</span>scp://ec2-user@<span class="nt">&lt;YOUR_EC2_IP&gt;</span>/home/ec2-user/app<span class="nt">&lt;/url&gt;</span>
        <span class="nt">&lt;fromFile&gt;</span>target/your-app-1.0.0.jar<span class="nt">&lt;/fromFile&gt;</span>
        <span class="nt">&lt;toFile&gt;</span>your-app.jar<span class="nt">&lt;/toFile&gt;</span>
      <span class="nt">&lt;/configuration&gt;</span>
    <span class="nt">&lt;/plugin&gt;</span>
  <span class="nt">&lt;/plugins&gt;</span>
<span class="nt">&lt;/build&gt;</span>
</code></pre></div></div> <p>Add your EC2 SSH key to <code class="language-plaintext highlighter-rouge">~/.m2/settings.xml</code>:</p> <div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;servers&gt;</span>
  <span class="nt">&lt;server&gt;</span>
    <span class="nt">&lt;id&gt;</span>ec2-server<span class="nt">&lt;/id&gt;</span>
    <span class="nt">&lt;privateKey&gt;</span>/path/to/your-key.pem<span class="nt">&lt;/privateKey&gt;</span>
  <span class="nt">&lt;/server&gt;</span>
<span class="nt">&lt;/servers&gt;</span>
</code></pre></div></div> <p>Then deploy with a single command:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mvn clean package wagon:upload-single <span class="nt">-DskipTests</span>
</code></pre></div></div> <hr/> <h3 id="option-c-github-actions-cicd-pipeline">Option C: GitHub Actions CI/CD Pipeline</h3> <p>For a fully automated pipeline, here’s a GitHub Actions workflow that tests, builds, and deploys on every push to <code class="language-plaintext highlighter-rouge">main</code>:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># .github/workflows/deploy.yml</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">Build and Deploy to EC2</span>

<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build-and-deploy</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>

    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Checkout code</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v3</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Set up Java</span>
        <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-java@v3</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">java-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">17"</span>
          <span class="na">distribution</span><span class="pi">:</span> <span class="s2">"</span><span class="s">temurin"</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Run tests</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mvn test</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build artifact</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mvn clean package -DskipTests</span>

      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Deploy to EC2</span>
        <span class="na">env</span><span class="pi">:</span>
          <span class="na">EC2_HOST</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">EC2_USER</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">EC2_KEY</span><span class="pi">:</span> <span class="s">$</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">echo "$EC2_KEY" &gt; key.pem</span>
          <span class="s">chmod 600 key.pem</span>
          <span class="s">scp -i key.pem -o StrictHostKeyChecking=no \</span>
            <span class="s">target/*.jar $EC2_USER@$EC2_HOST:/home/$EC2_USER/app/app.jar</span>
          <span class="s">ssh -i key.pem -o StrictHostKeyChecking=no $EC2_USER@$EC2_HOST \</span>
            <span class="s">"pkill -f 'java -jar' || true &amp;&amp; \</span>
             <span class="s">nohup java -jar /home/$EC2_USER/app/app.jar &gt; /home/$EC2_USER/app/app.log 2&gt;&amp;1 &amp;"</span>
          <span class="s">rm key.pem</span>
</code></pre></div></div> <p>Store <code class="language-plaintext highlighter-rouge">EC2_HOST</code>, <code class="language-plaintext highlighter-rouge">EC2_USER</code>, and <code class="language-plaintext highlighter-rouge">EC2_SSH_KEY</code> as GitHub repository secrets.</p> <hr/> <h2 id="part-4-running-as-a-systemd-service-recommended-for-production">Part 4: Running as a systemd Service (Recommended for Production)</h2> <p>Instead of <code class="language-plaintext highlighter-rouge">nohup</code>, manage the process with systemd — it restarts automatically on crash and survives reboots.</p> <p><strong>Create a service file on EC2:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/systemd/system/myapp.service
</code></pre></div></div> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[Unit]</span>
<span class="py">Description</span><span class="p">=</span><span class="s">My Java Application</span>
<span class="py">After</span><span class="p">=</span><span class="s">network.target</span>

<span class="nn">[Service]</span>
<span class="py">User</span><span class="p">=</span><span class="s">ec2-user</span>
<span class="py">WorkingDirectory</span><span class="p">=</span><span class="s">/home/ec2-user/app</span>
<span class="py">ExecStart</span><span class="p">=</span><span class="s">/usr/bin/java -jar /home/ec2-user/app/app.jar</span>
<span class="py">SuccessExitStatus</span><span class="p">=</span><span class="s">143</span>
<span class="py">Restart</span><span class="p">=</span><span class="s">on-failure</span>
<span class="py">RestartSec</span><span class="p">=</span><span class="s">10</span>
<span class="py">StandardOutput</span><span class="p">=</span><span class="s">journal</span>
<span class="py">StandardError</span><span class="p">=</span><span class="s">journal</span>

<span class="nn">[Install]</span>
<span class="py">WantedBy</span><span class="p">=</span><span class="s">multi-user.target</span>
</code></pre></div></div> <p><strong>Enable and start the service:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl daemon-reload
<span class="nb">sudo </span>systemctl <span class="nb">enable </span>myapp
<span class="nb">sudo </span>systemctl start myapp
</code></pre></div></div> <p><strong>Check status and tail logs:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status myapp
<span class="nb">sudo </span>journalctl <span class="nt">-u</span> myapp <span class="nt">-f</span>
</code></pre></div></div> <p>To deploy a new version, copy the new JAR and restart:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl restart myapp
</code></pre></div></div> <hr/> <h2 id="quick-reference">Quick Reference</h2> <table> <thead> <tr> <th style="text-align: left">Goal</th> <th style="text-align: left">Command</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Run all tests</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn test</code></td> </tr> <tr> <td style="text-align: left">Run integration tests</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn verify</code></td> </tr> <tr> <td style="text-align: left">Run a single test</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn test -Dtest=MyTest</code></td> </tr> <tr> <td style="text-align: left">Build (skip tests)</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn clean package -DskipTests</code></td> </tr> <tr> <td style="text-align: left">Build for prod profile</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">mvn clean package -Pprod</code></td> </tr> <tr> <td style="text-align: left">Copy JAR to EC2</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">scp -i key.pem target/app.jar ec2-user@HOST:/path/</code></td> </tr> <tr> <td style="text-align: left">SSH to EC2</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">ssh -i key.pem ec2-user@HOST</code></td> </tr> <tr> <td style="text-align: left">Restart systemd service</td> <td style="text-align: left"><code class="language-plaintext highlighter-rouge">sudo systemctl restart myapp</code></td> </tr> </tbody> </table> <hr/> <h2 id="wrapping-up">Wrapping Up</h2> <p>The Maven build lifecycle covers everything from compiling and testing to packaging, and with a few shell commands (or a CI pipeline), you can take that artifact all the way to a running EC2 instance. For production workloads, running your app as a systemd service is worth the five-minute setup — you get automatic restarts, centralized logging via <code class="language-plaintext highlighter-rouge">journalctl</code>, and clean start/stop semantics.</p> <p>From here, the natural next step is moving toward a more managed deployment target — ECS, Elastic Beanstalk, or a CodeDeploy-managed EC2 fleet — but this pipeline gets most teams very far, very fast.</p>]]></content><author><name></name></author><category term="devops"/><category term="devops-posts"/><category term="maven"/><category term="java"/><category term="aws"/><category term="ec2"/><category term="devops"/><category term="ci-cd"/><category term="serverless-arch"/><summary type="html"><![CDATA[A practical guide to running mvn test, packaging your Java app, and deploying it to AWS EC2 — including CI/CD with GitHub Actions and running as a systemd service.]]></summary></entry><entry><title type="html">Layer 5 — Spring Security: Role-Based Authorization and CORS</title><link href="https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors/" rel="alternate" type="text/html" title="Layer 5 — Spring Security: Role-Based Authorization and CORS"/><published>2026-05-12T14:40:00+00:00</published><updated>2026-05-12T14:40:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer5-spring-security-roles-cors/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>By the time a request reaches Spring Security’s authorization layer, it has already cleared four checkpoints:</p> <ol> <li><strong>nginx</strong> — TLS terminated, tenant host header preserved</li> <li><strong>TenantResolverFilter</strong> — tenant identity established</li> <li><strong>JwtAuthenticationFilter</strong> — user identity established (or not)</li> <li><strong>ApiKeyFilter</strong> — machine identity established (or not)</li> </ol> <p>Layer 5 answers the final question: <strong>is this authenticated principal allowed to do what they’re asking to do?</strong></p> <p>This is also where CORS is configured — determining which frontend origins are permitted to make cross-origin requests to the API.</p> <hr/> <h2 id="authorization-vs-authentication">Authorization vs Authentication</h2> <p>These terms are often conflated. In this filter chain they are strictly separated:</p> <ul> <li><strong>Authentication</strong> (Layers 3 and 4): <em>Who are you?</em> Verified by signature, token validity, key hash.</li> <li><strong>Authorization</strong> (Layer 5): <em>Are you allowed to do this?</em> Decided by roles, tenant context, and endpoint rules.</li> </ul> <p>A user can be perfectly authenticated but unauthorized — a valid <code class="language-plaintext highlighter-rouge">ROLE_USER</code> JWT that tries to reach an <code class="language-plaintext highlighter-rouge">ROLE_ADMIN</code> endpoint should get <code class="language-plaintext highlighter-rouge">403 Forbidden</code>, not <code class="language-plaintext highlighter-rouge">401 Unauthorized</code>. The distinction matters both for security semantics and client error handling.</p> <hr/> <h2 id="the-role-model">The Role Model</h2> <p>In a multi-tenant SaaS, roles need to carry tenant scope. A user who is an admin for tenant A must not be able to access tenant B’s admin endpoints — even with a valid token.</p> <p>We enforce this at two levels:</p> <p><strong>Level 1 — Token claims</strong> (Layer 3): The JWT’s <code class="language-plaintext highlighter-rouge">domain</code> claim is cross-checked against the resolved tenant context. A token for tenant A can’t be presented to tenant B’s API subdomain.</p> <p><strong>Level 2 — Spring Security authorization</strong> (Layer 5): <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> checks roles. Roles are loaded from the JWT claims for the current tenant, so the role context is already scoped to the right tenant by the time authorization runs.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Roles in use</span>
<span class="no">ROLE_USER</span>        <span class="c1">// standard authenticated user</span>
<span class="no">ROLE_ADMIN</span>       <span class="c1">// tenant administrator (full access to tenant data)</span>
<span class="no">ROLE_SUPERADMIN</span>  <span class="c1">// platform administrator (cross-tenant operations)</span>
<span class="no">ROLE_API_CLIENT</span>  <span class="c1">// machine-to-machine (API key authenticated)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ROLE_SUPERADMIN</code> is the only role that crosses tenant boundaries — and it’s never issued via the normal JWT flow. It’s only used for platform-level admin operations (provisioning, billing, monitoring).</p> <hr/> <h2 id="securityfilterchain-configuration">SecurityFilterChain Configuration</h2> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="nd">@EnableMethodSecurity</span>  <span class="c1">// enables @PreAuthorize</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SecurityConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">SecurityFilterChain</span> <span class="nf">filterChain</span><span class="o">(</span><span class="nc">HttpSecurity</span> <span class="n">http</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">http</span>
            <span class="c1">// Disable CSRF for stateless API — session cookie uses SameSite=Lax instead</span>
            <span class="o">.</span><span class="na">csrf</span><span class="o">(</span><span class="n">csrf</span> <span class="o">-&gt;</span> <span class="n">csrf</span><span class="o">.</span><span class="na">disable</span><span class="o">())</span>

            <span class="c1">// CORS configured separately</span>
            <span class="o">.</span><span class="na">cors</span><span class="o">(</span><span class="n">cors</span> <span class="o">-&gt;</span> <span class="n">cors</span><span class="o">.</span><span class="na">configurationSource</span><span class="o">(</span><span class="n">corsConfigurationSource</span><span class="o">()))</span>

            <span class="c1">// Session management — stateless for API, but allow session for browser</span>
            <span class="o">.</span><span class="na">sessionManagement</span><span class="o">(</span><span class="n">session</span> <span class="o">-&gt;</span> <span class="n">session</span>
                <span class="o">.</span><span class="na">sessionCreationPolicy</span><span class="o">(</span><span class="nc">SessionCreationPolicy</span><span class="o">.</span><span class="na">IF_REQUIRED</span><span class="o">))</span>

            <span class="c1">// Authorization rules</span>
            <span class="o">.</span><span class="na">authorizeHttpRequests</span><span class="o">(</span><span class="n">auth</span> <span class="o">-&gt;</span> <span class="n">auth</span>

                <span class="c1">// Public endpoints — no auth required</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">GET</span><span class="o">,</span> <span class="s">"/api/appoint/day/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">POST</span><span class="o">,</span> <span class="s">"/api/appoint/request"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">GET</span><span class="o">,</span> <span class="s">"/api/resources/public/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

                <span class="c1">// Auth endpoints — permit all (they ARE the auth flow)</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/auth/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

                <span class="c1">// Admin endpoints</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"ADMIN"</span><span class="o">)</span>

                <span class="c1">// Platform endpoints — superadmin only</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/platform/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"SUPERADMIN"</span><span class="o">)</span>

                <span class="c1">// API client endpoints</span>
                <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/automation/**"</span><span class="o">).</span><span class="na">hasRole</span><span class="o">(</span><span class="s">"API_CLIENT"</span><span class="o">)</span>

                <span class="c1">// Everything else requires authentication</span>
                <span class="o">.</span><span class="na">anyRequest</span><span class="o">().</span><span class="na">authenticated</span><span class="o">()</span>
            <span class="o">);</span>

        <span class="c1">// Register filters in order</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">tenantResolverFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">jwtAuthenticationFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>
        <span class="n">http</span><span class="o">.</span><span class="na">addFilterBefore</span><span class="o">(</span><span class="n">apiKeyFilter</span><span class="o">,</span> <span class="nc">UsernamePasswordAuthenticationFilter</span><span class="o">.</span><span class="na">class</span><span class="o">);</span>

        <span class="k">return</span> <span class="n">http</span><span class="o">.</span><span class="na">build</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="method-level-authorization-with-preauthorize">Method-Level Authorization with @PreAuthorize</h2> <p>URL-based authorization (<code class="language-plaintext highlighter-rouge">authorizeHttpRequests</code>) is coarse-grained — it matches URL patterns to roles. Method-level authorization is fine-grained — it checks conditions at the controller or service method.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@RestController</span>
<span class="nd">@RequestMapping</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">CrmController</span> <span class="o">{</span>

    <span class="c1">// Any authenticated user can list clients (for their tenant — data filtered by Hibernate)</span>
    <span class="nd">@GetMapping</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"isAuthenticated()"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ClientDto</span><span class="o">&gt;</span> <span class="nf">listClients</span><span class="o">()</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Only admins can update client records</span>
    <span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="nc">ClientDto</span> <span class="nf">updateClient</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="nd">@RequestBody</span> <span class="nc">ClientUpdateDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Only admins can toggle consent — this is a compliance-sensitive operation</span>
    <span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}/consent"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">updateConsent</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="nd">@RequestBody</span> <span class="nc">ConsentDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>

    <span class="c1">// Admins can delete, but not API clients — even if they have ROLE_API_CLIENT</span>
    <span class="nd">@DeleteMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
    <span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN') and !hasRole('API_CLIENT')"</span><span class="o">)</span>
    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">deleteClient</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Why use both URL rules and <code class="language-plaintext highlighter-rouge">@PreAuthorize</code>?</strong> URL rules catch misconfigured requests early (before hitting the controller). <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> is the authoritative check at the business logic boundary. Defense in depth — if a URL rule is accidentally too permissive, the method annotation is a second gate.</p> <hr/> <h2 id="cors-configuration">CORS Configuration</h2> <p>CORS (Cross-Origin Resource Sharing) controls which browser origins can make requests to your API. It’s a browser security mechanism — it does not protect server-to-server calls, but it does protect your users’ sessions from being hijacked by malicious web pages.</p> <h3 id="the-wrong-way">The Wrong Way</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// NEVER do this in production</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOriginPatterns</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"*"</span><span class="o">));</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowCredentials</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>  <span class="c1">// This combination is especially dangerous</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">allowedOriginPatterns("*")</code> with <code class="language-plaintext highlighter-rouge">allowCredentials(true)</code> is invalid in modern CORS specs and may be rejected by the browser — but more importantly, it signals a fundamental misunderstanding of the threat model. Credentials (cookies, auth headers) should never be sent to any origin.</p> <h3 id="the-pattern-to-avoid--regex-that-can-be-tricked">The Pattern to Avoid — Regex That Can Be Tricked</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Looks safe, but can be tricked</span>
<span class="n">config</span><span class="o">.</span><span class="na">setAllowedOriginPatterns</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"https://*.yourdomain.com"</span><span class="o">));</span>
</code></pre></div></div> <p>This pattern allows <code class="language-plaintext highlighter-rouge">https://evil-yourdomain.com</code> to pass the check if the regex is imprecise. Subdomain wildcards require careful implementation.</p> <h3 id="the-right-way--explicit-allowlist">The Right Way — Explicit Allowlist</h3> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Bean</span>
<span class="kd">public</span> <span class="nc">CorsConfigurationSource</span> <span class="nf">corsConfigurationSource</span><span class="o">()</span> <span class="o">{</span>
    <span class="nc">CorsConfiguration</span> <span class="n">config</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CorsConfiguration</span><span class="o">();</span>

    <span class="c1">// Explicit, complete list of allowed origins</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedOrigins</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span>
        <span class="s">"https://www.tenant-a.example.com"</span><span class="o">,</span>
        <span class="s">"https://www.tenant-b.example.com"</span><span class="o">,</span>
        <span class="s">"https://admin.yoursaas.com"</span>
    <span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedMethods</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"GET"</span><span class="o">,</span> <span class="s">"POST"</span><span class="o">,</span> <span class="s">"PATCH"</span><span class="o">,</span> <span class="s">"DELETE"</span><span class="o">,</span> <span class="s">"OPTIONS"</span><span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedHeaders</span><span class="o">(</span><span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span>
        <span class="s">"Authorization"</span><span class="o">,</span>
        <span class="s">"Content-Type"</span><span class="o">,</span>
        <span class="s">"X-API-KEY"</span>
    <span class="o">));</span>

    <span class="n">config</span><span class="o">.</span><span class="na">setAllowCredentials</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>  <span class="c1">// Required for cookies</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setMaxAge</span><span class="o">(</span><span class="mi">3600L</span><span class="o">);</span>           <span class="c1">// Cache preflight for 1 hour</span>

    <span class="nc">UrlBasedCorsConfigurationSource</span> <span class="n">source</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UrlBasedCorsConfigurationSource</span><span class="o">();</span>
    <span class="n">source</span><span class="o">.</span><span class="na">registerCorsConfiguration</span><span class="o">(</span><span class="s">"/api/**"</span><span class="o">,</span> <span class="n">config</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">source</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>For a multi-tenant system with dynamic tenant onboarding</strong>, maintain the allowed origins list in the same place you store tenant domain configuration — loaded at startup, refreshable via a cache. When a new tenant is onboarded, their frontend domain is added to the CORS allowlist as part of the provisioning process.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Bean</span>
<span class="kd">public</span> <span class="nc">CorsConfigurationSource</span> <span class="nf">corsConfigurationSource</span><span class="o">()</span> <span class="o">{</span>
    <span class="c1">// Load dynamically from tenant registry</span>
    <span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">allowedOrigins</span> <span class="o">=</span> <span class="n">tenantRegistry</span><span class="o">.</span><span class="na">getAllDomains</span><span class="o">()</span>
        <span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">domain</span> <span class="o">-&gt;</span> <span class="s">"https://www."</span> <span class="o">+</span> <span class="n">domain</span><span class="o">)</span>
        <span class="o">.</span><span class="na">collect</span><span class="o">(</span><span class="nc">Collectors</span><span class="o">.</span><span class="na">toList</span><span class="o">());</span>

    <span class="c1">// Add platform admin origin</span>
    <span class="n">allowedOrigins</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="s">"https://admin."</span> <span class="o">+</span> <span class="n">platformDomain</span><span class="o">);</span>

    <span class="nc">CorsConfiguration</span> <span class="n">config</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">CorsConfiguration</span><span class="o">();</span>
    <span class="n">config</span><span class="o">.</span><span class="na">setAllowedOrigins</span><span class="o">(</span><span class="n">allowedOrigins</span><span class="o">);</span>
    <span class="c1">// ... rest of config</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="common-authorization-mistakes">Common Authorization Mistakes</h2> <h3 id="1-authorizing-reads-but-not-writes">1. Authorizing Reads but Not Writes</h3> <p>A common pattern: GET endpoints are secured, but PATCH/DELETE endpoints on the same resource are missed.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Secure the write endpoints explicitly — don't assume URL rules catch everything</span>
<span class="nd">@PatchMapping</span><span class="o">(</span><span class="s">"/{id}"</span><span class="o">)</span>
<span class="nd">@PreAuthorize</span><span class="o">(</span><span class="s">"hasRole('ADMIN')"</span><span class="o">)</span>  <span class="c1">// Don't forget this on write methods</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">update</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="no">UUID</span> <span class="n">id</span><span class="o">,</span> <span class="o">...)</span> <span class="o">{</span> <span class="o">...</span> <span class="o">}</span>
</code></pre></div></div> <h3 id="2-trusting-the-client-for-role-escalation">2. Trusting the Client for Role Escalation</h3> <p>If a client can pass their own role in a request body or query parameter, and the server uses it without re-validating from the token, that’s a privilege escalation vulnerability.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// WRONG — trusts client-provided role</span>
<span class="nd">@PostMapping</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">create</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateDto</span> <span class="n">dto</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">role</span> <span class="o">=</span> <span class="n">dto</span><span class="o">.</span><span class="na">getRole</span><span class="o">();</span>  <span class="c1">// attacker sets this to "ADMIN"</span>
    <span class="n">userService</span><span class="o">.</span><span class="na">createWithRole</span><span class="o">(</span><span class="n">role</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// CORRECT — derive role from authenticated principal</span>
<span class="nd">@PostMapping</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">create</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateDto</span> <span class="n">dto</span><span class="o">,</span>
                   <span class="nd">@AuthenticationPrincipal</span> <span class="nc">UserDetails</span> <span class="n">currentUser</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">role</span> <span class="o">=</span> <span class="n">determineAllowedRole</span><span class="o">(</span><span class="n">currentUser</span><span class="o">);</span>  <span class="c1">// derived from token, not request body</span>
    <span class="n">userService</span><span class="o">.</span><span class="na">createWithRole</span><span class="o">(</span><span class="n">role</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <h3 id="3-missing-cors-preflight-handling">3. Missing CORS Preflight Handling</h3> <p>Browsers send an <code class="language-plaintext highlighter-rouge">OPTIONS</code> preflight request before state-changing cross-origin requests. If your security config blocks <code class="language-plaintext highlighter-rouge">OPTIONS</code> requests (requiring authentication for all methods), CORS will silently fail.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Ensure OPTIONS is permitted (Spring Security handles this automatically if CORS is configured,</span>
<span class="c1">// but verify it's not being blocked by an overly broad auth rule)</span>
<span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="nc">HttpMethod</span><span class="o">.</span><span class="na">OPTIONS</span><span class="o">,</span> <span class="s">"/**"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>
</code></pre></div></div> <h3 id="4-using-secured-instead-of-preauthorize">4. Using <code class="language-plaintext highlighter-rouge">@Secured</code> Instead of <code class="language-plaintext highlighter-rouge">@PreAuthorize</code></h3> <p><code class="language-plaintext highlighter-rouge">@Secured</code> only supports role names. <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> supports SpEL expressions, which allows compound conditions (<code class="language-plaintext highlighter-rouge">hasRole('ADMIN') and !hasRole('API_CLIENT')</code>). Prefer <code class="language-plaintext highlighter-rouge">@PreAuthorize</code> for flexibility.</p> <hr/> <h2 id="integration-tests">Integration Tests</h2> <p>The most important tests at this layer are integration tests that exercise the complete filter chain — not mocked units.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SpringBootTest</span>
<span class="nd">@AutoConfigureMockMvc</span>
<span class="kd">class</span> <span class="nc">SecurityConfigIT</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">publicEndpoint_noAuth_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/appoint/day/2026-05-12"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">protectedEndpoint_noAuth_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isUnauthorized</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">protectedEndpoint_userRole_adminEndpoint_returns403</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">userToken</span> <span class="o">=</span> <span class="n">generateToken</span><span class="o">(</span><span class="s">"ROLE_USER"</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/users"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">userToken</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isForbidden</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminEndpoint_adminRole_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">adminToken</span> <span class="o">=</span> <span class="n">generateToken</span><span class="o">(</span><span class="s">"ROLE_ADMIN"</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/users"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Authorization"</span><span class="o">,</span> <span class="s">"Bearer "</span> <span class="o">+</span> <span class="n">adminToken</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">corsPreflightRequest_allowedOrigin_returns200</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">options</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Origin"</span><span class="o">,</span> <span class="s">"https://www.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Access-Control-Request-Method"</span><span class="o">,</span> <span class="s">"GET"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">())</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">header</span><span class="o">().</span><span class="na">exists</span><span class="o">(</span><span class="s">"Access-Control-Allow-Origin"</span><span class="o">));</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">corsPreflightRequest_unknownOrigin_noCorsHeader</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">options</span><span class="o">(</span><span class="s">"/api/clients"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Origin"</span><span class="o">,</span> <span class="s">"https://evil.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Access-Control-Request-Method"</span><span class="o">,</span> <span class="s">"GET"</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">header</span><span class="o">().</span><span class="na">doesNotExist</span><span class="o">(</span><span class="s">"Access-Control-Allow-Origin"</span><span class="o">));</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@SpringBootTest</span>
<span class="nd">@AutoConfigureMockMvc</span>
<span class="kd">class</span> <span class="nc">AdminTokenIT</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminApiKey_canAccessAdminEndpoints</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/admin/report/weekly"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">validAdminKey</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isOk</span><span class="o">());</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">adminApiKey_cannotAccessSuperadminEndpoints</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">mockMvc</span><span class="o">.</span><span class="na">perform</span><span class="o">(</span><span class="n">get</span><span class="o">(</span><span class="s">"/api/platform/tenants"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"Host"</span><span class="o">,</span> <span class="s">"api.tenant.example.com"</span><span class="o">)</span>
                <span class="o">.</span><span class="na">header</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">validAdminKey</span><span class="o">))</span>
               <span class="o">.</span><span class="na">andExpect</span><span class="o">(</span><span class="n">status</span><span class="o">().</span><span class="na">isForbidden</span><span class="o">());</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Role Design
  [ ] Roles are documented with their permitted operations
  [ ] ROLE_SUPERADMIN is never issued via normal auth flow
  [ ] Role escalation via request body is not possible

URL-Based Authorization
  [ ] Public endpoints are explicitly listed (no implicit opens)
  [ ] /api/auth/** is allowed (not accidentally locked)
  [ ] OPTIONS method is not blocked (CORS preflight works)

Method-Level Authorization
  [ ] All POST/PATCH/DELETE endpoints have @PreAuthorize
  [ ] Read endpoints returning sensitive data have @PreAuthorize
  [ ] @PreAuthorize uses roles derived from token, not request body

CORS
  [ ] allowedOrigins is an explicit whitelist (no wildcard with credentials)
  [ ] Allowed origins list matches the tenant registry
  [ ] Unknown origin returns no Access-Control-Allow-Origin header
  [ ] CORS preflight (OPTIONS) returns correct headers for known origins

Tests
  [ ] Public endpoint, no auth → 200
  [ ] Protected endpoint, no auth → 401
  [ ] Protected endpoint, wrong role → 403
  [ ] Protected endpoint, correct role → 200
  [ ] CORS: known origin → Access-Control-Allow-Origin present
  [ ] CORS: unknown origin → Access-Control-Allow-Origin absent
  [ ] Admin API key → admin endpoints accessible
  [ ] Admin API key → superadmin endpoints blocked
</code></pre></div></div> <hr/> <h2 id="the-full-chain-in-perspective">The Full Chain in Perspective</h2> <p>This is the final layer. When all five layers are working correctly, a request has passed:</p> <table> <thead> <tr> <th>Layer</th> <th>Concern</th> <th>Failure Response</th> </tr> </thead> <tbody> <tr> <td>nginx</td> <td>TLS, rate limit, logging</td> <td>Connection refused / 429</td> </tr> <tr> <td>TenantResolverFilter</td> <td>Tenant identity</td> <td>400 Bad Request</td> </tr> <tr> <td>JwtAuthenticationFilter</td> <td>User identity</td> <td>401 Unauthorized</td> </tr> <tr> <td>ApiKeyFilter</td> <td>Machine identity</td> <td>401 Unauthorized</td> </tr> <tr> <td>Spring Security</td> <td>Authorization, CORS</td> <td>403 Forbidden / CORS error</td> </tr> </tbody> </table> <p>Each layer has a single job. Each layer has tests. Each layer fails explicitly rather than silently.</p> <p>The result is a security chain you can audit by reading down the filter order — and one where a failure at any layer is visible, testable, and fixable without touching the others.</p> <hr/> <p><em>This concludes the five-layer deep-dive series. Return to the overview: <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS: Layered Filters, JWT, and API Keys</a></em></p>]]></content><author><name></name></author><category term="architecture"/><category term="spring-security"/><category term="authorization"/><category term="cors"/><category term="roles"/><category term="java"/><category term="spring-boot"/><summary type="html"><![CDATA[A deep dive into Spring Security's authorization layer — covering @PreAuthorize, tenant-scoped roles, CORS configuration mistakes, and the integration tests that validate the full security chain.]]></summary></entry><entry><title type="html">Layer 4 — API Key Authentication: Securing Machine-to-Machine Requests</title><link href="https://crystalhansen.ca/blog/2026/layer4-api-key-authentication/" rel="alternate" type="text/html" title="Layer 4 — API Key Authentication: Securing Machine-to-Machine Requests"/><published>2026-05-12T14:30:00+00:00</published><updated>2026-05-12T14:30:00+00:00</updated><id>https://crystalhansen.ca/blog/2026/layer4-api-key-authentication</id><content type="html" xml:base="https://crystalhansen.ca/blog/2026/layer4-api-key-authentication/"><![CDATA[<blockquote> <p>This post is part of the <a href="/blog/2026/security-auditing-layers-multitenant-saas">Security Auditing in a Multi-Tenant SaaS</a> series. Each post deep-dives one layer of the filter chain.</p> </blockquote> <p>JWTs work well for human users. A person logs in, gets a token, and uses it for the next hour. But scheduled jobs, internal automation scripts, and admin tooling don’t have a human at the keyboard to re-authenticate. They need a credential that works unattended.</p> <p>API keys solve this — but they introduce their own set of risks that need to be managed carefully.</p> <hr/> <h2 id="when-to-use-api-keys-vs-jwts">When to Use API Keys vs JWTs</h2> <table> <thead> <tr> <th>Scenario</th> <th>Auth Method</th> <th>Why</th> </tr> </thead> <tbody> <tr> <td>User logs in via browser</td> <td>JWT + session cookie</td> <td>Short-lived, tied to identity, revocable via session</td> </tr> <tr> <td>Mobile client makes API calls</td> <td>JWT</td> <td>Stateless, short-lived</td> </tr> <tr> <td>Scheduled job (nightly report)</td> <td>API key</td> <td>Unattended, no human to re-auth</td> </tr> <tr> <td>Internal admin tooling</td> <td>API key</td> <td>Trusted internal caller, long-lived credential</td> </tr> <tr> <td>External webhook receiver</td> <td>API key + signature</td> <td>Inbound, no session possible</td> </tr> <tr> <td>Service-to-service (ML scorer)</td> <td>API key or mTLS</td> <td>Machine identity, no user context</td> </tr> </tbody> </table> <p>The key distinction is <strong>attended vs. unattended</strong>. If there’s a human who can re-authenticate, use JWTs. If the caller is a machine running on a schedule or reacting to events, use an API key.</p> <hr/> <h2 id="the-filter">The Filter</h2> <p>The API key filter sits after the JWT filter in the chain. By the time a request reaches this filter, one of two things is true:</p> <ol> <li>The JWT filter already authenticated the request (SecurityContext is populated)</li> <li>No valid JWT was found (SecurityContext is empty)</li> </ol> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="nd">@Order</span><span class="o">(</span><span class="mi">3</span><span class="o">)</span>  <span class="c1">// after TenantResolver (1) and JwtFilter (2)</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ApiKeyFilter</span> <span class="kd">extends</span> <span class="nc">OncePerRequestFilter</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ApiKeyService</span> <span class="n">apiKeyService</span><span class="o">;</span>

    <span class="nd">@Override</span>
    <span class="kd">protected</span> <span class="kt">void</span> <span class="nf">doFilterInternal</span><span class="o">(</span><span class="nc">HttpServletRequest</span> <span class="n">request</span><span class="o">,</span>
                                    <span class="nc">HttpServletResponse</span> <span class="n">response</span><span class="o">,</span>
                                    <span class="nc">FilterChain</span> <span class="n">chain</span><span class="o">)</span>
            <span class="kd">throws</span> <span class="nc">ServletException</span><span class="o">,</span> <span class="nc">IOException</span> <span class="o">{</span>

        <span class="c1">// CRITICAL: If a valid session/JWT already authenticated this request, skip.</span>
        <span class="c1">// This is the session passthrough fix — see below.</span>
        <span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">();</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">existingAuth</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">existingAuth</span><span class="o">.</span><span class="na">isAuthenticated</span><span class="o">())</span> <span class="o">{</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="nc">String</span> <span class="n">apiKey</span> <span class="o">=</span> <span class="n">request</span><span class="o">.</span><span class="na">getHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">);</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">apiKey</span> <span class="o">==</span> <span class="kc">null</span> <span class="o">||</span> <span class="n">apiKey</span><span class="o">.</span><span class="na">isBlank</span><span class="o">())</span> <span class="o">{</span>
            <span class="c1">// No API key provided — pass through. Spring Security decides if auth is required.</span>
            <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="c1">// Validate the key</span>
        <span class="nc">ApiKeyPrincipal</span> <span class="n">principal</span> <span class="o">=</span> <span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">apiKey</span><span class="o">,</span> <span class="nc">TenantContext</span><span class="o">.</span><span class="na">getCurrentTenant</span><span class="o">());</span>

        <span class="k">if</span> <span class="o">(</span><span class="n">principal</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
            <span class="n">response</span><span class="o">.</span><span class="na">sendError</span><span class="o">(</span><span class="nc">HttpServletResponse</span><span class="o">.</span><span class="na">SC_UNAUTHORIZED</span><span class="o">,</span> <span class="s">"Invalid API key"</span><span class="o">);</span>
            <span class="k">return</span><span class="o">;</span>
        <span class="o">}</span>

        <span class="c1">// Set authentication in SecurityContext</span>
        <span class="nc">ApiKeyAuthenticationToken</span> <span class="n">auth</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ApiKeyAuthenticationToken</span><span class="o">(</span>
            <span class="n">principal</span><span class="o">,</span>
            <span class="n">principal</span><span class="o">.</span><span class="na">getAuthorities</span><span class="o">()</span>
        <span class="o">);</span>
        <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">setAuthentication</span><span class="o">(</span><span class="n">auth</span><span class="o">);</span>

        <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="the-session-passthrough-bug">The Session Passthrough Bug</h2> <p>This filter has a subtle interaction with the JWT/session filter that produced a real production bug worth documenting.</p> <p><strong>The scenario:</strong></p> <ol> <li>A browser user is logged in with a valid session cookie</li> <li>The user’s request reaches the API key filter</li> <li>The request happens to include an <code class="language-plaintext highlighter-rouge">X-API-KEY</code> header (e.g., from a browser extension, a misconfigured fetch call, or a developer testing tool)</li> <li>That API key is invalid</li> <li>The filter returns <code class="language-plaintext highlighter-rouge">401</code> — even though the user has a perfectly valid session</li> </ol> <p>The user sees a confusing <code class="language-plaintext highlighter-rouge">401 Unauthorized</code> with no apparent reason.</p> <p><strong>The fix:</strong> Check whether the <code class="language-plaintext highlighter-rouge">SecurityContext</code> already contains a valid authentication before evaluating the API key header. If auth already exists, the filter is a no-op. The session is authoritative; the API key header is irrelevant.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// This check must be FIRST in the filter, before reading X-API-KEY</span>
<span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">();</span>
<span class="k">if</span> <span class="o">(</span><span class="n">existingAuth</span> <span class="o">!=</span> <span class="kc">null</span> <span class="o">&amp;&amp;</span> <span class="n">existingAuth</span><span class="o">.</span><span class="na">isAuthenticated</span><span class="o">())</span> <span class="o">{</span>
    <span class="n">chain</span><span class="o">.</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
    <span class="k">return</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p>This is a good example of why filter ordering and precedence must be explicitly tested, not assumed.</p> <hr/> <h2 id="storing-api-keys-securely">Storing API Keys Securely</h2> <p>An API key is a secret credential. Storing it in plaintext in the database means a database breach exposes every key immediately.</p> <p><strong>The pattern:</strong> store a hash of the key, validate by hashing the incoming value and comparing.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// On key creation — return the plaintext key to the caller ONCE</span>
<span class="kd">public</span> <span class="nc">ApiKeyCreationResult</span> <span class="nf">createKey</span><span class="o">(</span><span class="nc">String</span> <span class="n">tenantDomain</span><span class="o">,</span> <span class="nc">String</span> <span class="n">label</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">rawKey</span> <span class="o">=</span> <span class="n">generateSecureKey</span><span class="o">();</span>       <span class="c1">// 32 bytes of SecureRandom, base64url-encoded</span>
    <span class="nc">String</span> <span class="n">hashedKey</span> <span class="o">=</span> <span class="n">hash</span><span class="o">(</span><span class="n">rawKey</span><span class="o">);</span>           <span class="c1">// SHA-256 or bcrypt</span>

    <span class="nc">ApiKey</span> <span class="n">entity</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ApiKey</span><span class="o">();</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setDomain</span><span class="o">(</span><span class="n">tenantDomain</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setLabel</span><span class="o">(</span><span class="n">label</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setKeyHash</span><span class="o">(</span><span class="n">hashedKey</span><span class="o">);</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setCreatedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">());</span>
    <span class="n">entity</span><span class="o">.</span><span class="na">setLastUsedAt</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>
    <span class="n">repository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">entity</span><span class="o">);</span>

    <span class="c1">// Return plaintext key to caller — this is the ONLY time it's available</span>
    <span class="k">return</span> <span class="k">new</span> <span class="nf">ApiKeyCreationResult</span><span class="o">(</span><span class="n">entity</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span> <span class="n">rawKey</span><span class="o">);</span>
<span class="o">}</span>

<span class="c1">// On validation — hash the incoming key and compare</span>
<span class="kd">public</span> <span class="nc">ApiKeyPrincipal</span> <span class="nf">validate</span><span class="o">(</span><span class="nc">String</span> <span class="n">rawKey</span><span class="o">,</span> <span class="nc">String</span> <span class="n">tenantDomain</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">String</span> <span class="n">hash</span> <span class="o">=</span> <span class="n">hash</span><span class="o">(</span><span class="n">rawKey</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">repository</span><span class="o">.</span><span class="na">findByKeyHashAndDomain</span><span class="o">(</span><span class="n">hash</span><span class="o">,</span> <span class="n">tenantDomain</span><span class="o">)</span>
                     <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">key</span> <span class="o">-&gt;</span> <span class="o">{</span>
                         <span class="n">key</span><span class="o">.</span><span class="na">setLastUsedAt</span><span class="o">(</span><span class="nc">Instant</span><span class="o">.</span><span class="na">now</span><span class="o">());</span>  <span class="c1">// audit trail</span>
                         <span class="n">repository</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
                         <span class="k">return</span> <span class="k">new</span> <span class="nf">ApiKeyPrincipal</span><span class="o">(</span><span class="n">key</span><span class="o">);</span>
                     <span class="o">})</span>
                     <span class="o">.</span><span class="na">orElse</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><strong>Hashing algorithm choice:</strong></p> <ul> <li><strong>SHA-256</strong>: Fast, appropriate for API keys since the keys are long (256 bits of entropy) — brute force is infeasible regardless of hash speed.</li> <li><strong>bcrypt</strong>: Better for passwords (short, user-chosen secrets). Adds latency for a high-volume API key endpoint.</li> </ul> <p>For API keys with high entropy, SHA-256 is standard and widely used (GitHub, Stripe, and others follow this pattern).</p> <p><strong>Key format:</strong> Prefix your keys with a recognizable pattern so they’re easy to identify if accidentally leaked in logs, screenshots, or error messages:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>myapp_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
^           ^
prefix      32 random bytes (base64url)
</code></pre></div></div> <p>GitHub, Stripe, and Anthropic all use this pattern. Tools like <code class="language-plaintext highlighter-rouge">git secrets</code> and secret scanning in GitHub can be configured to catch these prefixes before they’re committed.</p> <hr/> <h2 id="key-scoping-and-tenant-isolation">Key Scoping and Tenant Isolation</h2> <p>API keys must be tenant-scoped. A key for tenant A must not work against tenant B’s API subdomain.</p> <p>The <code class="language-plaintext highlighter-rouge">validate()</code> method above passes <code class="language-plaintext highlighter-rouge">tenantDomain</code> from <code class="language-plaintext highlighter-rouge">TenantContext</code> and matches it against the key’s stored domain. A valid key for the wrong tenant returns <code class="language-plaintext highlighter-rouge">null</code> and the filter returns <code class="language-plaintext highlighter-rouge">401</code>.</p> <p>This means:</p> <ul> <li>Keys can’t be shared across tenants even intentionally</li> <li>A compromised key for one tenant doesn’t affect others</li> <li>The tenant domain is part of the key’s identity, visible in audit logs</li> </ul> <hr/> <h2 id="rotation-and-revocation">Rotation and Revocation</h2> <p>Unlike JWTs, API keys don’t expire automatically. This is their advantage (no re-auth) and their liability (a leaked key stays valid until explicitly revoked).</p> <p><strong>Rotation procedure:</strong></p> <ol> <li>Create a new key (system returns plaintext once)</li> <li>Update the caller (scheduled job, script) with the new key</li> <li>Verify the new key is working</li> <li>Delete the old key</li> </ol> <p>This is a zero-downtime rotation if done carefully. The two keys coexist briefly during the switchover.</p> <p><strong>Audit trail:</strong> The <code class="language-plaintext highlighter-rouge">lastUsedAt</code> timestamp on each key makes it possible to:</p> <ul> <li>Identify keys that haven’t been used in 90+ days (candidates for removal)</li> <li>Detect unexpected usage patterns (a key used at 3am that normally runs at midnight)</li> <li>Confirm a rotated key is no longer being used before deletion</li> </ul> <hr/> <h2 id="endpoint-allowlist">Endpoint Allowlist</h2> <p>Not every endpoint should accept API key authentication. Admin endpoints for bulk operations, data exports, or configuration changes should require explicit allowlisting rather than being implicitly available to any valid key.</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Configuration</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">SecurityConfig</span> <span class="o">{</span>

    <span class="nd">@Bean</span>
    <span class="kd">public</span> <span class="nc">SecurityFilterChain</span> <span class="nf">filterChain</span><span class="o">(</span><span class="nc">HttpSecurity</span> <span class="n">http</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">http</span><span class="o">.</span><span class="na">authorizeHttpRequests</span><span class="o">(</span><span class="n">auth</span> <span class="o">-&gt;</span> <span class="n">auth</span>
            <span class="c1">// Public endpoints — no auth required</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/appoint/day/**"</span><span class="o">,</span> <span class="s">"/api/appoint/request"</span><span class="o">).</span><span class="na">permitAll</span><span class="o">()</span>

            <span class="c1">// API-key-accessible admin endpoints — explicitly listed</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/report/weekly"</span><span class="o">).</span><span class="na">hasAuthority</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)</span>
            <span class="o">.</span><span class="na">requestMatchers</span><span class="o">(</span><span class="s">"/api/admin/import/csv"</span><span class="o">).</span><span class="na">hasAuthority</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)</span>

            <span class="c1">// Everything else requires JWT-based auth</span>
            <span class="o">.</span><span class="na">anyRequest</span><span class="o">().</span><span class="na">hasAnyRole</span><span class="o">(</span><span class="s">"USER"</span><span class="o">,</span> <span class="s">"ADMIN"</span><span class="o">)</span>
        <span class="o">);</span>
        <span class="k">return</span> <span class="n">http</span><span class="o">.</span><span class="na">build</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p>This makes the attack surface explicit: you can read the security config and know exactly which endpoints an API key can reach.</p> <hr/> <h2 id="testing">Testing</h2> <p>The test matrix for the API key filter must cover all the interaction cases, especially the session passthrough scenarios:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">class</span> <span class="nc">ApiKeyFilterTest</span> <span class="o">{</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">validApiKey_authenticatesRequest</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">key</span> <span class="o">=</span> <span class="s">"myapp_test_validkeyvalue"</span><span class="o">;</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="s">"tenant.example.com"</span><span class="o">))</span>
            <span class="o">.</span><span class="na">thenReturn</span><span class="o">(</span><span class="k">new</span> <span class="nc">ApiKeyPrincipal</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">,</span> <span class="nc">List</span><span class="o">.</span><span class="na">of</span><span class="o">(</span><span class="s">"ROLE_API_CLIENT"</span><span class="o">)));</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="n">key</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()).</span><span class="na">isNotNull</span><span class="o">();</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isNotEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">invalidApiKey_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">any</span><span class="o">())).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_badkey"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">noApiKey_passesThroughToNextFilter</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// No X-API-KEY header — chain should continue</span>
        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">verify</span><span class="o">(</span><span class="n">chain</span><span class="o">).</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">getAuthentication</span><span class="o">()).</span><span class="na">isNull</span><span class="o">();</span>
    <span class="o">}</span>

    <span class="c1">// THE CRITICAL SESSION PASSTHROUGH TEST</span>
    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">existingValidSession_withInvalidApiKey_doesNotReturn401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// Simulate a valid session already in the SecurityContext (set by JWT filter)</span>
        <span class="nc">Authentication</span> <span class="n">existingAuth</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">UsernamePasswordAuthenticationToken</span><span class="o">(</span>
            <span class="s">"alice"</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="nc">AuthorityUtils</span><span class="o">.</span><span class="na">createAuthorityList</span><span class="o">(</span><span class="s">"ROLE_USER"</span><span class="o">)</span>
        <span class="o">);</span>
        <span class="nc">SecurityContextHolder</span><span class="o">.</span><span class="na">getContext</span><span class="o">().</span><span class="na">setAuthentication</span><span class="o">(</span><span class="n">existingAuth</span><span class="o">);</span>

        <span class="c1">// Add an invalid API key header (simulates browser extension / bad client config)</span>
        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_invalidkey"</span><span class="o">);</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">any</span><span class="o">())).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="c1">// MUST pass through — the existing session should NOT be invalidated</span>
        <span class="n">verify</span><span class="o">(</span><span class="n">chain</span><span class="o">).</span><span class="na">doFilter</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isNotEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>

    <span class="nd">@Test</span>
    <span class="kt">void</span> <span class="nf">validApiKey_wrongTenant_returns401</span><span class="o">()</span> <span class="kd">throws</span> <span class="nc">Exception</span> <span class="o">{</span>
        <span class="c1">// Key is valid for tenant-a, but request is for tenant-b</span>
        <span class="n">when</span><span class="o">(</span><span class="n">apiKeyService</span><span class="o">.</span><span class="na">validate</span><span class="o">(</span><span class="n">any</span><span class="o">(),</span> <span class="n">eq</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">))).</span><span class="na">thenReturn</span><span class="o">(</span><span class="kc">null</span><span class="o">);</span>

        <span class="n">request</span><span class="o">.</span><span class="na">addHeader</span><span class="o">(</span><span class="s">"X-API-KEY"</span><span class="o">,</span> <span class="s">"myapp_test_tenantakey"</span><span class="o">);</span>
        <span class="nc">TenantContext</span><span class="o">.</span><span class="na">setCurrentTenant</span><span class="o">(</span><span class="s">"tenant-b.example.com"</span><span class="o">);</span>

        <span class="n">filter</span><span class="o">.</span><span class="na">doFilterInternal</span><span class="o">(</span><span class="n">request</span><span class="o">,</span> <span class="n">response</span><span class="o">,</span> <span class="n">chain</span><span class="o">);</span>

        <span class="n">assertThat</span><span class="o">(</span><span class="n">response</span><span class="o">.</span><span class="na">getStatus</span><span class="o">()).</span><span class="na">isEqualTo</span><span class="o">(</span><span class="mi">401</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <hr/> <h2 id="audit-checklist">Audit Checklist</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Key Storage
  [ ] API keys are stored as hashes (SHA-256 or bcrypt), never plaintext
  [ ] Plaintext key is returned exactly once at creation, not stored
  [ ] Keys have a recognizable prefix for secret scanning

Key Design
  [ ] Keys are &gt;= 256 bits of entropy (32 bytes from SecureRandom)
  [ ] Keys are tenant-scoped — cross-tenant use is rejected
  [ ] lastUsedAt is updated on every successful validation (audit trail)

Filter Logic
  [ ] Existing valid session → filter is a no-op (session passthrough)
  [ ] No X-API-KEY header → passes through (endpoint decides)
  [ ] Invalid key → 401 (not 403, not 500)
  [ ] Valid key, wrong tenant → 401

Endpoint Control
  [ ] API-key-accessible endpoints are explicitly listed, not open-ended
  [ ] ROLE_API_CLIENT is distinct from ROLE_USER and ROLE_ADMIN
  [ ] Sensitive endpoints (bulk delete, export) require additional scope or are excluded

Lifecycle
  [ ] Rotation procedure is documented and tested
  [ ] Keys unused for 90+ days are flagged for review
  [ ] Revocation removes the key immediately (no TTL/cache delay)

Tests
  [ ] Valid key → authenticated
  [ ] Invalid key → 401
  [ ] No key → passes through
  [ ] Valid session + invalid key → passes through (not 401)
  [ ] Valid key + wrong tenant → 401
</code></pre></div></div> <hr/> <p><em>Next in the series: <a href="/blog/2026/layer5-spring-security-roles-cors">Layer 5 — Spring Security: Role-Based Authorization and CORS</a></em></p> <hr/> <blockquote> <p><strong>A note on publishing this post:</strong> The first push of this article was rejected by GitHub’s secret scanner because the example key format (<code class="language-plaintext highlighter-rouge">sk_live_...</code>) matched Stripe’s live API key regex exactly. A blog post about API key security, blocked for containing what looked like a real API key. The examples were updated to use a clearly app-namespaced prefix (<code class="language-plaintext highlighter-rouge">myapp_live_...</code>) instead. This is the secret scanning feature working exactly as intended — and a good reminder that realistic-looking placeholder values in documentation are a real source of accidental secret exposure.</p> </blockquote>]]></content><author><name></name></author><category term="architecture"/><category term="api-key"/><category term="authentication"/><category term="spring-boot"/><category term="security"/><category term="automation"/><category term="java"/><summary type="html"><![CDATA[A deep dive into API key authentication in a Spring Boot multi-tenant API — covering when to use keys vs JWTs, hashing at rest, the session passthrough bug, and a complete test matrix.]]></summary></entry></feed>